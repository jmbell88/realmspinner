# Warlock Studio audit — 2026-09-07

**Recommendation:** close the seven Criticals before the next installer build — four of them
destroy or corrupt work a user has already done — then take the six per-keystroke undo Highs as
one sweep, since they are one defect wearing six file names and each has its own fix already
written in the file next door.

**112 findings: 7 Critical, 36 High, 39 Medium, 29 Low; 1 settled by observation.**

**Closed 2026-09-07: 110 of the 111 open findings are built and struck below**, each with a regression test that failed against the unfixed code. The one still open is `docs-06`, which needs a measurement before either number in it can be called correct; it is now `TODO.md`'s **P35**. Two further items the fix pass surfaced rather than closed are **P36** (whether Clay wants the "adjust last operation" card the removed bookkeeping fed) and **P37** (three more stages that rename onto a served name and are not yet classified). Gate after the fixes: `ruff check .` clean, `uv run pytest` **18,747 passed, 47 skipped**, `scripts/preflight.py --fast` passed.

Audited: version **0.0.38**, working tree at **`b0ecb0e0`**. Dirty at start:

```
 M .claude/skills/warlock-audit/SKILL.md
 M .claude/skills/warlock-audit/references/brief-explorer.md
 M .claude/skills/warlock-audit/references/constraints.md
 M .claude/skills/warlock-audit/references/report-template.md
 M .claude/skills/warlock-audit/references/subsystems.md
```

Those five are the user's own edits to this skill; nothing under `src/`, `tests/` or `docs/` was
modified. Scope: `all` → slices `shell`, `create`, `inker`, `clay`, `poser`, `troupe`, `plotter`,
`packwright`, `muse`, `sirens`, `service`, `pipelines`, `docs`, `tour`, split into **53 segments**
and one explorer each (`shell`: boot, documents, chrome, widgets, home-library, review-settings;
`create`: brief, workspace, viewer, panes; `inker`: document, paint, sheets, codecs, flourish,
mode, panes; `clay`: mesh, document, mode; `poser`: rig, poses, mode; `troupe`: engine, jobs,
render; `plotter`: map, tiles, mode; `packwright`: packer, mode; `muse`: engine, mode, jobs;
`sirens`: engine, playback, panes; `service`: queue, kinds, assets, gates; `pipelines`: children,
image, mesh, install; `docs`: A-B, C1, C2, D, E, F1, F2; `tour`: 1).
`uv run ruff check .`: **All checks passed!**
The suite was not run by the audit pass; findings are from reading and from the offline probes below. It was run once by the fix pass, after every fixer had returned.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong result the
user will hit, or a promise in the docs the code breaks. *Medium*: a defect with a workaround, or a
hard-invariant violation not yet reachable. *Low*: hardening, consistency, naming. *Reproduced*: an
executable example confirmed it (scripts under
`<scratchpad>/audit-2026-09-07/probes/`, run with `uv run --no-sync python <script>`).
*Inspected*: follows from the current code path. *Evidence gap*: release proof is missing from the
repository, not a demonstrated failure.

**Second-look.** Every Critical and High below was re-read by the orchestrator against the working
tree, and **all 22 probe scripts left by explorers were re-run and all 22 reproduced**. Dropped or
downgraded during merge: **none**. Widened during merge: `docs-05` (four dead commit hashes became
six — the orchestrator verified two more that segment C1 had not checked). Merged from two segments
that saw one defect from two sides: **`tour-01`** (shell-home-library saw the tour's arrow key
moving the library grid; tour-1 saw any app keystroke driving the tour — one missing ownership
check), **`service-06`** (shell-documents and service-kinds both found the `PRODUCTS` count), and
**`docs-07`** (create-brief and docs-F1 both found chapter 02's asset-type list omitting
*Character*; kept at the higher of the two severities, since docs-F1 established it as an inventory
disagreeing with `_ORDERED` rather than only a missing mention).

Three explorers additionally discarded their own candidate findings after testing them rather than
reporting them thin — a `bridge_edges` twist that turned out to use an invalid selection, an imgui
row-placement that a headless probe showed lands correctly, and a `create_pixel_sheet` resurrection
race the worker's re-read already closes. Those are recorded here as evidence that the freshness
rule held, not as findings.

## Critical

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **service-01 — Service: four job kinds** | ~~**A cancel arriving after publish deletes the finished artifact and records the job cancelled.** `_sheet` (`src/warlock/_q_rig.py:448`), `_sprite_synthesis` (`src/warlock/_q_sprite.py:680`), `_tile_sheet` (`src/warlock/_q_tilesheet.py:285`) and `_tile_set` (`src/warlock/_q_tileset.py:461`) publish their completion marker after their last cancel check and never call `self._cancel.commit()`, unlike all eight sibling stages. `queue.py`'s `finally` then runs `_set_cancelled` + `_discard_artifacts` for any uncommitted cancel-flagged job. *Inspected; orchestrator verified the commit counts (0, 0, and the ones present belong to `_rig`/`_pixel_sheet`/`_retexture`) and read the discard branch.* Minutes of GPU or Blender output are deleted while the row claims the work never happened.~~ **Built 2026-09-07:** all four stages commit the cancel token after their completion-marker write, and `tests/test_job_durability.py`'s `PUBLISHERS` scan -- a hand-written table that had gone stale, which is how four more instances of a guarded defect shipped -- now covers all eight publishers. `test_a_cancel_after_publish_does_not_discard_the_finished_sheet_draft_or_tileset`. | Add `if self._cancel is not None: self._cancel.commit()` after the completion-marker write in each of the four, mirroring `_charsheet` and `_pixel_sheet`. Regression: `test_a_cancel_after_publish_does_not_discard_the_finished_sheet_draft_or_tileset`. |
| **packwright-01 — Packwright: the packer** | ~~**Warlock writes `.wpack` files it can never reopen.** `MAX_SOURCE_PIXELS` (16M) is enforced only in `read_wpack`'s `_pixels_from` (`src/warlock/studio/packwright/wpack.py:345`); `PackDoc.add_source` (`src/warlock/studio/packwright/document.py:240`) refuses a duplicate key and `MAX_SPRITES` but has no pixel ceiling, and `wpack_bytes`/`snapshot_bytes` have none either. *Reproduced:* a 4096×4096 sprite (16,777,216 px) is accepted, written to 76,061 bytes, then refused on reopen. Breaks the reopenable-exports contract (INVARIANTS:215) with a document Warlock itself authored.~~ **Built 2026-09-07:** `add_source`/`replace_source` refuse past `MAX_SOURCE_PIXELS`, the same constant and comparison `read_wpack` uses. `test_a_document_with_a_source_past_max_source_pixels_is_refused_at_add_not_only_on_reopen`. | Refuse an over-ceiling sprite at `add_source`/`replace_source` using the same constant `read_wpack` enforces. Regression: `test_a_document_with_a_source_past_max_source_pixels_is_refused_at_add_not_only_on_reopen`. |
| **poser-01 — Poser: skeleton switch** | ~~**"Save clips" after switching skeletons overwrites the new template's clip library with the old skeleton's poses.** `set_template`'s `proceed()` (`src/warlock/studio/poser_mode.py:258`) resets `poses`, `presets` and `preview_path` but never touches `state.clips`/`clips_unsaved` and never calls `clips_refresh`; its guard asks only about the pose gizmo. `save_clips` (`src/warlock/studio/poser_mode.py:1286`) then passes the **new** `state.template` with a payload deep-copied from the **old** template's clips, and `service/clips.py` deliberately does not check bone names. *Inspected; orchestrator confirmed, and further found `clips_pump` is itself guarded on `clips_unsaved`, so even a refresh would decline to re-read.*~~ **Built 2026-09-07:** `proceed()` clears the clip fields and calls `clips_refresh`, and an unsaved-clips confirm now runs before the pose-gizmo guard. `test_switching_template_refreshes_the_clip_library_and_guards_unsaved_clip_edits`. | Reset the clip fields and call `clips_refresh(ctx)` in `proceed()`; extend the guard to `clips_unsaved`. Regression: `test_switching_template_refreshes_the_clip_library_and_guards_unsaved_clip_edits`. |
| **inker-01 — Inker: the Text tool** | ~~**An ordinary paragraph at a large point size takes down the whole app.** `text_stamp`'s docstring promises `None` "never an exception" and enumerates its declines; size-too-large is not among them, yet it calls `pixelguard.check` uncaught (`src/warlock/studio/inker/textstamp.py:132`), which raises `ValueError` past the 8192² ceiling. `inker_mode.stamp_text` (`src/warlock/studio/inker_mode.py:1680`) handles only `pixels is None` — no `except` — so it reaches `App.run`'s teardown and the native "had to close" dialog. *Inspected; orchestrator read both the contract and the caller.*~~ **Built 2026-09-07:** `text_stamp` catches the ceiling `ValueError` and returns `None`, as its docstring always promised. An existing test that pinned the raise was amended to pin the decline. `test_text_stamp_declines_rather_than_crashing_past_the_pixel_ceiling`. | Catch the `ValueError` inside `text_stamp` and return `None`, as the font-load and bbox failures already do. Regression: `test_text_stamp_declines_rather_than_crashing_past_the_pixel_ceiling`. |
| **inker-02 — Inker: masked regenerate** | ~~**A regeneration landing during a save writes into the layer stack the encoder is walking.** `land_inpaint` (`src/warlock/studio/panes/inker_bridge.py:771`) calls `apply_pixels` — autovivifying a cel and pushing an undo step — with no `tab.busy` check anywhere in its chain, while the sibling `_done_tileset_import` (`src/warlock/studio/inker_mode.py:1114`) explicitly re-checks `not target.busy` for the analogous write, citing "`add_tileset` pushes a history step into a stack an encode is walking". *Reproduced:* with `tab.saving = True`, `land_inpaint` returned `True`, `history.head` went 0→1, pixels changed, and a success toast fired. The hazard is a `stack.xml` that disagrees with its own PNG members.~~ **Built 2026-09-07:** `land_inpaint` re-checks `tab.busy` and toasts a refusal before `apply_pixels`. `test_land_inpaint_is_refused_while_the_tab_is_saving`. | Re-check `tab.busy` before `apply_pixels`; refuse and toast. Regression: `test_land_inpaint_is_refused_while_the_tab_is_saving`. |
| **shell-01 — Settings: LoRA registry** | ~~**A trained style can vanish from the registry with no error.** `import_lora`/`remove_imported_lora` (`src/warlock/generation.py:1384`) read all of `loras/manifests.json`, change one entry in memory and write the whole file back, staged but with no lock around the read-modify-write. The Settings pane's busy gates cover only the brief task that *queues* a train; the many-minutes job registers through the same unlocked door (`src/warlock/_q_lora.py:91`). *Inspected; orchestrator confirmed the worker call site and the whole-file rewrite.* The safetensors file is orphaned and unusable.~~ **Built 2026-09-07:** one process-wide `_MANIFEST_LOCK` around every read-modify-write of `manifests.json`, covering the pane door and `_q_lora` alike. `test_import_lora_and_a_concurrent_train_completion_do_not_drop_a_manifest_entry`. | Serialise every `manifests.json` write behind one lock shared by the pane and `_q_lora`, so a losing writer retries. Regression: `test_import_lora_and_a_concurrent_train_completion_do_not_drop_a_manifest_entry`. |
| **shell-02 — Library: the inspector** | ~~**A rename or retag is silently lost if you click another asset too quickly.** An edit the queue refused is parked in module-level `_unsent` (`src/warlock/studio/panes/inspector.py:570`) and retried only inside `_write_field`, which runs while that job's header is being drawn; `AppState.select` never drains it. *Inspected; orchestrator confirmed `_unsent` is touched nowhere else in `studio/`, and that its own comment assumes it "holds at most one entry per field being typed into right now" — the assumption a selection change breaks.* No toast, no error; the field reverts.~~ **Built 2026-09-07:** `flush_unsent_for` drains the outgoing job's parked edit on the frame the selection changes. `test_an_unsent_edit_is_not_lost_when_the_selection_changes_before_it_lands`. | Flush pending keys for the outgoing job when the selection changes. Regression: `test_an_unsent_edit_is_not_lost_when_the_selection_changes_before_it_lands`. |

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-01 — Clay: Properties** | ~~Position/rotation/scale and every generator parameter push **one undo step per keystroke**: `_transform` and `_generator` (`src/warlock/studio/panes/clay_props.py:148, 203`) call `set_transform`/`set_generator_params` under a bare `if changed:` with no `fold_undo`, while `_material` in the same file folds three times. *Inspected; orchestrator confirmed the fields are `input_float3`/`input_float4`, which fire per keystroke.* The 2026-09-06 audit fixed the neighbouring doors; these two were missed.~~ **Built 2026-09-07:** `fold_undo` after each transform and generator field. `test_editing_a_transform_or_generator_field_by_keystroke_is_one_undo_step`. | `controls.fold_undo(doc.history)` after each field. Regression: `test_editing_a_transform_or_generator_field_by_keystroke_is_one_undo_step`. |
| **clay-02 — Clay: Delete key** | ~~Deleting elements selected across two or more objects pushes **one undo step per object**, because `_delete` (`src/warlock/studio/clay_mode.py:879`) calls `selection.delete_selected` directly instead of routing through `clay_ops.run`, which folds. *Reproduced:* keyboard path 2 steps, menu path 1 step for the identical edit.~~ **Built 2026-09-07:** the Del handler routes through `clay_ops.run`. `test_deleting_faces_across_two_objects_with_the_delete_key_is_one_undo_step`. | Route the Del handler through `clay_ops.run(ctx, doc, clay_ops.get("delete"))`. Regression: `test_deleting_faces_across_two_objects_with_the_delete_key_is_one_undo_step`. |
| **clay-03 — Clay: shading** | ~~`auto_smooth` treats a **flipped-normal pair as a boundary edge** and leaves both faces smooth across a 180° disagreement (`src/warlock/studio/clay/shading.py:80`), because it only measures where `twin >= 0`. *Reproduced:* `flipped_pairs [[1,4]]`, smooth flags `[True, True]`. `adjacency.py`'s own docstring says importing a real-world GLB routinely yields exactly this.~~ **Built 2026-09-07:** `auto_smooth` marks both faces of every `flipped_pairs` entry sharp. `test_auto_smooth_marks_a_flipped_normal_seam_sharp_rather_than_smooth`. | Also mark both faces of every `flipped_pairs` entry sharp. Regression: `test_auto_smooth_marks_a_flipped_normal_seam_sharp_rather_than_smooth`. |
| **plotter-01 — Plotter: tileset editor** | ~~Per-tile Class/Probability, animation-frame duration and Wang colour Name all write per keystroke (`src/warlock/studio/panes/plotter_tileset_editor.py:156, 585, 1023`); `set_tile_meta`/`set_wang_colour` push unconditionally. The palette's own copy was fixed and carries a comment claiming the editor's copy "already does" fold — which is false. *Inspected.*~~ **Built 2026-09-07:** `fold_undo` after Class, Probability, frame duration and Wang name; the palette's false "already does" comment corrected. `test_tileset_editor_tile_class_and_duration_and_wang_name_typing_is_one_undo_step`. | `fold_undo` after each of the three. Regression: `test_tileset_editor_tile_class_and_duration_and_wang_name_typing_is_one_undo_step`. |
| **plotter-02 — Plotter: layer Properties** | ~~Name, Class, Offset and Parallax write per keystroke (`src/warlock/studio/panes/plotter_layers.py:782, 792, 846, 859`) while Opacity and Tint in the same function fold. *Inspected; orchestrator confirmed the asymmetry and the comment at line 807 explaining exactly why the fold is required.*~~ **Built 2026-09-07:** `fold_undo` after Name, Class, Offset and Parallax. `test_layer_properties_name_class_offset_parallax_typing_is_one_undo_step`. | `fold_undo` after each of the four. Regression: `test_layer_properties_name_class_offset_parallax_typing_is_one_undo_step`. |
| **plotter-03 — Plotter: map settings** | ~~Class, Background, Parallax origin, Skew and Hex side write per keystroke (`src/warlock/studio/panes/plotter_tools.py`), in the same dialog where Resize/Offset/Tile-size already stage behind an Apply button for this exact reason. *Inspected.*~~ **Built 2026-09-07:** `fold_undo` after Class, Background, Parallax origin, Skew and Hex side. `test_map_settings_popup_field_typing_is_one_undo_step`. | Fold, or move onto the draft-plus-Apply pattern beside them. Regression: `test_map_settings_popup_field_typing_is_one_undo_step`. |
| **packwright-03 — Packwright: sprite rename** | ~~Renaming a source pushes one undo step per keystroke (`src/warlock/studio/panes/packwright_sources.py:307`): `input_text("##rename", …)` with no `commit=True`, while `clay_outliner.py:231` passes `commit=True` on the identical widget. *Reproduced:* typing "lead" pushed 4 steps; one Ctrl+Z left "lea".~~ **Built 2026-09-07:** `commit=True` on the rename field. `test_renaming_a_packwright_source_is_one_undo_step_not_one_per_keystroke`. | Pass `commit=True`. Regression: `test_renaming_a_packwright_source_is_one_undo_step_not_one_per_keystroke`. |
| **packwright-02 — Packwright: sheet handoff** | ~~A Troupe "Add to Packwright" is **silently dropped** while a manual "Add a tile set…" picker is open: both submit under the bare key `packwright-tileset:{uid}` and neither call site inspects `ctx.submit`'s return (`src/warlock/studio/packwright_mode.py:144, 186`). *Reproduced:* manual submit `True`, handoff `False`, no toast.~~ **Built 2026-09-07:** the handoff keys on `job_id`/`sheet_id` rather than the bare uid. `test_a_rendered_sheet_handoff_is_not_silently_dropped_while_a_manual_tileset_pick_is_open`. | Key on the request's own identity, or toast on refusal. Regression: `test_a_rendered_sheet_handoff_is_not_silently_dropped_while_a_manual_tileset_pick_is_open`. |
| **packwright-04 — Packwright: the manual** | ~~Chapter 32 says "Four ways in" and omits the Troupe handoff that chapter 10 documents as a door (`docs/manual/32-packwright.md:29`). A reader consulting the mode's own reference chapter is told a feature does not exist. *Inspected.*~~ **Built 2026-09-07:** chapter 32 says "Five ways in" and carries a From Troupe bullet. `test_the_packwright_manual_chapter_lists_every_source_door_the_code_has`. | Add a "From Troupe" bullet matching chapter 10. Regression: `test_the_packwright_manual_chapter_lists_every_source_door_the_code_has`. |
| **inker-03 — Inker: Aseprite import** | ~~A cel carrying both precise bounds and a user note **loses the note**: `_read_chunk` clears `ud_owner` on any non-owner chunk (`src/warlock/studio/inker/asein.py:632`), so a `USER_DATA` chunk after a `CEL_EXTRA` attaches to nothing, warning "user data with no chunk before it was dropped" when a chunk was there. *Reproduced:* frame 1's note gone, frame 2's survives.~~ **Built 2026-09-07:** `_read_chunk` reassigns `ud_owner` only for owner chunk kinds. `test_a_cels_user_data_survives_a_precise_bounds_chunk_between_them`. | Only reassign `ud_owner` for owner chunk kinds. Regression: `test_a_cels_user_data_survives_a_precise_bounds_chunk_between_them`. |
| **inker-04 — Inker: ranged Shift** | ~~`translate_within` cross-fades all four channels linearly instead of the package's straight-alpha over (`src/warlock/studio/inker/mirror.py:128`), so shifting a feathered selection across frames onto non-empty pixels bakes wrong colours into the saved document. *Reproduced:* got `(128,0,127,192)` where the package's own rule gives `(170,0,85,192)`.~~ **Built 2026-09-07:** `translate_within` folds weight into alpha and composites with `composite.over`. `test_translate_within_composites_a_feathered_lift_the_way_the_rest_of_the_package_does`. | Fold weight into alpha only and composite with `composite.over`. Regression: `test_translate_within_composites_a_feathered_lift_the_way_the_rest_of_the_package_does`. |
| **inker-05 — Inker: Convolution Matrix** | ~~Any kernel whose off-diagonals cancel is misdetected as the identity and **silently does nothing** (`src/warlock/studio/inker/filters.py:833`); the fast path checks only centre weight and total sum. *Reproduced:* `m01=5, m21=-5` left the input byte-identical.~~ **Built 2026-09-07:** the identity fast path compares the raw kernel against the exact identity matrix. `test_convolve_does_not_mistake_a_balanced_off_diagonal_kernel_for_the_identity`. | Compare the raw kernel against the exact identity matrix. Regression: `test_convolve_does_not_mistake_a_balanced_off_diagonal_kernel_for_the_identity`. |
| **inker-06 — Flourish: Apply words** | ~~A malformed curve from the local text model raises `IndexError`/`KeyError` past a clamp that catches only `TypeError`/`ValueError` (`src/warlock/studio/inker/flourish/prims/__init__.py:58`), and `apply_diff` has no guard, so one bad value kills the whole request and none of the diff's valid changes apply. *Reproduced.*~~ **Built 2026-09-07:** `Param.clamp` also catches `IndexError`/`KeyError`. `test_apply_diff_drops_a_malformed_curve_value_instead_of_raising`. | Widen the except, or normalise curve key shapes before indexing. Regression: `test_apply_diff_drops_a_malformed_curve_value_instead_of_raising`. |
| **inker-07 — Flourish: engine snippets** | ~~The pygame and Godot "Copy" snippets hard-code a single-row strip (`src/warlock/studio/inker/flourish/engines.py:48`) while the per-tag export wraps past 8192px, so pasted code reads the wrong frames — or throws — on any long effect. *Inspected.*~~ **Built 2026-09-07:** `engines.describe` reads the plan's real `columns`/`rows`. `test_the_pygame_snippet_reads_a_wrapped_multi_row_sheet_correctly`. | Read the plan's real `columns`/`rows` into `engines.describe`. Regression: `test_the_pygame_snippet_reads_a_wrapped_multi_row_sheet_correctly`. |
| **inker-08 — Inker: paste from the OS** | ~~`paste_from_os` decodes the clipboard **on the frame thread with no ceiling at all** (`src/warlock/studio/inker_mode.py:1480`) — a twelfth door `pixelguard`'s "eleven doors" docstring never learned about. *Reproduced:* a 9000×9000 image accepted with **zero** `pixelguard` calls. No test catches it because `conftest.py` patches `grabclipboard` to `None` everywhere.~~ **Built 2026-09-07:** `paste_from_os` calls `pixelguard.check` before `convert`, and its refusal toast is a framed sentence rather than a forwarded exception. `test_paste_from_os_refuses_an_oversized_clipboard_image`. | Route through `ctx.submit` and `pixelguard.check`. Regression: `test_paste_from_os_refuses_an_oversized_clipboard_image`. |
| **poser-02 — Poser: imported rigged meshes** | ~~`op_remesh` and `_retexture_frame` (shared by `op_views`/`op_project`) call `_world_bounds` without `_strip_incoming_rig` (`src/warlock/pipelines/blender_worker.py:1969, 1519`) — the double Y-up/Z-up bug that function exists to fix for `op_rig`. *Reproduced against real Blender:* extent `(0.505, 0.896, 1.458)` vs a true `(1.138, 0.312, 1.507)`; calling `_strip_incoming_rig` first corrects it exactly. Mis-scales weld tolerance, decimate budget, cage extrusion and camera framing, with no `note_degraded`.~~ **Built 2026-09-07:** "import then strip" factored into `_import_measured`, used at every site feeding `_world_bounds`; `_import_glb` now has no other caller in the module. `test_op_remesh_measures_a_supplied_rigged_meshs_bounds_correctly`. | Factor "import then strip" into one helper used wherever `_import_glb` feeds `_world_bounds`. Regression: `test_op_remesh_measures_a_supplied_rigged_meshs_bounds_correctly`. |
| **poser-03 — Poser: the manual** | ~~Chapter 26 states a clip with root offsets "is refused by name rather than rendered subtly wrong" (`docs/manual/26-poser.md:84`), but that refusal was deliberately removed and `resample_clip` now interpolates root translation, pinned by `tests/test_sheet.py:637`. Stale since 2026-08-19, through a 2026-09-04 edit of the same page. *Inspected.*~~ **Built 2026-09-07:** chapter 26 describes root translation as interpolated. `test_manual_26_does_not_claim_clip_root_offsets_are_refused`. | Rewrite the paragraph to describe interpolation. Regression: `test_manual_26_does_not_claim_clip_root_offsets_are_refused`. |
| **troupe-01 — Troupe: character creation** | ~~A failure in `send_to_troupe` leaves a **permanently orphaned "done" mesh row** with no rig and no sheet (`src/warlock/service/characters.py:297`): the call sits after the row is committed with no cleanup, though the module's docstring claims "the mesh is built only once nothing left can refuse it". *Reproduced:* exception propagated, one done row left behind.~~ **Built 2026-09-07:** a failed `send_to_troupe` deletes the row and its directory before re-raising. `test_create_character_leaves_no_orphaned_row_when_send_to_troupe_fails`. | Wrap it in the same try/except used around `instantiate`, or make the mint resumable. Regression: `test_create_character_leaves_no_orphaned_row_when_send_to_troupe_fails`. |
| **troupe-02 — Troupe: character brief** | ~~An appearance slider touched under one species **survives onto a different species** named by a later prompt edit (`src/warlock/studio/panes/settings_character.py:180`); only the manual combo clears overrides. *Reproduced:* the ogre's `bulk: 1.0` landed on the elf. Contradicts the module's own invariant about not accumulating a character out of two briefs.~~ **Built 2026-09-07:** overrides drop when the resolved family changes. `test_a_species_change_via_the_prompt_drops_a_touched_appearance_slider`. | Drop the overrides when the resolved family changes, as `_family()` does. Regression: `test_a_species_change_via_the_prompt_drops_a_touched_appearance_slider`. |
| **muse-01 — Muse: retakes** | ~~The stored `retake_seed` is **never forwarded to the sampler** (`src/warlock/_q_music.py:107`) — the retake branch sends only `retake_variance` — so at the default variance roughly 70% of the blended noise is unseeded and no retake can be reproduced, making the service layer's seed-walk machinery and INVARIANTS:60 dead weight. *Inspected; orchestrator confirmed the branch returns without `retake_seeds`.* **The existing test asserts the broken output verbatim and must be amended by whoever fixes this.**~~ **Built 2026-09-07:** the retake branch emits `retake_seeds` from `params["retake_seed"]`; the test that pinned the broken output was amended. `test_a_retake_forwards_its_stored_retake_seed_to_the_sampler`. | Emit `retake_seeds` from `params["retake_seed"]`. Regression: `test_a_retake_forwards_its_stored_retake_seed_to_the_sampler`. |
| **muse-02 — Muse: loop region** | ~~Dragging a loop grip past the other marker makes the **untouched marker drift**, because `_input` re-reads the other bound live each frame instead of snapshotting it (`src/warlock/studio/panes/muse_player.py:188`), feeding each frame's sort back in as the next anchor. *Reproduced:* expected `(8.0, 12.0)`, got `(10.0, 12.0)`. Contradicts chapter 35.~~ **Built 2026-09-07:** the untouched marker is snapshotted at `is_item_activated()`. `test_dragging_a_grip_past_the_other_marker_keeps_the_untouched_one_fixed`. | Snapshot the untouched marker at `is_item_activated()`. Regression: `test_dragging_a_grip_past_the_other_marker_keeps_the_untouched_one_fixed`. |
| **sirens-01 — Sirens: WAV import** | ~~A WAV declaring a 0 Hz rate raises an uncaught `ZeroDivisionError` (`src/warlock/studio/sirens/wavout.py:189`) instead of the `ValueError` both call sites catch and reframe, so an otherwise-intact song fails to open with "Something went wrong; see the log" and no field. *Reproduced.*~~ **Built 2026-09-07:** `read_wav` refuses `source_rate <= 0` as `ValueError`. `test_read_wav_refuses_a_zero_sample_rate_instead_of_crashing`. | Refuse `source_rate <= 0` as `ValueError` before the resample branch. Regression: `test_read_wav_refuses_a_zero_sample_rate_instead_of_crashing`. |
| **sirens-02 — Sirens: the Space key** | ~~`toggle_play` reads the tag-blind `sirens_audio.playing()` (`src/warlock/studio/sirens_play.py:435`) instead of asking whether *the song* is sounding, so Space during a note preview or pattern audition just silences it. With preview on by default this is an everyday two-presses-to-play bug. *Reproduced:* `play()` never called, `stop()` called.~~ **Built 2026-09-07:** `toggle_play` stops only when `sirens_audio.tag() == tab.uid`. `test_space_starts_the_song_while_a_pattern_audition_is_still_sounding`. | Stop only when `sirens_audio.tag() == tab.uid`. Regression: `test_space_starts_the_song_while_a_pattern_audition_is_still_sounding`. |
| **sirens-03 — Sirens: Order and Instruments** | ~~"Add a pattern", "Duplicate" and "Add" call the document with no cap check and no try/except (`src/warlock/studio/panes/sirens_orders.py:53, 285`, `src/warlock/studio/panes/sirens_instruments.py:53`), so filling a song to its own documented ceiling raises out of `draw()` and `guard.py` **replaces the whole pane** after three presses. *Reproduced:* bare `ValueError` at 256 patterns / 128 instruments.~~ **Built 2026-09-07:** `pattern_room`/`instrument_room` grey the buttons with a reason naming the ceiling. `test_add_pattern_button_refuses_gracefully_at_max_patterns`. | Grey the button with a `*_reason` naming the ceiling, as `sirens_effects.py` does. Regression: `test_add_pattern_button_refuses_gracefully_at_max_patterns`. |
| **create-01 — Create: rework panels** | ~~`ctx.state.field_errors` is one **flat, unnamespaced** dict while three rework panels reuse the bare ids `strength`, `texture_size` and `custom_faces` (`src/warlock/studio/panes/texture_panel.py:74`, `src/warlock/studio/panes/remesh_panel.py:60`, `src/warlock/studio/panes/sheet_panel.py:588`), so a refusal from one door rings a sibling panel's unrelated control — and the inspector draws those panels together routinely. *Inspected.*~~ **Built 2026-09-07:** field ids namespaced per panel and each door's refusal relabelled before it reaches `field_errors`. `test_a_remesh_texture_size_refusal_does_not_ring_the_retexture_atlas_control`. | Prefix ids per panel, or key `field_errors` by `(submit key, field)`. Regression: `test_a_remesh_texture_size_refusal_does_not_ring_the_retexture_atlas_control`. |
| **create-02 — Create: Game-ready remesh** | ~~`remesh_panel.draw` is wired only into the live Create session's stage list, never into the Library's "Rig & Pose" tab (`src/warlock/studio/panes/inspector.py:344`), though chapter 23 documents the control with no such caveat. *Inspected; orchestrator confirmed `_rig_tab` draws `retarget_panel` and `texture_panel` but not `remesh_panel`.* Unreachable for every already-finished mesh.~~ **Built 2026-09-07:** `_rig_tab` draws `remesh_panel`. `test_the_library_rig_and_pose_tab_offers_game_ready_remesh`. | Add `remesh_panel.draw(ctx, job)` to `_rig_tab`. Regression: `test_the_library_rig_and_pose_tab_offers_game_ready_remesh`. |
| **create-03 — Create: Advanced model** | ~~Choosing the fast checkpoint through Advanced **loses its curated note**: `resolve_recipe` reuses a curated `Recipe` only when the request's `quality` already matches (`src/warlock/generation.py:520`), and nothing sets `quality="fast"` since the control was folded into the Model combo — contradicting `model_options`' own claim that "Nothing is lost by folding them". *Reproduced.*~~ **Built 2026-09-07:** the curated lookup filters on `generation_type` alone. `test_advanced_selection_of_the_fast_checkpoint_keeps_its_curated_note`. | Look up the curated recipe by `base_model` filtered on `generation_type` only. Regression: `test_advanced_selection_of_the_fast_checkpoint_keeps_its_curated_note`. |
| **shell-03 — Startup: corrupt job database** | ~~The promised "start with an empty index?" offer is **unreachable**: `JobStore` now opens inside `Runtime._start()` on the splash thread, so `StoreUnreadable` is swallowed by `App.run`'s catch-all (`src/warlock/studio/main.py:1057`) before `_run_locked`'s handler can offer the reset (`src/warlock/studio/main.py:5274`). Every launch shows the generic native alert instead, contradicting `_offer_store_reset`'s own docstring. *Inspected.*~~ **Built 2026-09-07:** `App.run` re-raises `StoreUnreadable` past its catch-all, `teardown()` still running from `finally`. `test_a_corrupt_job_database_reaches_run_locked_as_store_unreadable`. | Re-raise `StoreUnreadable` before the catch-all, or open the store ahead of constructing `App`. Regression: `test_a_corrupt_job_database_reaches_run_locked_as_store_unreadable`. |
| **shell-04 — Shell: column layout** | ~~`layout_skeleton.heights()` **overflows the room it was given** when two floored SHARE slots exceed what is left after the FILL reservation (`src/warlock/studio/layout_skeleton.py:106`). *Reproduced:* at `avail=200`, Inker's Tools+Tiles+Generation returns `[124, 140, 0]` summing to 264 — Save/Export starved and panes drawn past the column's bottom. The existing test only checks a comfortable 900px.~~ **Built 2026-09-07:** every SHARE floor is reserved the way the FILL floor is, shrinking proportionally when they cannot all fit; an existing test whose expectation encoded the same bug one level down was corrected. `test_two_floored_share_slots_can_push_the_columns_total_past_its_room`. | Reserve remaining SHARE floors the way the FILL floor is reserved; shrink proportionally if they cannot fit. Regression: `test_two_floored_share_slots_can_push_the_columns_total_past_its_room`. |
| **shell-05 — Shell: command palette** | ~~A "Go to <mode>" command for a gated mode is drawn **enabled with an empty reason** and does nothing on Enter (`src/warlock/studio/palette.py:323`) — no toast, no explanation — on exactly the fresh-install case `model_gate` exists for, while the rail's matching item greys out and routes to Settings. *Reproduced:* `enabled=True`, `why=''`, mode unchanged.~~ **Built 2026-09-07:** `_mode_commands` consults `model_gate.mode_gate`. `test_go_to_a_gated_mode_is_greyed_in_the_palette_rather_than_silently_refusing`. | Consult `model_gate.mode_gate` in `_mode_commands`, and route a refusal through a toast. Regression: `test_go_to_a_gated_mode_is_greyed_in_the_palette_rather_than_silently_refusing`. |
| **shell-06 — Shell: the menu bar** | ~~`reroll` has no `_COMMAND_PATHS` entry (`src/warlock/studio/menus.py:34`) so it is **absent from Home and Library** — where selecting-then-rerolling happens — and appears in nine unrelated workspace modes instead. *Reproduced.* Chapter 38 states as a guarantee that palette and menu bar carry the same commands.~~ **Built 2026-09-07:** `"reroll": ("Edit",)` added to `_COMMAND_PATHS`. `test_reroll_is_reachable_from_the_menu_bar_in_the_library`. | Add `"reroll": ("Edit",)` beside `"delete"`. Regression: `test_reroll_is_reachable_from_the_menu_bar_in_the_library`. |
| **pipelines-01 — Pipelines: stem separation** | ~~Every separation failure surfaces as **"exited with code 1" plus a progress line**, never the real cause: the worker writes its error into the result file and exits 1, but `rigging.run_worker` deletes that file and raises on any nonzero exit before reading it (`src/warlock/pipelines/separation_worker.py:169`, `src/warlock/rigging.py:861`), making `_q_music.py`'s error handler unreachable dead code. *Reproduced.* `service/downloads.py`'s own runner does this correctly.~~ **Built 2026-09-07:** `separation_worker` exits 0 once the failure is on disk, keeping 2 and 3 for the cases where nothing was written. `test_a_failed_separation_surfaces_the_workers_own_error_message_not_just_an_exit_code`. | Exit 0 on a handled failure, or read `result_path` before raising. Regression: `test_a_failed_separation_surfaces_the_workers_own_error_message_not_just_an_exit_code`. |
| **tour-01 — Tour: key ownership** *(merged: tour + shell)* | ~~The tour card reads Enter/Left/Right **unconditionally every frame with no focus check** (`src/warlock/studio/panes/tour.py:472`), so an Enter confirming a rename or an arrow moving a caret also advances — or completes — the tour; and the same keystroke drives the mode underneath, moving the Library grid selection (`src/warlock/studio/panes/library.py:1138`, `src/warlock/studio/main.py:2926`). Both directions violate INVARIANTS:490, "it never acts for the reader". *Inspected, seen from two sides by two segments.*~~ **Built 2026-09-07:** the card's key reads are gated on `is_window_focused()` and `_shortcut` refuses the same keys while the card owns them. `test_pressing_enter_in_an_unrelated_text_field_does_not_advance_the_tour`. | Gate the card's key reads on `is_window_focused()`, as `create_brief.py` does via `focus.item`. Regression: `test_pressing_enter_in_an_unrelated_text_field_does_not_advance_the_tour`. |
| **docs-01 — Docs: the seam metric** | ~~Inker's live seam indicator still uses `tiling.seam_ratio`/`SEAM_MAX` (`src/warlock/studio/inker/tiling.py:217`), the metric `2026-08-30-seam-dominance.md` **measured as false-alarming on exactly Inker's content** (flat-cell pixel art; up to 20 of 24 seamless tiles misflagged) and replaced in `pipelines/seam.py` with `SEAM_DOMINANCE_MAX`. *Inspected; orchestrator confirmed `seam.py:196` decides on dominance while `tiling.py` has only the ratio.* The fix reached one copy of the measurement and not the other; the guard only asserts the two retired constants are equal.~~ **Built 2026-09-07:** `seam_dominance`/`SEAM_DOMINANCE_MAX` ported into `tiling.py` with a parity test against `pipelines/seam.py`, and `inker_canvas.seam_text` switched to them. `test_the_canvas_seam_indicator_decides_on_dominance_not_the_retired_ratio`. | Port `_dominance`/`SEAM_DOMINANCE_MAX` into `tiling.py` and switch the indicator. Regression: `test_inker_seam_indicator_does_not_false_alarm_on_flat_cell_pixel_art`. |
| **docs-02 — Docs: CLAUDE.md** | ~~The `rebuild.ps1` comment claims the plain invocation rebuilds the native DLL (`CLAUDE.md:23`), but `-Native` is opt-in: the default run only checks the DLL is present and **silently keeps a stale one**. *Inspected; orchestrator confirmed `[switch]$Native`, `if (-not $Native)`, and the script's own comment "default is to leave the DLL alone".* A contributor editing `native/*.c` tests against the old kernel, silently breaking bit-identical parity.~~ **Built 2026-09-07:** the comment matches `.PARAMETER Native`. `test_claude_md_rebuild_comment_does_not_claim_the_native_dll_rebuilds_by_default`. | Correct the comment to match `.PARAMETER Native`. Regression: `test_claude_md_rebuild_comment_does_not_claim_the_native_dll_rebuilds_by_default`. |
| **docs-03 — Docs: measured joints** | ~~Chapter 08 sends a user hitting the A-pose trap to a **"Measured joints" control that does not exist** (`docs/manual/08-rigging-and-posing.md:45`). *Inspected; orchestrator confirmed `create_rig`'s signature has no `joints` parameter (`src/warlock/service/rig.py:56`), `stage_rig.py` draws no such control, and every `joints="measured"` site is inside the Troupe character pipeline — two of which explicitly **withhold** it.* The documented first-line fix is a dead end; the only real remedy is hand-correction via "Adjust joints".~~ **Built 2026-09-07:** chapter 08 rewritten: the measured fit is automatic and Troupe-only, and Adjust joints is the remedy. `test_rig_stage_offers_no_measured_joints_control`. | Wire the control into the Rig stage, or rewrite the chapter to say the measured fit is automatic and Troupe-only. Regression: `test_rig_stage_offers_no_measured_joints_control`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-04 — Clay: `.wblk` open** | ~~`read_wblk` bounds only decompressed bytes, with no equivalent of `glbimport`'s `MAX_OBJECTS`/`MAX_TRIANGLES` (`src/warlock/studio/clay/serialize.py:522`). *Reproduced:* 20,000 objects (4.9× the ceiling, 4% of the byte budget) opened in 8.9 s with no warning — on a path crash recovery takes without being able to ask.~~ **Built 2026-09-07:** object and triangle counts checked against `glbimport`'s ceilings. | Check object and triangle counts against the same constants, or route through the slow-document confirm. |
| **clay-05 — Clay: glTF loader** | ~~`gltf.load` bounds node count but iterates `materials`/`meshes` unbounded, and a `count: 0` accessor is charged 0 bytes (`src/warlock/studio/viewer/gltf.py:335`), so unreferenced declarations trip no ceiling. *Reproduced:* 500,000 entries, 3.7 s, `_spent` never moved.~~ **Built 2026-09-07:** `MAX_MATERIALS`/`MAX_MESHES` beside `MAX_NODES`. | Add a declared-count ceiling beside `MAX_NODES`. |
| **clay-06 — Clay: booleans** | ~~`boolean()` transforms every selected mesh **before** `_refuse_complexity` (`src/warlock/studio/clay/ops_boolean.py:138`), so a refused op still pays the full allocation.~~ **Built 2026-09-07:** the refusal is computed from untransformed inputs. | Compute the refusal from untransformed inputs first. |
| **clay-07 — Clay: duplicate** | ~~Ctrl+J and the outliner's Duplicate bypass `clay_ops.run` (`src/warlock/studio/clay_mode.py:1040`), so the same edit lands in history under the generic "object add" label and never sets `last_op`. *Reproduced.*~~ **Built 2026-09-07:** Ctrl+J and the outliner's Duplicate route through `clay_ops.run`. | Route both through `clay_ops.run`. |
| **clay-08 — Clay: X-ray tooltip** | ~~The tooltip advertises "(Alt+Z)" but nothing binds it, and `handle_key` consumes the press (`src/warlock/studio/panes/clay_header.py:415`). *Reproduced.*~~ **Built 2026-09-07:** Alt+Z wired, added to `shortcuts.py` and to manual chapter 38. | Wire Alt+Z and add it to `shortcuts.py`, or drop the claim. |
| **clay-09 — Clay: viewport cache** | ~~`ClayView.draw`'s frame-skip key uses a bare `id(doc)` with nothing pinning the document (`src/warlock/studio/clay_view.py:263`); with `rev` starting at 0 for every new document, a recycled address can return a closed document's texture. Every other identity-keyed cache in the class pins its object.~~ **Built 2026-09-07:** `ClayView` pins the drawn document beside its `id`. | Pin the document, or invalidate in `ClayView.clear()`. |
| **create-04 — Create: retarget panel** | ~~Field ids `remesh_profile`/`custom_faces` name a **different door**; `optimize_job` raises `field="profile"` (`src/warlock/studio/panes/retarget_panel.py:82`), so a genuine refusal rings neither control — the defect the panel's own comment claims to have fixed.~~ **Built 2026-09-07:** the ids are `profile` and `custom_triangles`. | Use `"profile"` and `"custom_triangles"`. |
| **create-05 — Create: the band sweep** | ~~A tie with `auto` is reported "clearly better" whenever the tied band sorts first (`src/warlock/sweep.py:205`); `best is baseline` is the only tie check. *Reproduced.* A human tuning hardware may hard-code a band with no benefit.~~ **Built 2026-09-07:** `margin <= 0` is no better. | Treat `margin <= 0` as no better. |
| **create-06 — Create: legacy aliases** | ~~`asset_type_from_params` normalises an alias under `asset_type` but returns it raw under `generation_type` (`src/warlock/studio/create_assets.py:166`), reintroducing the retired `model_3d` spelling. *Reproduced;* currently masked by an immediate `sync_legacy_fields`.~~ **Built 2026-09-07:** the `generation_type` branch normalises like `asset_type`. | Mirror the `asset_type` branch. |
| **create-07 — Create: dead viewer door** | ~~`Viewer.compare()` parses and uploads a GLB synchronously and has **no callers** (`src/warlock/studio/viewer_embed.py:303`); wiring a button to it reintroduces the frame-freeze the `parse_model`/`adopt_compare` split fixed.~~ **Built 2026-09-07:** `Viewer.compare()` deleted; no callers. | Delete it, or add a real caller and a test. |
| **inker-09 — Inker: Flourish digests** | ~~`apply_flourish` records a digest for slots where it wrote nothing (`src/warlock/studio/inker/_doc_flourish.py:308`), so a later hand-drawn edit on a blank effect frame is judged against a digest that never matched real pixels. *Reproduced:* 18 phantom entries from one ordinary call.~~ **Built 2026-09-07:** the digest is written only where a cel was. | Gate the digest write on the same condition as the cel creation. |
| **inker-10 — Flourish: bake cost** | ~~Every recipe field is clamped individually but nothing bounds their product before `bake()` (`src/warlock/studio/inker/flourish/recipe.py:37`), and `TaskRunner` has no cancel API — so a hand-edited preset can freeze Regenerate indefinitely or raise `MemoryError`.~~ **Built 2026-09-07:** `bake_cost`/`check_bake_cost` added and called ahead of both `ctx.submit` sites, refusing with a toast. | Add a combined-cost ceiling refused before submit. |
| **muse-03 — Muse: failed searches** | ~~`muse-` is **missing from `main.py`'s task-failure prefix chain** that every other mode appears in (`src/warlock/studio/main.py:1510`), and no `on_task_failed` exists, so a failed loop search leaves `finding` set and the row stuck on a spinner permanently.~~ **Built 2026-09-07:** `muse_mode.on_task_failed` added and `muse-` routed to it. | Add `on_task_failed` and route `muse-` to it. |
| **muse-04 — Muse: recipe column** | ~~The pane drawing six refusable controls never reads `field_errors` (`src/warlock/studio/panes/muse_recipe.py`), unlike the bar and the derive popup; `test_field_error_wiring.py`'s table includes no Muse pane.~~ **Built 2026-09-07:** the recipe column rings and clears its six fields, and the pane joined `test_field_error_wiring.py`'s table. | Add the per-field ring. |
| **muse-05 — Muse: repaint window** | ~~An out-of-range window always names `field="repaint_start"` even when `repaint_end` is the wrong one (`src/warlock/service/_jobs_music.py:513`), so the ring lands on the control that was never wrong; the `extend` task four lines up gets this right. **The parametrized test pins the wrong field and must be updated with the fix.**~~ **Built 2026-09-07:** the refusal names `repaint_end` when the end is what is out of range; the parametrized row that pinned the wrong field was corrected. | Mirror the `extend` pattern. |
| **packwright-05 — Packwright: tileset popup** | ~~`tileset_occupancy`/`dedup_tiles` recompute **synchronously on the frame thread** per typed digit (`src/warlock/studio/panes/packwright_sources.py:159`); the module's own comment measures it at "some hundreds of milliseconds" on a full sheet.~~ **Built 2026-09-07:** the counts moved behind `ctx.submit`. | Move behind `ctx.submit`. |
| **packwright-06 — Packwright: import pin prose** | ~~The package docstring, INVARIANTS:203 and the pin test's own docstring say seven / six / four outward imports; the enforced set has **eight** across six modules, because `wpack.py`'s `from .. import zipguard` is in no prose.~~ **Built 2026-09-07:** the docstring, the pin test and INVARIANTS all say eight, and the pin test asserts the count as well as the set. | Correct all three to match the set. |
| **plotter-04 — Plotter: dead plan citation** | ~~`_map_model.py:420` cites the deleted `PLOTTER_PLAN.md`; both guards miss it because `RETIRED_PLANS` does not list it.~~ **Built 2026-09-07:** the citation points at where the reasoning lives, and `RETIRED_PLANS` lists the file. | Cite where the reasoning lives now and extend `RETIRED_PLANS`. |
| **poser-04 — Poser: measured joints** | ~~`validate_joints` sits outside `op_rig`'s try (`src/warlock/pipelines/blender_worker.py:1076`) and `main()` wraps nothing, so `joints="measured"` against a non-humanoid template crashes the worker instead of falling back — against the comment above it, "Costs the measurement, never the rig". Not reachable through today's UI.~~ **Built 2026-09-07:** `validate_joints` moved inside `op_rig`'s try, so it falls back to the bbox fit. | Move it inside the try, or refuse the combination at the door. |
| **poser-05 — Poser: clip root offsets** | ~~`root_translation` is converted with a bare `float(v)` in both clip doors (`src/warlock/service/clips.py:227`, `src/warlock/rigging.py:288`) with no finite or magnitude check, unlike the identical field on a library pose. *Reproduced:* `[nan, 1e30, 0.0]` round-trips. `parse_clip_library`'s docstring names this exact incident for the neighbouring field.~~ **Built 2026-09-07:** both clip doors share `poselib.validate_root_translation`. | Share `poselib.validate_record`'s check. |
| **poser-06 — Poser: clip refusals** | ~~A malformed `root_translation` raises a bare `ValueError` out of `_check_shape` instead of a field-addressed `Invalid` (`src/warlock/service/clips.py:227`), so it surfaces as the generic "Something went wrong". *Reproduced.*~~ **Built 2026-09-07:** the refusal is `Invalid(field="poses")`. | Wrap and raise `Invalid(field="poses")`. |
| **service-02 — Service: character preview** | ~~`shutil.move` onto an existing destination degrades to non-atomic `copy2` on Windows (`src/warlock/service/characters.py:378`), so a concurrent build can tear a GLB a viewer is reading — the hazard the surrounding comment claims is handled.~~ **Built 2026-09-07:** `os.replace` from the scratch dir, same volume as the destination. | Use `os.replace` from the scratch dir. |
| **service-03 — Service: reference edits** | ~~`save_edited_image`'s check-then-copy-then-write takes no lock (`src/warlock/service/files.py:548`), so two concurrent saves can make `input.orig.png` — undo's only anchor to the generated pixels — a copy of an already-edited image, unrecoverably.~~ **Built 2026-09-07:** the check-then-copy-then-write is behind `convert_lock`. | Take a per-job lock, as the sibling doors do. |
| **service-04 — Service: pack install timeout** | ~~The 4-hour timeout force-kills the child regardless of phase (`src/warlock/service/packs.py:498`), while the UI deliberately withdraws Cancel once pip starts because killing mid-install leaves the running app's `site-packages` half-written (INVARIANTS:27). The invariant is honoured on the click path and ignored on the timeout path.~~ **Built 2026-09-07:** the phase is tracked and a `PHASE_COMMIT` timeout no longer force-kills. | Track the phase and refuse to force-kill during `PHASE_COMMIT`. |
| **service-05 — Service: update timeout** | ~~The shared runner's timeout message is hardcoded to "The update check timed out." even when a two-hour installer **download** stalls (`src/warlock/service/updates.py:301`).~~ **Built 2026-09-07:** the operation label is passed through. | Pass the operation label through. |
| **sirens-04 — Sirens: Delete reasons** | ~~The disabled-reason ternary tests `editable` instead of the selection state (`src/warlock/studio/panes/sirens_effects.py:71`, `src/warlock/studio/panes/sirens_instruments.py:61`) — the test is inverted, so each state shows the other's sentence.~~ **Built 2026-09-07:** the ternary tests the selection state. | Swap the test to match `sirens_orders.py`. |
| **sirens-05 — Sirens: sample Delete** | ~~The reason is an **empty string** while the song is saving (`src/warlock/studio/panes/sirens_instruments.py:154`) — the exact failure `disabled_button`'s docstring exists to prevent, at the moment it most needs explaining.~~ **Built 2026-09-07:** the busy reason is named. | Name the busy reason. |
| **sirens-06 — Sirens: envelope drag** | ~~`_input`'s `if tab.busy: return` fires before the deactivation check (`src/warlock/studio/panes/sirens_envelopes.py:349`), so a save started mid-drag skips the frame that would fold, leaving every painted column its own undo step.~~ **Built 2026-09-07:** the gesture closes before the early return. | Close the gesture before the early return. |
| **troupe-03 — Troupe: QA seams** | ~~A run whose `movement` names no entry in `movements` is silently treated as cyclic (`src/warlock/studio/troupe/qa.py:274`), and `preview_layout` never checks the two agree — so a one-shot gets a fabricated seam flag against INVARIANTS:460.~~ **Built 2026-09-07:** an unknown movement is no longer defaulted to cyclic. | Validate the cross-reference, or skip the metric for an unknown movement. |
| **troupe-04 — Troupe: re-render rows** | ~~`rerender_charsheet` hand-strips three derived params and omits `validation` (`src/warlock/service/troupe.py:446`), so a queued re-render carries a stale verdict about frames it has not rendered — the hazard `DERIVED_PARAMS`' own comment describes. *Reproduced.*~~ **Built 2026-09-07:** `validation` is stripped with the other derived params. | Add it, or drive the strip from `DERIVED_PARAMS`. |
| **pipelines-02 — Pipelines: wrapping** | ~~Nothing refuses `tile=True` combined with `sheet`/`tilesheet` (`src/warlock/pipelines/text2image.py:975`), so circular padding could wrap a contact sheet edge-to-edge — the module's comments state the invariant, only caller discipline enforces it.~~ **Built 2026-09-07:** `_generate` refuses `tile` with `sheet`/`tilesheet`. | Refuse the combination in `_generate`. |
| **pipelines-03 — Pipelines: pack manifest** | ~~`parse_manifest` never checks the wheel-filename shape `wheel_dist()` requires (`src/warlock/packs.py:328`), so a hand-edited `packs.json` turns opening Settings → Packs into an unhandled `ValueError` instead of the clean `ManifestError` its docstring promises. *Reproduced.*~~ **Built 2026-09-07:** `parse_manifest` validates the wheel-filename shape and raises `ManifestError`. | Validate the shape at load time. |
| **shell-07 — Shell: recents** | ~~`remember`/`forget` dedupe by raw string equality (`src/warlock/studio/recents.py:148`) with no `normcase`/`resolve`, unlike `docmodes.find_path` which exists because "Level.WMAP" and "level.wmap" are one file on Windows — so one file can occupy two of ten slots.~~ **Built 2026-09-07:** `remember`/`forget` share a `normcase`/`resolve` key. | Normalise with the same key. |
| **shell-08 — Shell: overlay toolbar** | ~~Line width is reserved for the Tiled toggle under `offers_inker` but the toggle draws only under the stricter `shows_tiled` (`src/warlock/studio/panes/overlay.py:80`), so the Frame button inherits a reservation sized for a label that is never drawn and the row wraps when it would have fit.~~ **Built 2026-09-07:** the Frame button reserves its own width. | Reserve by the button's own width. |
| **shell-09 — Review: the Good button** | ~~"Good (A)" is drawn with no `reason` while "Bad (R)" and "Skip (S)" beside it pass one for the identical gate (`src/warlock/studio/review_panes.py:210`), against the surrounding comment's stated contract.~~ **Built 2026-09-07:** `reason=no_row` on the Good button. | Pass `reason=no_row`. |
| **docs-04 — Docs: measurement count** | ~~INVARIANTS:585 says `docs/measurements/` holds "a dozen now"; it holds **54**. *Reproduced.* The sentence's own hedge shows the author expected it to go stale without protecting it.~~ **Built 2026-09-07:** the count is gone. | Delete the count per the sentence's own rule. |
| **docs-05 — Docs: dead commit citations** | ~~**Six** commit hashes in INVARIANTS.md do not resolve — `bf8889c`, `28e35d9`, `de87838`, `09c64b4` (line 585) and `2a56df6` (225), `cc7ee724` (290). *Reproduced by the orchestrator with `git rev-parse`; control hashes `1fc1573` and `3476f114` do resolve, so the repository is not shallow.* The same paragraph promises a reader can chase these.~~ **Built 2026-09-07:** the six dead hashes re-derived; two of them resolved to one commit. | Re-derive with `git log --diff-filter=D`, or drop the hashes. |
| **docs-06 — Docs: Muse Steps guidance** | The manual says "below about 30 the output audibly falls apart; above about 80 you are paying for time" (`docs/manual/16-generating-a-soundtrack.md:59`, `docs/manual/35-muse.md:57`) while the slider's own tooltip says "past about 60, not better" (`src/warlock/studio/panes/muse_recipe.py:68`) — a different threshold and no below-30 warning. Neither figure has a measurement document. *Inspected.* | **Open — moved to `TODO.md` P35 on 2026-09-07.** Deliberately not closed by the fix pass: the manual says one thing, the tooltip another, and *neither* figure has a measurement behind it, so picking a winner would have written a number into the manual on no more authority than the one already there. This repository's rule is that a figure the corpus is keyed on gets a dated document before it changes. |
| **docs-07 — Docs: chapter 02 asset types** *(merged: create + docs)* | ~~Chapter 02 says *3D Model* plus "the other four entries" and names four (`docs/manual/02-your-first-asset.md:43`), but `create_assets._ORDERED` offers **five** others — *Character* is omitted, and it is the only asset type needing no GPU, which is exactly what a reader without a capable card most needs to know. *Inspected, found independently by two segments.*~~ **Built 2026-09-07:** chapter 02 says five and names Character. | Say "the other five entries" and name Character, or point at chapter 11. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-10** | ~~`state.last_op` is written by every op and read by **no pane**; the "adjust last operation" card and Repeat it feeds do not exist in the shipped UI and are not in `TODO.md`.~~ **Built 2026-09-07:** the unread bookkeeping removed, with its five tests; the control itself is now TODO.md's P36. | Build the control, or remove the bookkeeping and file it. |
| **create-08** | ~~On a candidate card, "Make 3D" is left alone on a half-width row (`src/warlock/studio/generation_workspace.py:306`), against the function's own "two per row" design.~~ **Built 2026-09-07:** "Make 3D" pairs with Rerun. | Pair it with a neighbour. |
| **create-09** | ~~An accessor with an unrecognised `componentType`/`type` raises a bare `KeyError` (`src/warlock/studio/viewer/gltf.py:428`) instead of the named `ValueError` every sibling boundary raises.~~ **Built 2026-09-07:** membership checked before indexing. | Check membership before indexing. |
| **create-10** | ~~`studio/viewer/__init__.py` claims package-wide freedom from imgui/pygame but the pin covers **2 of 17** modules. No live violation today.~~ **Built 2026-09-07:** the pin globs all 17 modules. | Glob the whole package, as the sibling pins do. |
| **inker-11** | ~~`sheet.pack`/`pixelize.reduce_frames` gate on `exists()` where `is_file()` is meant, defeating the named "no rendered frame for cell N" refusal.~~ **Built 2026-09-07:** `is_file()` at both gates. | Use `is_file()`. |
| **inker-12** | ~~INVARIANTS:207 cites `_Parse.tileset_ud_run`, which does not exist; the mechanism is the generic `ud_owner` tuple.~~ **Built 2026-09-07:** INVARIANTS names `_Parse.ud_owner`. | Name `_Parse.ud_owner`. |
| **inker-13** | ~~`sprite.render` computes a full-frame noise field per frame to read one scalar (`src/warlock/studio/inker/flourish/prims/sprite.py:50`), against the package's own cost bar.~~ **Built 2026-09-07:** one lattice point rather than a full-frame plane. | Sample a single lattice point. |
| **muse-06** | ~~A `#:` doc-comment block describing `compose_strength` is attached to `loop_memory` (`src/warlock/studio/muse_state.py:174`).~~ **Built 2026-09-07:** the doc-comment is split at the paragraph boundary. | Split at the paragraph boundary. |
| **packwright-07** | ~~The preview and items panes rebuild a full source index every frame though `pack_generation` exists as the memo key.~~ **Built 2026-09-07:** `source_index` caches on the tab, keyed on `pack_generation`. | Cache on the tab. |
| **pipelines-04** | ~~`lines_from` reads `PeekNamedPipe`'s byte count unbounded (`src/warlock/pipelines/_workerio.py:99`) — the one uncapped read in the module whose purpose is controlling how stdin is read.~~ **Built 2026-09-07:** the read is capped at `MAX_STDIN_CHUNK`. | Cap the read. |
| **pipelines-05** | ~~`measure()` restates the has-alpha condition instead of calling the module's own `has_alpha()` (`src/warlock/pipelines/reference.py:324`), which `subject_mask()` already uses.~~ **Built 2026-09-07:** `measure()` calls `has_alpha()`. | Call `has_alpha`. |
| **pipelines-06** | ~~`+` binds tighter than `or` in `report_line`'s tiercheck fallback (`src/warlock/pipelines/remesh.py:126`), so "; lost something" can never print. *Reproduced.*~~ **Built 2026-09-07:** parenthesised. | Parenthesise. |
| **pipelines-07** | ~~`roll_half` is documented and relied on as its own inverse but is off by one for odd dimensions (`src/warlock/pipelines/seam.py:208`). *Reproduced;* latent — every caller uses 1024.~~ **Built 2026-09-07:** `roll_half` refuses odd dimensions. | Assert even dimensions, or make it self-inverse. |
| **pipelines-08** | ~~The uninstaller names `%USERPROFILE%\.warlock` and ignores `WARLOCK_HOME` (`installer/warlock.iss:79`), telling a relocated user their data survived where it never lived.~~ **Built 2026-09-07:** the uninstaller reads `WARLOCK_HOME` first. | Read `WARLOCK_HOME` first. |
| **poser-07** | ~~"Update key from pose"'s disabled reason tests `state.frame` when `posing` is the real cause, so it briefly names a stale in-between frame.~~ **Built 2026-09-07:** `_update_key_reason` checks `posing` first. | Check `posing` first. |
| **service-06** *(merged: service + shell)* | ~~`PRODUCTS`' comment says "Six entries" for a dict of **seven** (`src/warlock/followups.py:32`); `remesh` was added without updating it. Found independently by two segments.~~ **Built 2026-09-07:** seven. | Say seven, or drop the number. |
| **service-07** | ~~`db.py`'s `jobs.kind` schema comment lists ten kinds; the service writes **thirteen** — `charsheet`, `music` and `separate` are absent.~~ **Built 2026-09-07:** all thirteen, marked illustrative. | List all thirteen, or mark it illustrative. |
| **service-08** | ~~`dispatch_shortfall_message`'s comment calls `headroom_gib` a submission-time budget; its only caller passes a dispatch-time free-plus-credited figure (`src/warlock/vram.py:963`).~~ **Built 2026-09-07:** the comment matches the caller. | Reword to match the caller. |
| **service-09** | ~~`JobStore.transaction()` guards only `create()`'s commit (`src/warlock/db.py:579`); any other write inside a savepoint would collapse it early. Not triggered by today's three callers.~~ **Built 2026-09-07:** every write method commits through `_commit`. | Guard every commit-issuing method, or document the restriction. |
| **shell-10** | ~~`Form.footer()` draws a raw `imgui.separator()` instead of the shared `divider()` door (`src/warlock/studio/forms.py:440`); the AST guard scans only `panes/*.py`.~~ **Built 2026-09-07:** `widgets.divider()`. | Use `widgets.divider()`. |
| **shell-11** | ~~`_inker_export_specs` rebuilds the ~30-command palette list a second time per call (`src/warlock/studio/menus.py:161`), up to four times a frame.~~ **Built 2026-09-07:** the command list is built once. | Build once and pass it in. |
| **sirens-07** | ~~`_caret_span` — which picks the characters the caret rings for a two-digit column — has no direct test, unlike its pulled-out neighbours. *Evidence gap.*~~ **Built 2026-09-07:** cases added beside `first_channel`/`column_at` -- coverage only; there was no defect to reproduce. | Add cases beside `first_channel`/`column_at`. |
| **tour-03** | ~~`scripts.py`'s docstring says "Four of them" and explains four; `TOURS` holds **five** — `muse-basics` is unexplained, and it is the one tour needing weights.~~ **Built 2026-09-07:** five, with muse-basics explained. | Say five and add its paragraph. |
| **tour-04** | ~~`modes.py:147`'s comment says "these four… these seven"; `RAIL_GROUPS` holds three and eight.~~ **Built 2026-09-07:** three and eight. | Correct the counts. |
| **troupe-05** | ~~Two panes hardcode `512`/`256` instead of reading `charsheet.MAX_CELLS`/`WARN_CELLS`, which one of them already imports `charsheet` for.~~ **Built 2026-09-07:** `charsheet.MAX_CELLS`/`WARN_CELLS` read from the constants. | Read the constants. |
| **docs-08** | ~~`2026-08-17-ground-reduction.md` cites `ground.py:reduce_texture`, `GROUND_VERSION` and `GROUND_NEGATIVE_PROMPT`; all three were renamed into `tilesheet.py`. Its own front-matter half-corrects this.~~ **Built 2026-09-07:** the current `tilesheet.py` symbols named. | Name the current symbols. |
| **docs-09** | ~~`TODO.md`'s P28 cites `family.ARCHETYPES[key].channels`; only `get_archetype()`/`archetypes()` are public.~~ **Built 2026-09-07:** `family.get_archetype(key).channels`. | Cite the real accessor. |
| **docs-10** | ~~Chapters 04 and 37 call the labelling section "Teaching the judge"; the pane draws `widgets.section("Teach the judge")` (`src/warlock/studio/review_panes.py:329`).~~ **Built 2026-09-07:** both chapters say "Teach the judge". | Match one to the other. |
| **docs-11** | ~~Chapter 03 says the overflow entry reads "Add to a Packwright atlas"; `verbs.add_to` builds "Add to Packwright as an atlas source", which chapter 10 quotes correctly.~~ **Built 2026-09-07:** chapters 03 and 32 quote the real label. | Fix chapter 03's wording. |

## Human-only — owed, not open

Findings that need a card, a clean machine, real Aseprite or Tiled, or ears. These move to
`TODO.md` in the fix phase and are not counted as open here.

**None outstanding.** The one human-only finding was settled during this audit and is struck below;
nothing is owed to `TODO.md` from this pass.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| **tour-02** | ~~INVARIANTS:490 says a step's "Read more" raises the Manual **over** the tour card; the registration comment at `main.py:4409` says the ordering exists so the card stays **on top**. The two contradict each other, and neither overlay sets `NoFocusOnAppearing`, so imgui's focus-on-appearing default decides.~~ **Settled 2026-09-07** by direct observation in a live window: the overlay renders correctly and the tour behaves as intended, so the z-order is not a defect. The registration comment describes what happens; if INVARIANTS:490 still reads the other way it is the stale sentence, correctable as a one-line docs edit rather than an owed investigation. | Settled — no hardware owed. |

## Coverage

- **Slices and segments run:** all 14 slices, all 53 segments, one explorer each — `shell`: boot,
  documents, chrome, widgets, home-library, review-settings; `create`: brief, workspace, viewer,
  panes; `inker`: document, paint, sheets, codecs, flourish, mode, panes; `clay`: mesh, document,
  mode; `poser`: rig, poses, mode; `troupe`: engine, jobs, render; `plotter`: map, tiles, mode;
  `packwright`: packer, mode; `muse`: engine, mode, jobs; `sirens`: engine, playback, panes;
  `service`: queue, kinds, assets, gates; `pipelines`: children, image, mesh, install; `docs`: A-B,
  C1, C2, D, E, F1, F2; `tour`: 1.
  **Segments deliberately not launched:** `pipelines`' fifth segment, `workspace`, by that row's own
  instruction — under `all` its files are read by the workspace rows that own them
  (`pixel.py`/`pixelize.py`/`pixelsheet.py`/`sheet.py` by `inker-sheets`; `charsheet.py`/
  `spritesynth.py` by `troupe-render`; `jointfit.py` by `poser-rig` and `pose2d.py` by
  `poser-poses`; `tileatlas.py`/`tilemask.py`/`tilesheet.py` by `plotter-tiles`; `audioout.py`/
  `music_client.py` by `muse-jobs`). Every one of those files was read by its owning segment.
- **Explorers that failed or timed out:** none. All 53 returned records and a coverage note.
- **Files a segment owned but did not open, and why** — the honest residue of this pass, largest
  first:
  - `plotter-map` opened 14 of 19 files and **never opened `edits.py`, `_map_layers.py` or
    `tsx.py`** (99 KB between them). `edits.py` is where the undo-gesture class would have to be
    verified line by line — and the three Plotter Highs found in the sibling `mode` segment are all
    of that class, so this gap sits directly beside a confirmed defect pattern. It did run
    `tests/plotter/` (1197 passed).
  - `blender_worker.py` (2170 lines) was left half-unread by **both** segments that hold it:
    `poser-rig` did not read ~407–960 or ~1545–1935, and `pipelines-children` did not read
    `op_sheet`, `op_fbx`, `op_views` or `op_project`. `poser-02`'s confirmed defect is in
    `_retexture_frame`, which `op_views`/`op_project` share — so the unread region is the one most
    implicated by a finding.
  - `inker-document` skimmed `selection.py` (1056 lines) and `_doc_tiles.py` (1267) structurally
    only, naming them "the most likely place a further defect is still sitting".
  - `inker-paint` left ~two-thirds of `brush.py` and half of `animation.py` unread;
    `inker-mode` sampled `inker_state.py`'s 1300-line class body and did not read
    `inker_ops.py`'s ~200 individual op registrations one by one.
  - `shell-boot` read ~2000 of `main.py`'s 5330 lines; `shell-widgets` left `widgets.py`'s middle
    third and most of `tokens.py` unread.
  - `plotter-mode` did not read the bulk of `plotter_canvas.py` (3600 lines) — "skipped for time,
    not judged clean".
  - `service-kinds` read six of its files only partially (`_q_tileset`, `_q_troupe`, `_q_mesh`,
    `_q_music`, `_jobs_resubmit`, `_jobs_rework`) and explicitly declines to claim full
    sweep-table coverage there.
  - `clay-document` never opened `presets.py` and read a third of `primitives.py`.
  - `docs-E` did not read **33 of the 35 modules in `service/`** for docstring staleness (section
    G), nor ~40 of `queue.py`'s docstrings. `docs-A-B` did not read `CHANGELOG.md` past line 815 or
    `manual/parser.py`. `docs-D` read ~35 of the 54 measurement documents by constant-sweep only,
    which cannot catch a defect not keyed to a named constant. `docs-F2` verified chapters 28, 29
    and 31 as prose without tracing their narrower control claims to handler code.
  - `docs-F1` read 27 of its 28 chapters, leaving `38-shortcuts.md` unread because the check-up
    marks that row gated by `tests/manual/test_shortcuts.py`; it did not attempt to show the gate
    inadequate. Chapters 13, 39, 40, 42–45 were read but only spot-checked, having few
    pane-control claims of the kind section F asks about.
  - `pipelines-install` did not read the nine `native/*.c` bodies (signatures verified against the
    header and the ctypes bindings; the bit-parity suite is the stronger guard) or ~21 developer
    scripts.
  - `muse-jobs` grep-audited but did not read the ~25 vendored `acestep/` model files.
- **A miss inside a returned segment:** `docs-C1` reported **no findings** over INVARIANTS lead-ins
  1–320, but did not check commit-hash citations and so missed the two dead hashes at lines 225 and
  290 that `docs-C2` spotted from outside its range. The orchestrator verified both. This is
  recorded because a clean return is only as strong as what it actually checked.
- **Checklist items not evaluable in this scope:** the exercise-driver classes (a greyed control's
  *live* reason, `disabled-no-reason` sweeps) need a window and were only read statically — several
  explorers said so explicitly. Measured numeric claims inside `docs/measurements/` (timings,
  percentages, VRAM figures) were not re-derived; that needs the original corpora or the GPU lane.
  `tests/troupe/test_ulpc.py` is permanently skipped in this tree because its fixtures are
  gitignored for CC-BY-SA reasons, so `ulpc.py`'s factual claims have no runnable proof here.
- **Defects in this skill's own table**, found by explorers and worth fixing before the next run:
  `subsystems.md`'s INVARIANTS line numbers have drifted throughout — **nine** separate segments
  reported cited leads landing 15–40 lines off, and one (`clay-document`'s "398") could not be
  resolved by content search at all. `tests/ui/` is cited as a `shell` guard but **does not exist**
  (`git ls-files` and `git log --all` both return nothing; only untracked `.pyc` files remain).
  `studio/skeletons.py` is listed in the `poser` row as a rig source but is the pane-layout table,
  and `studio/_view_overlay.py` is listed in the `tour` row as the tour's drawing side but is Clay's
  mesh-picking overlay. The `create` row's `viewer` segment and the `clay` row's `mode` segment
  disagree about who owns `_view_bounds/_drag/_overlay/_pick.py`; `create-viewer` read them.
- **Not done by this audit:** no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not close those.
  The suite was not run at all by this pass — that belongs to the fix phase.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and the test.` **That happened on 2026-09-07 for 110 of them**, in one fix pass of eighteen fixers partitioned by file ownership, plus five second rounds for files no fixer owned and three repairs where a fix broke something outside its own scope.
A human-only finding is moved to `TODO.md`. When nothing is left, this file is deleted rather than
ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it. Nothing under `src/` or `scripts/`
may cite this filename.
