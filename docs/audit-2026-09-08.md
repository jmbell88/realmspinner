# Warlock Studio audit — 2026-09-08

**Recommendation:** close the one High (Alt+drag grabs a joint instead of orbiting in the pose
viewport) before the next installer build, then take create-02 and create-03 together, since both
are places where Create discards something the user typed without saying so.

**7 findings: 0 Critical, 1 High, 5 Medium, 1 Low; 0 human-only.**

**Closed 2026-09-08: all seven are built and struck below**, each with a regression test that
failed against the unfixed code (create-03's first proof failed on a constant the fix introduced
and was re-proven on the old dialog text; create-06's first fix was a two-second wall-clock cache
in `provenance.py`, refused at review because the audit's own rule is "a generation counter, never
wall time" and a stale fingerprint in a provenance record is worse than a slow frame, and it was
rebuilt as a content-keyed memo in the pane with provenance left byte-identical). Nothing moved to
`TODO.md`. This file is kept rather than deleted because it was never committed before the fix
pass, so deleting it would leave git nothing to keep.

Audited: version **0.0.41**, working tree at **`6c89b4f6`**. Dirty at start: `clean`. Scope:
`create` → slices `create`, split into **4** segments and one explorer each (`create`: brief,
workspace, viewer, panes). `uv run ruff check .`: **All checks passed!** The suite was not run by
this audit; findings are from reading and from the offline probes below.

The previous audit (`docs/audit-2026-09-07.md`, scope `all`) closed create-01..create-10 the same
day. Every explorer was handed that list and the four commits that have touched Create since
(`89cb6412`, `16de9f7b`, `63dd1ad7`, `489d15b0`); nothing below re-lists a struck finding, and
four of the seven are in code those four commits added or left behind.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong result the
user will hit, or a promise in the docs the code breaks. *Medium*: a defect with a workaround, or
a hard-invariant violation not yet reachable. *Low*: hardening, consistency, naming. *Reproduced*:
an executable example confirmed it (scripts under
`<scratchpad>/audit-2026-09-08/probes/`, run with `uv run --no-sync python <script>`).
*Inspected*: follows from the current code path. *Evidence gap*: release proof is missing from
the repository, not a demonstrated failure.

**Second-look.** Every Critical and High below was re-read by the orchestrator against the working
tree, and **all five probe scripts left by explorers were re-run and all five reproduced**. The
High is *Inspected* (a pose-viewport press cannot be driven without a GL context); the
orchestrator read `_press` end to end and confirmed no Alt read exists in `viewer_embed.py` or
`_viewer_pose.py`. Dropped or downgraded during merge: **none**. Merged from two segments that saw
one defect from two sides: **none**. The orchestrator additionally confirmed for create-06 that
`settings_2d._resolved_recipe` carries no memo key and is called from three draw-path helpers.

## Critical

Critical — none.

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-01 — Poser and Create: the pose viewport** | ~~**Alt+drag started over a joint marker or a gizmo handle moves the joint instead of orbiting the camera.** `Viewer._press` ([viewer_embed.py:541](src/warlock/studio/viewer_embed.py)) tests the gizmo hit and the marker hit before falling through to orbit, and neither it nor `_viewer_pose.py` reads the Alt modifier anywhere, while its own comment at line 582 says "Alt+drag orbits in every mode, in every viewport" and `shortcuts.py:131,265` promise "Alt+drag: Orbit, in any mode". Clay's `_view_drag.py:185` gets it right: it checks `alt` via `_mods()` and sets `_grab = "orbit"` *before* the gizmo test. *Inspected; orchestrator read both handlers and grepped both files for `KMOD_ALT`/`get_mods`.* Joint markers cover much of a humanoid rig's footprint, so the documented "look around without disturbing anything" gesture drags a joint out of position as one clean undo step: quiet corruption of a pose, not an error.~~ **Built 2026-09-08:** `Viewer._alt_held()` is read first inside the pose-mode branch of `_press` and short-circuits to orbit before the gizmo and marker tests, mirroring `_view_drag`. `test_alt_drag_orbits_even_when_the_press_lands_on_a_pose_marker`. | Read `pygame.key.get_mods() & KMOD_ALT` at the top of `_press`'s button-1 branch and short-circuit to `_grab = "orbit"` when held, mirroring `_view_drag`. Regression: `test_alt_drag_orbits_even_when_the_press_lands_on_a_pose_marker`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-02 — Create: Texture and Retarget panels** | ~~**Looking at another asset and coming back discards the surface-texture prompt and the custom triangle budget.** `texture_panel._form` ([texture_panel.py:153](src/warlock/studio/panes/texture_panel.py)) and `retarget_panel._form` ([retarget_panel.py:128](src/warlock/studio/panes/retarget_panel.py)) keep one slot in `ctx.state.preview` and reset it whenever `form["job_id"] != job_id`. Commit `89cb6412` fixed the identical pattern in `remesh_panel`, `sheet_panel` and `sprite_panel` with a per-job dict and did not touch these two siblings. *Reproduced:* `custom_triangles` 12345 → 50000, prompt "rusted iron with mossy patches" → "". No test references `retexture_form` or `retarget_form`.~~ **Built 2026-09-08:** both panels keep a `{job_id: form}` dict in `ctx.state.preview`, the shape their three siblings got. `test_a_retexture_form_survives_a_look_at_another_asset`, `test_a_retarget_form_survives_a_look_at_another_asset`. | Give both panels the `setdefault("<name>_forms", {})` per-job dict shape their three siblings got. Regression: `test_a_retexture_form_survives_a_look_at_another_asset`, `test_a_retarget_form_survives_a_look_at_another_asset`. |
| **create-03 — Create: Reset** | ~~**The Reset confirm dialog names six things and discards far more.** The `dialogs.Confirm` text in `_reset` ([create_brief.py:499](src/warlock/studio/create_brief.py)) says the prompt, negative prompt, model, LoRA, reference and run controls go back to defaults, but `default_form_2d()` also resets `asset_type`/`generation_type`/`output` and every Tileset and Sprite arm field (`materials`, `variants`, `style_lock`, `seam_erase`, `palette`, `cell_size`). The dialog is documented in the same function as "the only guard against the accidental press now". *Reproduced:* nine unnamed fields reset. `tests/test_form_resets.py::test_both_resets_are_behind_a_confirm_dialog` checks only that a Confirm exists, not its scope.~~ **Built 2026-09-08:** the text, now the module constant `_RESET_CONFIRM_MESSAGE`, names the asset type and the Tileset, Sprite Sheet and Character fields; behaviour unchanged; chapter 22 says the same. `test_reset_confirm_text_names_every_field_it_discards`. | Broaden the text to "every image-side setting, including the asset type and any sheet, tile or sprite fields", or scope `_reset` to the fields it names. Regression: `test_reset_confirm_text_names_every_field_it_discards`. |
| **create-04 — Create: the stage rail** | ~~**The Rig segment predicts "open" for a job whose mesh exists but whose status is not `done`, and the door then refuses.** `create_stages.available` ([create_stages.py:309](src/warlock/studio/create_stages.py)) checks only `stage == "model"` and `"model.glb" in files`, while `rig.create_rig` ([service/rig.py:64](src/warlock/service/rig.py)) also requires `status == "done"`; the function's docstring promises its wording is "the service's own, verbatim" so the tooltip and the toast are one sentence. *Reproduced:* `available("rig")` returns `None` for `status=running` and `status=error`. The "reference" arm of the same function already checks status. `tests/test_create_stages.py`'s job fixture hard-codes `status: done`.~~ **Built 2026-09-08:** the rig/pose arm also refuses `status != "done"`, wording verbatim from `service/rig.py`. `test_available_for_rig_refuses_a_model_stage_job_that_is_not_done`. | Add the `status == "done"` check to the rig/pose arm, mirroring the reference arm. Regression: `test_available_for_rig_refuses_a_model_stage_job_that_is_not_done`. |
| **create-05 — Create: Mesh stage** | ~~**The inherited-value label names the wrong source when the Mesh stage's input is a selected finished mesh.** `_source_param` ([settings_3d.py:400](src/warlock/studio/panes/settings_3d.py)) reads `ctx.state.source_job` directly instead of through `_effective_source`, so with no explicit pick the "Mesh resolution"/"Background" unset option says "keep the reference's" while the muted line under it and `_submit` correctly name the mesh's parent reference. This is the case commit `63dd1ad7` fixed for the explicit pick, reintroduced for the fallback added the same day. *Reproduced:* label `keep the reference's`, effective source `ref1`. `tests/test_settings_3d_inherit.py` covers only the explicit pick and the no-source case.~~ **Built 2026-09-08:** `_source_param` resolves through `_effective_source`, as `_submit` does. `test_inherited_mesh_option_names_the_selected_meshs_reference_value`. | Resolve through `_effective_source` as `_submit` does. Regression: `test_inherited_mesh_option_names_the_selected_meshs_reference_value`. |
| **create-06 — Create: recipe resolution on the frame thread** | ~~**Every recipe resolution re-walks the checkpoint directory tree, and resolution runs inside the draw.** `file_fingerprint` ([provenance.py:63](src/warlock/provenance.py)) takes the `is_dir()` branch before its cache lookup, and `_dir_fingerprint` has no cache of its own, so `rglob("*")` plus a stat per file runs on every call; every installed base is a directory. `resolve_recipe` ([generation.py:590,610](src/warlock/generation.py)) fingerprints on each resolution, and `settings_2d._resolved_recipe` ([settings_2d.py:197](src/warlock/studio/panes/settings_2d.py)) calls it from three note helpers with no memo key, its own comment saying it "runs sixty times a second inside the draw". *Reproduced:* two identical calls, two `rglob` walks. Per-leaf hashes are cached; the walk is not.~~ **Built 2026-09-08:** `settings_2d._resolved_recipe` memoises on the request's contents plus the config's identity, so the walk runs once per form edit; `provenance.py` is unchanged, since a cache there would have to be wall-clock or need an install-time generation counter nothing bumps yet. `test_resolved_recipe_is_not_recomputed_while_the_form_is_unchanged`, `test_an_edit_and_undo_resolves_again_rather_than_reading_a_sticky_cache`. | Cache `_dir_fingerprint` on the directory's own `(path, size, mtime_ns)` ahead of the walk, and memoise `_resolved_recipe` on the form. Regression: `test_file_fingerprint_does_not_rewalk_an_unchanged_directory_every_call`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-07 — Create: matte preview** | ~~`MatteState.cache` ([matte_preview.py:82](src/warlock/studio/matte_preview.py)) keeps one up-to-384² preview per `job_id` ever opened in the promote-matte modal for the life of the process; `open_for`/`close` reset the open preview but never evict the entry the closed job left. *Inspected.* Only the current preview is ever read back.~~ **Built 2026-09-08:** `open_for` pops the outgoing job's entry when the job changes; `pump` was confirmed the only reader and it reads only the open job. `test_matte_preview_cache_does_not_grow_without_bound_across_jobs`. | Drop the previous job's entry when `open_for` moves to a new `job_id`. Regression: `test_matte_preview_cache_does_not_grow_without_bound_across_jobs`. |

## Human-only — owed, not open

Findings that need a card, a clean machine, real Aseprite or Tiled, or ears. These move to
`TODO.md` in the fix phase and are not counted as open here.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | none | — |

## Coverage

- Slices and segments run: `create`: brief, workspace, viewer, panes. Slices or segments skipped:
  none.
- Explorers that failed or timed out: none. All four returned.
- Files a segment owned but did not open, and why: `panes` read `settings_2d.py` (3007 lines)
  and `settings_character.py` (937 lines) only through the four post-audit commits' diffs and the
  sections those diffs touched, not line by line; `viewer` read `gltf.py` selectively (ceilings,
  `update_world`, skin/mesh bounds), on the orchestrator's steer, since it was audited three times
  in the preceding four days. Every other owned file was read in full.
- Files the subsystem table does not list: `studio/asset_exits.py` (new in `16de9f7b`, read by
  `brief` as a neighbour), `studio/readiness.py` (new in `89cb6412`, read by `panes` as a
  neighbour), and `studio/_viewer_pose.py` (the `PoseOps` mixin `viewer_embed.py` imports, read
  by `viewer` as a neighbour). All three belong in the `create` row of
  `.claude/skills/warlock-audit/references/subsystems.md`; that table also cites INVARIANTS line
  numbers from 2026-09-05 that have since moved (the glTF ceilings paragraph is now near line
  212), and every explorer located the paragraphs by their quoted words instead.
- Checklist items not evaluable in this scope: GL-dependent behaviour in `studio/viewer/`
  (shading output, MSAA resolve, the golden-image pins) needs a context and a card; the
  frame-thread and per-frame classes were checked by reading, not by profiling; `panes` did not
  make a dedicated pass over the staged-writes, subprocess, cancel and journal classes, none of
  which its files touch directly.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not close
  those.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and the
test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is deleted
rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it. Nothing under `src/`
or `scripts/` may cite this filename.
