# Warlock Studio audit — 2026-09-11

**Recommendation:** close the five Criticals before the next installer build — four of them are
one shape (a count read from a file decides an allocation that nothing bounds) in the Inker
codecs and the tile slicer, so they are close to one piece of work; then the two Review
blinding leaks, which quietly corrupt the judging data the whole verdict programme rests on.


> **Every finding in this file was built on 2026-09-11, the day it was written,
> and the one human-only finding moved to `TODO.md` as P44.** The gate ran once
> with nothing else editing: `ruff` clean, `20,565 passed, 48 skipped, 0 failed`
> in 2:30, `preflight --fast` passed. Each row below is struck; the regression
> test named in its right-hand column is the thing that now stands behind it,
> and every one of them was shown to fail against the unfixed code before the
> fix went in.
>
> This file is normally deleted once nothing is left open, with git keeping it.
> It is kept here because the reporting pass and the fix pass ran in one
> session, so it was never committed separately -- deleting it would have left
> nothing behind rather than a record. The substance has also gone where it
> belongs: nine paragraphs in `docs/INVARIANTS.md`, an entry in `CHANGELOG.md`,
> `TODO.md` P44 and P45, nine manual chapters, `SECURITY.md`, and a backfilled
> `docs/measurements/2026-08-01-trellis-band.md`.

**100 open findings: 5 Critical, 31 High, 31 Medium, 33 Low; 1 human-only.**

Audited: version **0.0.42**, working tree at **`32c7b6b7`**. Dirty at start: `clean`. Scope:
`all` → all fourteen slices, split into **53 segments and one explorer each**: `shell`
(boot, documents, chrome, widgets, home-library, review-settings); `create` (brief,
workspace, viewer, panes); `inker` (document, paint, sheets, codecs, flourish, mode, panes);
`clay` (mesh, document, mode); `poser` (rig, poses, mode); `troupe` (engine, jobs, render);
`plotter` (map, tiles, mode); `packwright` (packer, mode); `muse` (engine, mode, jobs);
`sirens` (engine, playback, panes); `service` (queue, kinds, assets, gates); `pipelines`
(children, image, mesh, install); `docs` (A-B, C1, C2, D, E, F1, F2); `tour` (tour-1).
`uv run ruff check .`: **All checks passed**. The suite was not run by this audit; findings
are from reading and from the offline probes below.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong result
the user will hit, or a promise in the docs the code breaks. *Medium*: a defect with a
workaround, or a hard-invariant violation not yet reachable. *Low*: hardening, consistency,
naming. *Reproduced*: an executable example confirmed it (scripts under
`<scratchpad>/audit-2026-09-11/probes/`, run with `uv run --no-sync python <script>`).
*Inspected*: follows from the current code path. *Evidence gap*: release proof is missing
from the repository, not a demonstrated failure.

**Second-look.** Every Critical and High below was re-read by the orchestrator against the
working tree; all five Criticals were confirmed by **running their probes**, and twenty-three
Highs were confirmed by probe or by opening the cited lines. Dropped or downgraded during
merge: `none`. Merged from two segments that saw one defect from two sides: **create-01**
(`clay`/document and `create`/viewer each found a different unchecked index field in
`viewer/gltf.py`), **plotter-02** (`plotter`/mode found it from the callers, `plotter`/map
confirmed it from the engine and refined it), **create-04** (`create`/viewer found it,
`poser`/mode confirmed it and showed why the docstring's justification does not cover the
automatic path).

## Critical

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| ~~**inker-01 — Inker: `.aseprite` reader**~~ *(built)* | **A single-frame `.aseprite` with N drawable layers and no cels allocates N full-canvas RGBA arrays with no ceiling on N.** [inker/asein.py:1881](src/warlock/studio/inker/asein.py). *Reproduced:* a 2,544-byte file produced **424 MiB** (174,833×); extrapolates to ~256 GiB from ~24 KB at the 8192² canvas ceiling. An instant OOM of the single-process app, taking every other open document with it. | Mirror `ora.py`'s own `_layer_budget`/`MAX_ORA_LAYERS` precedent in the still-document branch. Regression: `test_a_single_frame_aseprite_with_many_empty_layers_has_a_ceiling`. |
| ~~**inker-02 — Inker: ORA palette**~~ *(built)* | **An `.ora`'s `palette.gpl` is parsed with no row cap and assigned straight onto `doc.palette` for RGB/grayscale documents.** [inker/ora.py:1707](src/warlock/studio/inker/ora.py), [inker/gpl.py:57](src/warlock/studio/inker/gpl.py). *Reproduced:* a 44,420-byte archive took **20.0 s and 703 MiB** building 5,000,000 entries; zipguard's 1 GiB member ceiling permits ~179 M rows of that shape. The indexed path is bounded by `MAX_COLOURS`; these two are not. | Give `gpl.parse` (or `_read_palette`) a row ceiling refused by name, as `asein.py`'s `_MAX_PALETTE_ENTRIES` already does. Regression: `test_an_ora_palette_gpl_row_count_has_a_ceiling`. |
| ~~**inker-03 — Inker: ORA tilesets**~~ *(built)* | **`_read_tiles` decodes every entry in `tiles.json`'s `tilesets` list independently, with no per-document budget and no cache by member name.** [inker/ora.py:1969](src/warlock/studio/inker/ora.py). *Reproduced:* a 1,134-byte archive naming one 512² PNG 500 times cost **502 MiB**; the flat layer reader's own `planes` dict already avoids exactly this re-decode. | Apply the `_layer_budget` ceiling to the tilesets loop, and/or cache by member name. Regression: `test_an_ora_tiles_json_cannot_repeat_one_tileset_without_a_ceiling`. |
| ~~**inker-04 — Inker: floating selection**~~ *(built)* | **A floating selection's scale is floored at 0.01 and never ceilinged, so a scale-handle drag can ask `render_transform` for an unbounded allocation.** [inker/selection.py:911](src/warlock/studio/inker/selection.py), [panes/inker_canvas.py:1233](src/warlock/studio/panes/inker_canvas.py). *Reproduced:* uncaught `MemoryError`. Worse, `self.scale` is written **before** the allocation, so every later re-render retries the same failure until the transform is torn down. Reachable through ordinary use: zoomed out, a normal mouse movement is a large document-space distance over a small reference length. | Clamp scale to a pixel budget in `FloatingBuffer.transform` before the render, as `MAX_MORPH_RADIUS`/`MAX_STAMP` bound their own gestures. Regression: `test_floating_buffer_scale_is_ceilinged_before_the_render_allocates`. |
| ~~**plotter-01 — Plotter: tile slicing**~~ *(built)* | **`slicing.recompose` allocates `(rows × tile_h) × (cols × tile_w)` with no ceiling; a finely-ruled image makes `detect_grid` report thousands of 1 px cells, which are then multiplied by the *map's* tile size (up to 512).** [tilegrid/slicing.py:210](src/warlock/studio/tilegrid/slicing.py), [plotter_tilesets.py:431](src/warlock/studio/plotter_tilesets.py). *Reproduced:* a 512² source allocated **1,073.7 MB** (a 1,024× blow-up); at the real 8192² decode ceiling with a 64 px map tile the same shape asks for ~275 GB — **on the frame thread**, from an ordinary Import button, with the open map's unsaved edits at risk. | Ceiling `rows*tile_h * cols*tile_w` in `recompose` before `np.zeros`, so every caller inherits the refusal — `tileset.MAX_COLLECTION_PIXELS`'s "checked before the loop" rule. Regression: `test_recompose_refuses_an_output_past_a_pixel_ceiling`. |

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| ~~**shell-01 — Review: blinding**~~ *(built)* | **The sweep list draws `spec_summary` — "varies trellis_gss (1.0, 1.5) - 3 seed(s)" — under every row unconditionally, while Blind is on.** [review_panes.py:324](src/warlock/studio/review_panes.py). *Inspected, orchestrator-confirmed:* no `state.blind` guard; the comment beside it even says "under the name the user typed". Names exactly the axis and values blinding exists to hide. | Withhold the line under `state.blind`, as `score_line` already withholds the judge's opinion. Regression: `test_review_sweep_list_hides_axis_and_values_under_blinding`. |
| ~~**shell-02 — Review: blinding**~~ *(built)* | **The single-sweep delete confirm interpolates the raw `sweep['label']` instead of `bucket_label`.** [review_panes.py:536](src/warlock/studio/review_panes.py). *Inspected, orchestrator-confirmed.* `bucket_label`'s own docstring says the expression existed in three places and a fourth was about to be written for the removal confirm — the **bulk** path was fixed and this per-sweep one was not, un-blinding a session through the one door left open. | Use `review_mode.bucket_label(state, sweep)` at both interpolations. Regression: `test_single_sweep_delete_confirm_uses_the_blinded_label`. |
| ~~**shell-03 — Library: bulk export**~~ *(built)* | **Two bulk actions from one selection each publish their popup onto the single unkeyed slot `ctx.state._library_export` (or `_library_convert`), so the second clobbers the first.** [panes/library.py:1828](src/warlock/studio/panes/library.py). *Inspected, orchestrator-confirmed.* The dropped export never happens with no error shown, and its task thread is left blocked forever on `popup.decisions.get()`, permanently occupying one of `TaskRunner`'s four workers. Reachable in two clicks because the native pickers carry no owner window. | Key the popup per task, or refuse a second export/convert while the slot is set. Regression: `test_two_concurrent_bulk_export_tasks_do_not_clobber_each_others_popup_slot`. |
| ~~**shell-04 — Home: status rows**~~ *(built)* | **Manual 03 and 21 promise a health/"Issues" row on Home that `landing.HOME_STATUS` deliberately excludes, and never mention the "setup" row it does show.** [panes/landing.py:479](src/warlock/studio/panes/landing.py), [docs/manual/21-home.md:85](docs/manual/21-home.md). *Inspected, orchestrator-confirmed:* `HOME_STATUS = ("setup", "queue", "review")`. | Drop the Issues row from both chapters (noting it moved to the rail badge and doctor banner) and document the setup row. Regression: `test_manual_home_status_rows_match_landing_HOME_STATUS`. |
| ~~**shell-05 — Shortcuts chapter**~~ *(built)* | **Chapter 38 tells the reader to open the shortcut sheet from "the Shortcuts button in the navigation rail's footer"; no such control exists.** [docs/manual/38-shortcuts.md:4](docs/manual/38-shortcuts.md), [rail.py:1](src/warlock/studio/rail.py). *Inspected, orchestrator-confirmed:* `rail.py`'s own docstring names "shortcuts" as living in the global menu or status bar, and the footer group is only `("review", "settings")`. The chapter's existing ratchet covers "top bar" wording, not this. | Point at Ctrl+/ and Help ▸ Keyboard shortcuts, and extend the ratchet. Regression: `test_the_shortcuts_chapter_does_not_send_the_reader_to_a_rail_button_that_does_not_exist`. |
| ~~**shell-06 — Settings: UI scale**~~ *(built)* | **Manual 41 describes UI scale as a 0.5×–2× slider that "takes effect as you drag it"; it has been a five-step combo capped at 150% since 2026-09-10.** [docs/manual/41-app-settings.md:23](docs/manual/41-app-settings.md), [tokens.py:41](src/warlock/studio/tokens.py). *Inspected, orchestrator-confirmed:* `UI_SCALE_STEPS = (0.5, 0.75, 1.0, 1.25, 1.5)`; the commit that made the change touched this very file and left the paragraph. | Describe the combo and its real ceiling, ideally in terms of `UI_SCALE_STEPS`. Regression: `test_manual_ui_scale_paragraph_matches_the_combo_control`. |
| ~~**create-01 — glTF loader: index type checks**~~ *(built)* | **Several index-shaped fields reach a bare `0 <= i < len(...)` comparison with no `_check_int_index`, raising an un-messaged `TypeError` instead of the named `ValueError` the loader's own docstring claims for "every index-shaped field".** A node's `children` entries, a scene's `nodes` roots and the document's `scene` index [viewer/gltf.py:221](src/warlock/studio/viewer/gltf.py), and an image's `bufferView` [viewer/gltf.py:815](src/warlock/studio/viewer/gltf.py). *Reproduced.* **Seen from two sides:** `create`/viewer and `clay`/document each found a different field missed by the same 2026-09-09 sweep. Callers surface the raw text — Clay's wrapper embeds it verbatim. | Validate each with `_check_int_index` before the comparison. Regression: `test_a_non_integer_child_index_is_refused_with_a_value_error_not_a_type_error`. |
| ~~**create-02 — Create: character preview**~~ *(built)* | **"character-preview" completion calls `viewer.load_model()` — a synchronous glTF parse plus per-slot PNG decode — inside `_on_task_done` on the frame thread.** [main.py:2578](src/warlock/studio/main.py). *Inspected, orchestrator-confirmed.* This is the T2-class bug the 2026-09-02 review fixed for the reference-PNG and mesh paths; every "Preview character" press reintroduces the stall. `test_frame_thread_doors.py` pins `_sync_viewer` only. | Split it the way `_sync_viewer` is split: parse on the task thread, adopt on the frame thread. Regression: `test_the_character_preview_landing_does_not_decode_on_the_frame_thread`. |
| ~~**inker-05 — Inker: ORA animation**~~ *(built)* | **`_read_animation` builds one `Track` per entry in `animation.json`'s `tracks` list with no ceiling.** [inker/ora.py:1419](src/warlock/studio/inker/ora.py). *Reproduced:* a 1,349-byte archive took **9.26 s and 370 MiB** for 200,000 tracks. The shared blank plane keeps pixel cost bounded, but the ~287,000× size-to-memory ratio still reaches hundreds of GiB inside zipguard's member ceiling. | Bound `tracks` and `frames` before the lists are built. Regression: `test_an_ora_animation_json_cannot_declare_unbounded_tracks`. |
| ~~**inker-06 — Flourish: bake cost**~~ *(built)* | **`bake_cost` counts layers flat and never looks at a layer's own parameters, so a particle-heavy recipe passes `check_bake_cost` and then vastly overruns the patience budget.** [inker/flourish/recipe.py:229](src/warlock/studio/inker/flourish/recipe.py). *Reproduced:* three "particles" layers at their **published slider maxima** (count 400, size 64) passed at 78.6 M of 212 M units while rendering ~2.19 s per frame — ~656 s for the bake against the ~90 s `MAX_BAKE_COST` is calibrated to, with no cancel available. No hand-edited preset needed. | Fold each layer's cost-relevant parameters into the product, or have each primitive declare a cost multiplier. Regression: `test_check_bake_cost_refuses_a_particle_heavy_layer_that_would_blow_the_patience_budget`. |
| ~~**inker-07 — Inker: colour-mode convert**~~ *(built)* | **The convert task mutates `Document.color_mode`, the layer planes and the shared `_composite` buffer on a `TaskRunner` worker while the canvas keeps rendering that tab every frame with no busy gate.** [panes/inker_bridge.py:1249](src/warlock/studio/panes/inker_bridge.py). *Inspected, orchestrator-confirmed:* the submitted `run()` calls `commit_convert`/`cancel_convert`/`_convert_mode_run`; its docstring says "Neither branch touches `ctx` or imgui", which is true and beside the point. A ~43 s window on a 2048² plane. Its test asserts only that the work was handed to `ctx.submit`, never rendering concurrently. | Compute on the task thread and apply on the frame thread through `Done.result`, or gate the composite upload on `tab.busy`. Regression: `test_a_document_being_converted_off_thread_is_never_rendered_concurrently`. |
| ~~**clay-01 — Clay: `loop_cut`**~~ *(built)* | **`loop_cut` has no size ceiling at all, though INVARIANTS states every one of Clay's mesh ops now has one.** [clay/ops_bevel.py:83](src/warlock/studio/clay/ops_bevel.py). *Reproduced:* 263 ms at 50 k quads, 2.26 s at 400 k, linear, synchronous on the frame thread with nothing to cancel. `bevel_edges`' own `MAX_BEVELED_CORNERS`/`_refuse_size` sit a few lines above it. | Ceiling the walk before `_walk` runs. Regression: `test_loop_cut_refuses_before_it_walks_a_strip_past_a_size_ceiling`. |
| ~~**clay-02 — Clay: dissolve ceiling**~~ *(built)* | **`MAX_DISSOLVED_RING`'s stated justification is false for the shape it guards: 20,000 corners "keeps that search well under a second", but ear-clipping is quadratic on a concave ring.** [clay/ops_dissolve.py:94](src/warlock/studio/clay/ops_dissolve.py), [clay/earclip.py:186](src/warlock/studio/clay/earclip.py). *Reproduced:* 897 ms at 1,600 corners, 3.63 s at 3,200 — extrapolating to **~140 s at the pinned ceiling**, on the frame thread, uncancellable. The concave case is the one `ops_dissolve.py`'s own docstring says a dissolve *routinely* produces. | Bound by measured cost on a worst-case concave polygon, or fall back to a fan past a concave-specific corner ceiling; pin the real number in `docs/measurements/`. Regression: `test_dissolving_a_zigzag_selection_to_the_ring_ceiling_does_not_take_minutes`. |
| ~~**clay-03 — Clay: GLB import**~~ *(built)* | **`glbio.split_glb` never checks the JSON chunk parses to an object, and `_declared_budget` runs *before* `glb_to_claydoc`'s protective wrapper.** [glbio.py:22](src/warlock/glbio.py), [clay/glbimport.py:95](src/warlock/studio/clay/glbimport.py). *Reproduced:* a GLB whose JSON chunk is a bare array surfaces a raw `AttributeError`, defeating entirely the broad `except Exception` that exists to turn any malformed file into "This file could not be read as a GLB". | Check `isinstance(doc, dict)` in `split_glb` — fixes every caller at once. Regression: `test_a_glb_whose_json_chunk_is_not_an_object_is_refused_by_name_not_a_bare_attributeerror`. |
| ~~**clay-04 — Clay: boolean operations**~~ *(built)* | **`ops_boolean` implements union, difference and intersection, and an MCP agent can invoke all three, but `clay_ops.OPS` — the one table the context menu, the tools grid and the keyboard all read — registers only `union`.** [clay_ops.py:1374](src/warlock/studio/clay_ops.py), [clay/ops_boolean.py:54](src/warlock/studio/clay/ops_boolean.py). *Inspected, orchestrator-confirmed:* `KINDS = ("union", "difference", "intersection")`. A workspace named for booleans ships two of its three verbs with no human control anywhere. (Manual 30 promises only Union and Merge, so no docs promise is broken — checked.) | Register `difference` and `intersection` mirroring `_union`'s shape. Regression: `test_every_ops_boolean_kind_has_a_human_reachable_op`. |
| ~~**poser-01 — Poser: job poses**~~ *(built)* | **A job's own pose file gets only the file-level guards and never the semantic validation library poses get, so a hand-edited one throws a bare `KeyError` out of `posed_model`, and a malformed quaternion is forwarded unvalidated into the Blender worker.** [service/rig.py:222](src/warlock/service/rig.py), [pipelines/blender_worker.py:2205](src/warlock/pipelines/blender_worker.py). *Reproduced.* INVARIANTS names this exact class as fixed for `rig.json` and for library poses; the identical hole is open for job poses, and it wastes a whole Blender spin-up before failing. | Run `validate_bones`/`validate_pose` at the read door, mirroring `service.poses._record_or_not_found`. Regression: `test_a_job_pose_file_missing_bones_is_refused_cleanly_not_a_key_error`. |
| ~~**poser-02 — Poser: root offset**~~ *(built)* | **"Save pose to this asset" never writes `root_translation`, and `apply_asset_pose` never restores one, though the shared-library save/apply pair both handle it.** [poser_mode.py:659](src/warlock/studio/poser_mode.py). *Inspected, orchestrator-confirmed:* `root_translation` appears in `_payload`/`apply_pose` and in neither asset-scoped door. A crouch or hop authored with Move root — the manual's own worked example — is silently dropped, and the baked GLB and sheet rows come out standing. | Add the field to both, mirroring `_payload`/`apply_pose`. Regression: `test_save_pose_to_asset_round_trips_the_root_offset`. |
| ~~**troupe-01 — Troupe: layout refusals**~~ *(built)* | **Four layout-plan refusals raise `Invalid` with no `field`, though the pane draws `form_ui.note("layout")` to catch exactly them.** [service/troupe.py:287](src/warlock/service/troupe.py), [service/characters.py:548](src/warlock/service/characters.py), [panes/troupe_settings.py:148](src/warlock/studio/panes/troupe_settings.py). *Inspected, orchestrator-confirmed:* the pane's own comment states the contract the door breaks. An unrenderable frame count fails to highlight the table it is about. | Pass `field="layout"` on the reachable `ValueError` branch at all four sites. Regression: `test_a_layout_that_cannot_be_planned_is_refused_with_field_layout`. |
| ~~**plotter-02 — Plotter: object delete**~~ *(built)* | **A dock Shift+click can build a selection spanning two object layers; every delete path then calls `remove_objects` against only the active layer, which silently ignores uids it does not find and returns a count no caller checks — then all callers clear the whole selection.** [panes/plotter_objects.py:167](src/warlock/studio/panes/plotter_objects.py), [plotter_mode.py:1164](src/warlock/studio/plotter_mode.py), [panes/plotter_layers.py:1038](src/warlock/studio/panes/plotter_layers.py), [plotter/_map_objects.py:141](src/warlock/studio/plotter/_map_objects.py). **Seen from two sides:** the engine explorer confirmed the contract is sound — the count is accurate and tested — so the defect is the three unchecking callers plus `plotter_state.selected_objects` being a flat `set[int]` with no per-layer scoping. The comment at the delete site promising "a Delete over five objects is one Ctrl+Z" is false across layers. | Scope the dock selection per layer, or group by owning layer and delete per layer, pruning the selection to what was actually removed. Regression: `test_deleting_a_dock_selected_group_spanning_two_object_layers_removes_every_object`. |
| ~~**packwright-01 — Packwright: crash recovery**~~ *(built)* | **`_journal_adopt` reads and decodes a recovered `.wpack` synchronously on the frame thread, because Recover calls `journal.take` inside `draw()` with no `ctx.submit` in the chain.** [packwright_mode.py:958](src/warlock/studio/packwright_mode.py). *Reproduced:* 137.7 ms for a 300-sprite, 17.4 MB pack — an ~8-frame freeze, and `MAX_PACK_SOURCE_BYTES` allows ~1 GB. Clay's and Inker's `_journal_adopt` both defer through `ctx.submit` with a docstring naming this hazard. | Submit a `packwright-recover:` task and adopt from `on_task_done`. Regression: `test_packwright_journal_adopt_reads_and_decodes_on_a_task_not_the_frame_thread`. |
| ~~**packwright-02 — Packwright: tile-set import**~~ *(built)* | **A tile-set import started from tab A lands in whichever tab is active when it resolves.** [packwright_mode.py:207](src/warlock/studio/packwright_mode.py), [packwright_state.py:157](src/warlock/studio/packwright_state.py). *Reproduced, orchestrator-confirmed:* `tileset_import` is a single state-wide field that records only that the tab is still *open*, never which asked, and `import_tileset` resolves through `active(ctx)`. Sprites are silently added to the wrong atlas; the sibling `packwright-add` door in the same file routes correctly by task key. | Record the requesting tab's uid and target it, declining if it has closed. Regression: `test_a_tileset_import_lands_in_the_tab_that_requested_it_not_whichever_is_active`. |
| ~~**muse-01 — Muse: audio device**~~ *(built)* | **Nothing in Muse's mode or panes ever calls `sirens_audio.available()`, so on a device-less machine the transport stays enabled and Play silently does nothing.** [muse_mode.py:500](src/warlock/studio/muse_mode.py), [panes/muse_player.py:274](src/warlock/studio/panes/muse_player.py). *Inspected, orchestrator-confirmed:* `_play_from`'s two `sirens_audio.play(...)` calls have **no `else`** — Stop-then-Play, seeking and "Play the loop" all no-op; only the first-decode landing toasts. Manual 35 states outright "A machine with no device says so rather than offering a dead button". `sirens_transport.py` already uses the right idiom. | Grey both transports with `reason=sirens_audio.unavailable_reason()`, and toast on refusal in `_play_from`. Regression: `test_pressing_play_with_no_audio_device_tells_the_user_rather_than_doing_nothing`. |
| ~~**muse-02 — Muse: ACE-Step extend**~~ *(built)* | **The vendored sampler's `is_extend` reattach concatenates the *other* branch's variable on the batch axis instead of the time axis.** [pipelines/acestep/pipeline_ace_step.py:1409](src/warlock/pipelines/acestep/pipeline_ace_step.py). *Inspected, orchestrator-confirmed:* the code reads `torch.cat([to_right_pad_gt_latents, target_latents], dim=0)` inside `if to_left_pad_gt_latents is not None:`. A right-only extend raises `TypeError`; both-sided corrupts the batch dimension — not the graceful trim the door's `MAX_EXTEND_DURATION` comment describes. The door's seconds limit also sits ~0.09 s above the sampler's frame-truncated ceiling, leaving a reachable window. | Fix to `[to_left_pad_gt_latents, target_latents], dim=-1`, re-derive the vendor diff and update `ATTRIBUTION.md`; tighten the door's boundary. Regression: `test_extend_near_the_240s_ceiling_reattaches_the_correct_pad_tensor_on_the_time_axis`. |
| ~~**sirens-01 — Sirens: `.wsng` size**~~ *(built)* | **Every `.wsng` ever saved is written uncompressed: the archive is opened `ZIP_DEFLATED` but every member goes through a bare `ZipInfo`, whose `compress_type` defaults to `ZIP_STORED`.** [sirens/wsng.py:178](src/warlock/studio/sirens/wsng.py). *Reproduced, orchestrator-confirmed:* 5× for `song.json`, ~30× for a sparse pattern, compounding for samples up to ~50 MB each. `ora.py`'s `_member()` helper exists precisely to prevent this and names the failure in its own docstring. | Set `info.compress_type = zipfile.ZIP_DEFLATED` per member, mirroring `_member()`. Regression: `test_wsng_bytes_actually_deflates_its_members`. |
| ~~**sirens-02 — Sirens: panic key**~~ *(built)* | **Shift+Escape stops the device but never bumps `play_request` or clears `sounding`, so an audition still rendering is judged "still wanted" and plays seconds after the user demanded silence.** [sirens_keys.py:25](src/warlock/studio/sirens_keys.py), [sirens_play.py:413](src/warlock/studio/sirens_play.py). *Reproduced, orchestrator-confirmed.* The one binding whose entire purpose is "silence right now" reintroduces the S1 defect (2026-09-05) through a second path that bypasses `sirens_play.stop`'s withdrawal. | Call `sirens_play.stop(ctx)` instead of `sirens_audio.stop()`. Regression: `test_the_panic_key_withdraws_a_pattern_audition_still_rendering`. |
| ~~**service-01 — Settings: Storage**~~ *(built)* | **`downloads.disk_usage` walks only `t2i_model_root` and `trellis_models_dir`, never `trellis_runtime_dir`.** [service/downloads.py:343](src/warlock/service/downloads.py). *Inspected, orchestrator-confirmed.* Since the reconstruction engine became its own download (2026-09-10, ~0.82 GB), the Storage panel's "measured, not declared" figure is silently short by the engine's files — the very number that function's docstring says it exists to fix. `fetch.removal_plan` already uses all three roots. | Add `trellis_runtime_dir` as a third root, deduped as `engine_root` already is. Regression: `test_disk_usage_includes_the_downloaded_trellis_runtime_directory`. |
| ~~**service-02 — Service: matte staging**~~ *(built)* | **`matte.prepare()` stages `cutout.png` through a fixed temp name with no `convert_lock`, and three independent doors can reach it.** [service/matte.py:240](src/warlock/service/matte.py). *Reproduced:* two concurrent calls both raised a Windows `PermissionError` on `os.replace` and **no `cutout.png` was written for either**. This is the M03 `.name.tmp` incident recurring in the one staged write in the segment that follows neither established pattern (tokenised name, or lock). The promote path degrades silently; the modal and the Inker hand-off surface a failed task. | Wrap in `svc.convert_lock(job_id, matte.CUTOUT)` or route through `files._staged_write`'s tokenised name. Regression: `test_concurrent_matte_prepare_calls_for_one_job_do_not_share_a_temp_name`. |
| ~~**service-03 — Queue: cancel logging**~~ *(built)* | **An exception raised while a cancel is set but not yet committed falls through both branches and is discarded entirely — no `log.exception`, no error log, `error` stays `None`, and the row is written as a plain `cancelled`.** [queue.py:1788](src/warlock/queue.py). *Inspected, orchestrator-confirmed:* the two branches are `is_set() and committed` and `not is_set()`. A real bug coinciding with a Cancel press leaves zero trace anywhere and reads as a clean cancel. The sibling branch's comment documents this exact class being fixed once. | Always log here regardless of `committed`, as the committed branch does. Regression: `test_an_unrelated_exception_during_a_pending_cancel_is_still_logged`. |
| ~~**pipelines-01 — Doctor: manual**~~ *(built)* | **Three manual chapters still call a missing `trellis-server.exe` (and the GGUF weights) a FATAL doctor row; both became `fatal=False, pending_install=True` on 2026-09-10.** [docs/manual/39-installation.md:368](docs/manual/39-installation.md), [docs/manual/40-configuration.md:26](docs/manual/40-configuration.md), [docs/manual/42-troubleshooting.md:99](docs/manual/42-troubleshooting.md), [doctor.py:466](src/warlock/doctor.py). *Inspected, orchestrator-confirmed:* `fatal=False, pending_install=True` at lines 466, 494 and 534; chapter 39 contradicts **itself**, correctly at line 96 and wrongly at 368. A fresh user is told their working install is broken when doctor reports `[SETUP]` and exits 0 — the false alarm the change was written to end. | Name only the VRAM budget as fatal; rewrite the 42 and 40 rows as setup/pending. Regression: `test_manual_does_not_call_the_engine_or_gguf_rows_fatal`. |
| ~~**docs-01 — Measurements: `trellis_band`**~~ *(built)* | **`trellis_band` — the constant `CLAUDE.md` itself names as *the* example of a corpus-keyed constant — has no `docs/measurements/` document, and none was ever deleted.** [config.py:424](src/warlock/config.py). *Evidence gap, orchestrator-confirmed:* 62 documents on disk, none for it; its entire justification (a dated 2026-08-01 sweep table) lives only as a code comment. The rule this directory exists to enforce has never been honoured for its own headline example, so a future change has no baseline to be measured against. | Backfill a dated document transcribing the sweep already in the comment, and point the comment at it as every sibling constant does. Regression: `test_trellis_band_default_is_backed_by_a_dated_measurement_document`. |
| ~~**docs-02 — Manual: tool keys**~~ *(built)* | **Chapter 5 says in bold that "pressing that letter again cycles within the group — so `B` is the brush, `B` again is the spray"; no such gesture exists.** [docs/manual/05-drawing.md:33](docs/manual/05-drawing.md), [inker_ops.py:2910](src/warlock/studio/inker_ops.py). *Inspected, orchestrator-confirmed:* Spray binds to `Shift+B` and `A`. A reader pressing `B` twice gets Brush, still. The chapter's own per-tool letters are individually correct; only the mechanism sentence is wrong. | Describe the real mechanism — each tool has its own dedicated letter — and drop the example. Regression: `test_chapter05_tool_group_prose_matches_actual_key_bindings`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| ~~**shell-07 — Quit: pack commit**~~ *(built)* | A quit deferred while a `pack:` task is in its uninterruptible commit phase is never resumed if that task **fails** — `_resume_deferred_quit` is called only from the success half. [main.py:1577](src/warlock/studio/main.py). *Reproduced.* The toast says "Quitting once it finishes"; on a failed install the request is silently dropped. | Resume from the failure branch too. Regression: `test_a_deferred_quit_resumes_after_the_commit_phase_fails_not_only_after_it_succeeds`. |
| ~~**shell-08 — Forms: undo folding**~~ *(built)* | `Form.slider`/`number`/`text` draw the field inside `Form.field()`'s `with` block, which then draws a trailing note and `dummy()`, so the imgui "last item" on return is the dummy — a caller's `controls.fold_undo` is a no-op, and `Form` exposes no `commit`/`history` parameter. [forms.py:121](src/warlock/studio/forms.py). *Inspected.* Structurally un-foldable from outside; invisible until someone drags and checks undo depth. | Give the three an optional `history`/`commit` and fold inside the block. Regression: `test_form_slider_used_for_an_undoable_field_folds_a_multi_frame_drag_into_one_step`. |
| ~~**create-03 — Create: preview adoption**~~ *(built)* | `_show_character_preview` adopts unconditionally, with no token or selection check, unlike `_adopt_model`'s `viewer.pending` gate. [main.py:2578](src/warlock/studio/main.py). *Inspected.* A build landing after the user moved on silently replaces the viewport. | Record a token at submit and compare at landing. Regression: `test_a_character_preview_that_lands_after_the_user_navigates_away_does_not_hijack_the_viewport`. |
| ~~**create-04 — Poser: viewport parse**~~ *(built)* | `sync_asset`/`sync_preview` are called from `_poser_viewport`'s draw every frame, and on a path or template change call `viewer.load_model()` synchronously. [poser_mode.py:961](src/warlock/studio/poser_mode.py), [poser_viewport.py:96](src/warlock/studio/poser_viewport.py). **Seen from two sides:** `poser`/mode confirmed and refined it — the docstrings justify the parse by citing `pose_panel._enter`, but that is a button press in the same frame, while `_land_rerig` fires when a background Blender job completes on an arbitrary frame, exactly the case `_sync_viewer`'s split exists for. `test_frame_thread_doors.py`'s door list omits Poser. | Route both through the parse/adopt split. Regression: `test_poser_does_not_decode_the_rig_on_the_frame_thread`. |
| ~~**create-05 — Generation: img2img round-trip**~~ *(built)* | `GenerationRequest.from_dict` never reads `init_image` or `init_strength` back, so a request rebuilt from a dict silently loses its img2img intent — no error, no `CompatibilityIssue`, no toast. [generation.py:227](src/warlock/generation.py), [service/jobs.py:135](src/warlock/service/jobs.py). *Reproduced.* The door explicitly supports raw dicts, and the module docstring calls itself safe for migration and API validation. | Read both fields in `from_dict` as every other scalar is read. Regression: `test_from_dict_round_trips_init_image_and_init_strength`. |
| ~~**inker-08 — Inker: filter radius**~~ *(built)* | `blur`/`sharpen` take an unceilinged radius, unlike `despeckle` (`DESPECKLE_MAX`) and `outline` (`OUTLINE_MAX_SIZE`) in the same file, which clamp internally precisely so a stale settings entry cannot go round the slider. [inker/filters.py:308](src/warlock/studio/inker/filters.py). *Reproduced:* 1.45 s and 385 MB at radius 2000 on an 8×8 canvas, versus 0.001 s at the slider's own maximum — and `preview_filter` runs on the frame thread. | Clamp to a named module constant inside `blur`/`sharpen`. Regression: `test_blur_radius_is_capped_independent_of_the_shared_radius_span`. |
| ~~**inker-09 — Inker: sheet import**~~ *(built)* | `MAX_SHEET_FRAMES` is enforced only in `grid_rects`; `document_from_sheet`, fed verbatim from a rendered sheet's on-disk sidecar, builds one Frame and Layer per cell with no ceiling. [inker/sheetin.py:341](src/warlock/studio/inker/sheetin.py), [inker_open.py:452](src/warlock/studio/inker_open.py). *Reproduced:* 65,536 cells (16× the ceiling) accepted in 0.24 s. The 1 MB sidecar cap still permits ~7× the ceiling. | Check the cell count in `document_from_sheet`, or hoist the check into `_document_from_rects`. Regression: `test_document_from_sheet_refuses_a_cell_count_past_max_sheet_frames`. |
| ~~**inker-10 — Inker: walk canvas**~~ *(built)* | `panes/inker_walk_canvas.py`'s decidable half — `_radius`'s zoom conversion and `owns_mouse`/`handle`'s ownership gate between the walk session and the paint tools — has no test anywhere. [panes/inker_walk_canvas.py:171](src/warlock/studio/panes/inker_walk_canvas.py). *Inspected.* A regression would silently let a walk-setup click paint on the drawing. | Add a headless test module for the three. Regression: `test_walk_canvas_owns_the_mouse_only_while_a_session_is_open`. |
| ~~**clay-05 — Clay: hole fill**~~ *(built)* | `fill_hole` calls `boundary_loops(mesh)` — which walks every boundary corner in the whole mesh — before its ring-size refusal. [clay/adjacency.py:279](src/warlock/studio/clay/adjacency.py), [clay/ops_topo.py:686](src/warlock/studio/clay/ops_topo.py). *Reproduced:* 878 ms to fill a 4-edge hole in a mesh with many small unrelated holes — the kind `adjacency.py`'s own docstring says a real GLB routinely produces. | Flood only from the selected edges, or cap the corners walked. Regression: `test_filling_a_small_hole_does_not_pay_for_every_other_holes_boundary_walk`. |
| ~~**clay-06 — Clay: deleted-object cache**~~ *(built)* | `selection.delete_selected` reimplements object removal inline instead of calling `ClayDoc.remove_object`, and never pops `_mesh_stamps`, so each deleted object's full CSR arrays stay alive for the document's life. [clay/selection.py:112](src/warlock/studio/clay/selection.py). *Reproduced.* INVARIANTS already states this rule for `ClayState.manifold` — "wherever an object leaves `doc.objects`", not wherever one audit reached — and the sweep never extended to `_mesh_stamps`. An agent mints a stamp per tool call. | Pop the stamp in that branch, or route through `remove_object`. Regression: `test_deleting_selected_objects_through_delete_selected_drops_their_mesh_stamp_entries`. |
| ~~**clay-07 — Clay: `.wblk` reader**~~ *(built)* | An object entry's `params` is passed to `dict(...)` with no type check, so a hand-edited archive raises a bare `TypeError` instead of the named refusal every other field in the same function gets. [clay/serialize.py:646](src/warlock/studio/clay/serialize.py). *Reproduced.* This is the file crash recovery reads with no user to ask. | Validate `params` is a dict or None. Regression: `test_a_wblk_objects_non_dict_params_field_is_refused_by_name_not_a_bare_exception`. |
| ~~**clay-08 — Clay: inset region mode**~~ *(built)* | `ops_topo.inset_faces(..., region=True)` — a documented, tested mode — is reachable from no control at all, because the registered `"inset"` op declares only `thickness`/`depth`, and `agent_clay.py` derives its schema from the same `Op.params`. [clay_ops.py:1499](src/warlock/studio/clay_ops.py), [clay/ops_topo.py:283](src/warlock/studio/clay/ops_topo.py). *Inspected.* Unreachable on both surfaces at once. (Manual 30 does not promise it — checked.) | Add a boolean `region` param, as `place-between`'s `fit` already does. Regression: `test_inset_faces_region_mode_is_reachable_through_the_op_registry`. |
| ~~**poser-03 — Rigging: template reads**~~ *(built)* | `_load_templates`, `_load_pose_library` and `_load_clip_library` read JSON with no size check, while `read_record` in the same file stats and refuses over `MAX_RECORD_BYTES` first. [rigging.py:99](src/warlock/rigging.py). *Reproduced.* One of the three reads `user_clip_dir()`, which the file's own docstring calls user-editable. | Give all three the same stat-before-read guard. Regression: `test_an_oversized_clip_library_file_is_skipped_not_fully_read`. |
| ~~**poser-04 — Clips: read door**~~ *(built)* | `_load_clip_library`/`parse_clip_library` apply no per-item ceiling on the way in, though `service.clips.save`'s `_check_shape` caps `MAX_LIBRARY_KEYS`/`MAX_KEYS` on the way out. [rigging.py:327](src/warlock/rigging.py). *Reproduced.* The two doors disagree. | Cap the counts at read as well, so the doors agree. Regression: `test_an_oversized_user_clip_library_is_refused_before_being_read`. |
| ~~**poser-05 — Poser: re-rig tracking**~~ *(built)* | `rerig_job_id`/`rerig_source_job` are single un-scoped fields, so re-rigging a second asset before the first lands overwrites the tracking with no check. [poser_mode.py:211](src/warlock/studio/poser_mode.py). *Inspected.* `PoserState.asset_poses_loading`'s own docstring names this as the 2026-09-08 finding poser-04, fixed there by scoping per job id; the same fix never reached these two. | Scope per job id, as `asset_poses_loading` now is. Regression: `test_rerigging_a_second_asset_does_not_drop_tracking_of_the_first`. |
| ~~**troupe-02 — Troupe: derived params**~~ *(built)* | `rerender_charsheet` strips the previous run's derived answers with a hand-written four-key tuple instead of deriving the set from `validation.DERIVED_PARAMS`. [service/troupe.py:507](src/warlock/service/troupe.py). *Inspected.* This function was already bitten once by exactly this (2026-09-07, troupe-04); its test re-asserts the same four hardcoded keys, so a fifth addition would go unstripped. | Filter over `DERIVED_PARAMS`. Regression: `test_rerender_charsheets_strip_list_is_derived_from_derived_params_not_hand_copied`. |
| ~~**troupe-03 — Troupe: reframe cancel**~~ *(built)* | `_charsheet`'s reframe retry fires a second full Blender render with no cancel check between the two; the only check sits after both. [_q_troupe.py:227](src/warlock/_q_troupe.py). *Inspected.* A cancel during the first render is not honoured until a second "minute a go" render has also finished. | Check the cancel event before the retry, as the pixel-art pass already does. Regression: `test_a_cancel_between_the_first_render_and_the_reframe_retry_skips_the_second_render`. |
| ~~**troupe-04 — Troupe: palette list**~~ *(built)* | `troupe_mode.options()` and `settings_character.options()` cache the installed-palette list for the life of the process with no invalidation. [troupe_mode.py:1172](src/warlock/studio/troupe_mode.py), [panes/settings_character.py:105](src/warlock/studio/panes/settings_character.py). *Reproduced.* A palette dropped in mid-session never appears until restart, though `service/palettes.py`'s docstring calls this a drop-in-while-running directory and `inspector.palette_names` already got this exact fix. | Key the cache on `stamps.stamp_ns(config.palette_dir)`. Regression: `test_a_palette_dropped_in_mid_session_appears_in_the_troupe_and_character_forms`. |
| ~~**plotter-03 — Tilegrid: role inference**~~ *(built)* | `infer_roles` walks `rows × cols` cells in a pure-Python loop up to four times with no ceiling, and `_terrain_from` calls it unconditionally whenever the count reaches 47 — true for nearly any real image. [tilegrid/roles.py:162](src/warlock/studio/tilegrid/roles.py). *Reproduced:* ~84 µs/cell, extrapolating to ~23 minutes for an 8192² image at a 2 px map tile, occupying a task-runner thread with nothing to cancel. | Ceiling the cell count before the loop. Regression: `test_infer_roles_refuses_a_cell_count_past_a_ceiling`. |
| ~~**plotter-04 — Plotter: tileset editor**~~ *(built)* | The Tiles tab draws one imgui button per tile, every frame, with no cap or windowing. [panes/plotter_tileset_editor.py:195](src/warlock/studio/panes/plotter_tileset_editor.py). *Inspected.* The sibling palette in `plotter_tileset.py` draws the atlas as one image and its docstring gives the reason at 256 tiles; a 2048² sheet at 16 px is 16,384 buttons per frame. Nothing bounds an imported tileset's tile count. | Draw the atlas as one image with a draw-list selection, or clip to the visible region. Regression: `test_the_tiles_tab_does_not_draw_a_button_per_tile_on_a_large_tileset`. |
| ~~**packwright-03 — Packwright: `assert`**~~ *(built)* | `_extrude`'s only defence against a margin-violating frame is a bare `assert`, compiled out under `python -O`. [packwright/compose.py:33](src/warlock/studio/packwright/compose.py). *Reproduced.* Inker has an AST scan forbidding exactly this, with reasoning that is nearly a paraphrase of this module's own comment — but it is scoped to `studio/inker/` only. | Raise explicitly, and add the AST scan over `studio/packwright/`. Regression: `test_extrude_refuses_a_margin_violating_frame_even_under_dash_o`. |
| ~~**muse-03 — Muse: loop finder**~~ *(built)* | A `find_loops` result is adopted onto whatever player holds that job id with no check it is still the search being waited on, unlike the `LOAD_PREFIX` branch a few lines below. [muse_mode.py:270](src/warlock/studio/muse_mode.py). *Reproduced.* An abandoned search lands later and overwrites a region the user has since set by hand, with no press behind it. | Guard on `one.finding` or a request counter, mirroring the M11 fix on decode. Regression: `test_a_stale_find_loops_result_does_not_override_a_region_set_after_the_search_was_abandoned`. |
| ~~**sirens-03 — Sirens: leaving the mode**~~ *(built)* | `release_all`'s docstring claims "silencing the device is exactly what leaving the mode already does", but `state.set_mode` has no such step and `release_all` is called from nowhere but the panic key — and is absent from `main.py`'s own app-quit sweep, which does call Inker's, Plotter's and Packwright's. [sirens_keys.py:29](src/warlock/studio/sirens_keys.py), [main.py:5107](src/warlock/studio/main.py). *Inspected.* Audio keeps playing with no visible transport. | Wire `sirens_play.stop(ctx)` into the mode-leave path, or correct the docstring. Regression: `test_leaving_sirens_mode_stops_a_sounding_song`. |
| ~~**sirens-04 — Sirens: disabled reasons**~~ *(built)* | The bridge's Export and Compose buttons pick their disabled reason with an inline ternary. [panes/sirens_bridge.py:92](src/warlock/studio/panes/sirens_bridge.py). *Inspected.* This is the fourth instance of a shape that shipped a wrong, state-inverted message three separate times in this pane family (sirens-03/04/05, then 2026-09-08); every sibling was pulled into a pure tested function and these two were missed by all of those sweeps. | Pull out `export_reason`/`compose_reason` and test all four state combinations. Regression: `test_bridge_export_and_compose_reasons_are_pulled_out_and_tested`. |
| ~~**pipelines-02 — Reference: unmeasured marker**~~ *(built)* | `reference.normalise()` discards `out_report.extra` wholesale when re-measuring its own padded canvas comes back unmeasured, losing the `"measured": False` marker `rank.composition_score` depends on. [pipelines/reference.py:518](src/warlock/pipelines/reference.py), [pipelines/rank.py:105](src/warlock/pipelines/rank.py). *Reproduced.* The candidate is then scored from default fields as a real, poorly-framed measurement — reopening, one call site later, the miscalibration pipelines-03 closed on 2026-09-08 for the input-image case. | Merge `out_report.extra` rather than replacing it. Regression: `test_normalise_canvas_leak_keeps_the_unmeasured_marker`. |
| ~~**pipelines-03 — TRELLIS: launch check**~~ *(built)* | `ensure_started()` checks `exe.exists()` rather than `is_file()`, so a damaged unpack that left a *directory* at that name passes and then fails inside `Popen` with a raw `OSError`. [pipelines/trellis.py:294](src/warlock/pipelines/trellis.py). *Reproduced.* `doctor.py`'s own L01 fix and `optimize.py`'s gltfpack check both use `is_file()` for this exact case; the actual launch path was never updated. | Use `is_file()`. Regression: `test_ensure_started_refuses_a_directory_named_trellis_server_exe_with_a_clear_error`. |
| ~~**docs-03 — Install: extras table**~~ *(built)* | The `music` row lists five packages; `pyproject.toml`'s `music` extra also pulls torchaudio, librosa, loguru, py3langid, pypinyin, num2words, hangul-romanize, cutlet and fugashi — including the torchaudio its own Demucs claim needs. [docs/manual/39-installation.md:35](docs/manual/39-installation.md). *Inspected.* Understates install weight for a reader sizing the pack. | List the full set or point at `pyproject.toml` as the `rig` row already does. Regression: `test_installation_music_extras_table_lists_every_declared_package`. |
| ~~**docs-04 — Security: MCP bridge**~~ *(built)* | `SECURITY.md`'s "What is in scope" never mentions the MCP agent bridge — a same-machine named-pipe/socket listener accepting JSON-RPC from an external local process, landed in 0.0.42 — though its own module docstring flags the class of risk. [SECURITY.md:13](SECURITY.md), [mcp/pipe.py:1](src/warlock/mcp/pipe.py). *Inspected.* A reporter gets no signal this surface exists or is meant to be probed. | Add a bullet naming `warlock/mcp/` and its framing, noting it is off by default and same-user. Regression: `test_security_md_names_the_mcp_bridge_as_in_scope`. |
| ~~**docs-05 — INVARIANTS: publisher count**~~ *(built)* | The paragraph says "Eight stages publish" and that the test "scans all eight publishers"; `PUBLISHERS` has had nine entries since `_remesh` was added on 2026-09-08. [docs/INVARIANTS.md:178](docs/INVARIANTS.md), [tests/test_job_durability.py:207](tests/test_job_durability.py). *Reproduced.* The guard is correct; the prose describing it went stale — in the very paragraph warning that "the guard against this defect had itself gone stale". | Say nine, name `_remesh`. Regression: `test_invariants_publishers_count_matches_the_guard_table`. |
| ~~**docs-06 — INVARIANTS: probe blind spot**~~ *(built)* | The control-probe paragraph says "ten of them, all in `widgets.py`"; the pinned test's own `RAW_IMGUI_CONTROLS = 9`, lowered by the 2026-09-08 button-vocabulary pass, whose change the test file's comment records. [docs/INVARIANTS.md:681](docs/INVARIANTS.md), [tests/test_probe.py:30](tests/test_probe.py). *Reproduced.* A reader undercounts the probe's blind spot by one. | Say nine. Regression: `test_invariants_raw_imgui_controls_count_matches_the_pinned_probe_constant`. |
| ~~**docs-07 — Measurements: stranded pre-registration**~~ *(built)* | `2026-08-10-zelda-props.md` is an open pre-registration ("Results — to be filled in after the run") whose entire procedure depends on guidance fields and sweep scripts deleted seven days later by `2026-08-17-taxonomy-retirement.md`, with no cross-reference either way. [docs/measurements/2026-08-10-zelda-props.md](docs/measurements/2026-08-10-zelda-props.md). *Inspected.* A reader who tries to run it hits a dead end with nothing explaining why. | Add a superseded banner pointing at the retirement document. Regression: `test_zelda_props_pre_registration_is_marked_retired_after_taxonomy_removal`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| ~~**shell-09 — Shortcuts: F10**~~ *(built)* | F10, listed under "Everywhere" on the Ctrl+/ sheet, is swallowed while the Manual is open, because the Manual-owns-it guard returns first for every key but Escape. [main.py:3107](src/warlock/studio/main.py). *Reproduced.* | Move the F10 check above the guard, beside Ctrl+K/Ctrl+//F1. Regression: `test_f10_toggles_the_frame_rate_readout_even_while_the_manual_is_open`. |
| ~~**shell-10 — Journal: stale inventory**~~ *(built)* | `journal.py`'s module docstring still lists a `.profile.json` kind deleted whole on 2026-08-31, and never mentions Sirens' `.wsng`, the real sixth format; `landing._KIND_MODES`' comment claims two exceptions where one remains. [journal.py:4](src/warlock/studio/journal.py), [panes/landing.py:705](src/warlock/studio/panes/landing.py). *Inspected.* The provider set itself is correct and complete — verified against `modes.KEYS` and every registration site. | List the current six and fix the comment; tighten the two partial-set assertions to the exact six. Regression: `test_journal_docstring_and_kind_modes_comment_name_no_deleted_profile_kind`. |
| ~~**shell-11 — Rail: stale comment**~~ *(built)* | The row-compression comment says "Adding Troupe made the rail fifteen rows"; `rows` computes 13 today and never computed 15 at that commit — the figure counted four footer utility rows the variable no longer includes. [rail.py:387](src/warlock/studio/rail.py). *Inspected.* It is the stated justification for the floor arithmetic. | Cite the current count or drop the figure. |
| ~~**shell-12 — Inspector: busy button**~~ *(built)* | "Read the trellis log" greys with no `reason=` and no spinner while the fetch is in flight, unlike the two sibling busy-artifact buttons in the same file. [panes/inspector.py:713](src/warlock/studio/panes/inspector.py). *Inspected.* | Draw a spinner beside it, matching the siblings. Regression: `test_trellis_log_button_shows_a_spinner_while_busy`. |
| ~~**shell-13 — Settings: greyed reasons**~~ *(built)* | Five controls (Import a LoRA, two Train entries, the model Delete and Install buttons) grey silently under the same busy gate that sibling controls in the identical file explain. [panes/app_settings.py:1351](src/warlock/studio/panes/app_settings.py). *Inspected.* | Add the matching `reason=` to each. Regression: `test_lora_and_model_row_buttons_grey_with_a_reason_when_busy`. |
| ~~**shell-14 — Quit: review tasks**~~ *(built)* | `_quit_summary` names in-flight downloads, exports and pack installs but checks no `review-` prefixed key, so quitting mid-sweep-launch (twenty to forty job creations) warns about nothing. [main.py:3559](src/warlock/studio/main.py). *Inspected.* Same shape as H02's `pack:` gap. | Add a `review-` line. Regression: `test_quit_summary_names_a_sweep_launch_or_delete_in_flight`. |
| ~~**create-06 — Create: status wording**~~ *(built)* | `asset_exits._status_reason` hand-writes a second mapping of the four job-status sentences that `validation.STATUS_SENTENCES`/`not_done_message` already own, worded differently. [asset_exits.py:126](src/warlock/studio/asset_exits.py). *Inspected.* Its sibling `create_stages.available()` states the rule this breaks in its own docstring — the tooltip and the toast must be one sentence, not two paraphrases. | Build from the service's own table. Regression: `test_asset_exits_status_reason_reuses_the_services_status_sentences`. |
| ~~**create-07 — Create: seed field**~~ *(built)* | The comment explaining why Seed draws no error ring says nothing in `service` raises a refusal naming it; `check_seed` raises `Invalid(field="seed")`. [panes/settings_2d.py:2105](src/warlock/studio/panes/settings_2d.py), [service/validation.py:297](src/warlock/service/validation.py). *Inspected.* What keeps it unreachable is that imgui's `InputInt` stores an int32 whose range happens to coincide with `MAX_SEED` — incidental, and unpinned; a value from a hand-edited settings file still refuses with no control ringed. | Correct the comment and either ring the field or clamp at form load. Regression: `test_a_malformed_seed_from_a_settings_file_still_rings_the_seed_control`. |
| ~~**inker-11 — Flourish: preset popup**~~ *(built)* | The Insert-effect popup calls `presets.names()` (a directory glob) and `presets.load()` (read, parse and clamp) every frame it is open, with no memo key. [panes/inker_flourish.py:65](src/warlock/studio/panes/inker_flourish.py). *Reproduced:* 5 frames, 5 calls each. Harmless at 29 presets; unbounded work tied to draw frequency. | Cache per popup-open or on directory mtime. Regression: `test_the_insert_popup_does_not_reread_the_preset_directory_every_frame`. |
| ~~**clay-09 — Clay: bevel comment**~~ *(built)* | `MAX_BEVELED_CORNERS`' comment states "about 1.6us per corner"; the measured rate is per *face* (~0.4 µs per corner). [clay/ops_bevel.py:263](src/warlock/studio/clay/ops_bevel.py). *Reproduced.* The ceiling is safe either way, but this is the figure a future recalibration would reuse, understating cost 4×. | Restate the unit. |
| ~~**poser-06 — Clips: locked file**~~ *(built)* | `service.clips.revert()`'s `unlink()` is not wrapped in `except OSError`, unlike its sibling delete doors, so a locked file raises a raw `PermissionError` out of the service layer. [service/clips.py:343](src/warlock/service/clips.py). *Reproduced.* | Wrap and raise `Failed`, matching `delete_pose`. Regression: `test_reverting_a_locked_clip_file_reports_as_a_failure_not_a_traceback`. |
| ~~**poser-07 — Poser: clip reasons**~~ *(built)* | Both clip-editor buttons say "The skeleton preview is still loading." even when the real cause is a build or asset failure, and one states it as an inline literal with no testable function. [panes/poser_clips.py:57](src/warlock/studio/panes/poser_clips.py). *Inspected.* Clips refresh independently of the preview, so a failure leaves both claiming "still loading" indefinitely. | Thread `state.error`/`asset_error` in and give both a pure function. Regression: `test_update_key_reason_names_a_build_failure_not_still_loading`. |
| ~~**troupe-05 — Troupe: layout spec**~~ *(built)* | `spec._parse` validates frame counts, durations and name collisions but never that `columns`, `render_size` or any `sizes` entry is positive, so a structurally valid `layout.json` crashes with a bare `ZeroDivisionError` in a derived property. [studio/troupe/spec.py:161](src/warlock/studio/troupe/spec.py). *Reproduced.* | Refuse by name in `_parse`, as the frame/duration checks do. Regression: `test_a_layout_with_zero_columns_or_a_zero_size_rung_is_refused_by_name`. |
| ~~**troupe-06 — Rigging: sheet listing**~~ *(built)* | `list_sheets` guards a sheet's PNG with `.exists()` rather than `.is_file()`, though the identical checks beside it were deliberately moved to `is_file()` by the 2026-09-07/08 audits. [rigging.py:1341](src/warlock/rigging.py). *Inspected.* | Use `is_file()`. Regression: `test_list_sheets_skips_a_sidecar_whose_png_name_is_a_directory`. |
| ~~**muse-04 — Muse: WAV round-trip**~~ *(built)* | `read_track` re-quantises soundfile's `/32768` normalisation by `×32767`, so a full-scale sample comes back 1 LSB off — contradicting `_wav`'s docstring claim that "the pair is exact". [muse_io.py:79](src/warlock/studio/muse_io.py). *Reproduced.* `sirens/wavout.py`'s `read_wav` was hand-written to avoid this exact mismatch and says so. Inaudible today; the false exactness claim is the defect. | Re-scale by 32768, or decode raw PCM as `wavout` does; failing that, correct the docstring. Regression: `test_read_track_round_trips_a_full_scale_int16_sample_exactly`. |
| ~~**muse-05 — Muse: stems reason**~~ *(built)* | The Stems button's "already split" refusal is an inline ternary passed straight to `ghost_button` — the fifth literal, missed when muse-04/muse-07 pulled every other one into `_ready_reason`. [panes/muse_results.py:163](src/warlock/studio/panes/muse_results.py). *Inspected.* | Extract `_stems_reason` beside `_ready_reason`. Regression: `test_the_stems_already_split_reason_is_a_pure_testable_function`. |
| ~~**muse-06 — Muse: per-frame set**~~ *(built)* | `sync` rebuilds a set of every job id in the cache every frame purely to test one membership, with no memo key. [muse_mode.py:386](src/warlock/studio/muse_mode.py). *Inspected.* Cost grows with total library size, not Muse's rows. | Short-circuit the scan or key on a cache generation. Regression: `test_sync_does_not_allocate_a_set_of_every_job_every_frame`. |
| ~~**muse-07 — Muse: instrumental**~~ *(built)* | Manual 35 says choosing Instrumental "greys the field and sends nothing, whatever it still shows from before", implying the lyrics stay visible; `_set_instrumental` erases them immediately, which its own test confirms is intended. [docs/manual/35-muse.md:30](docs/manual/35-muse.md), [muse_brief.py:318](src/warlock/studio/muse_brief.py). *Inspected.* | Use the brief table's own accurate phrasing. Regression: `test_the_instrumental_manual_sentence_matches_the_clear_on_toggle_behaviour`. |
| ~~**muse-08 — Follow-ups: separation**~~ *(built)* | `followups.PRODUCTS` lists seven kinds and omits `separate`, though `separate_job` carries `source_job` and writes into that job's directory exactly like the other seven. [followups.py:36](src/warlock/followups.py). *Inspected.* A finished separation gets the generic toast — the inconsistency already fixed once for `remesh` after the count went stale. | Add `separate` and update the docstring's count. Regression: `test_followups_products_names_every_source_job_carrying_kind_including_separate`. |
| ~~**service-04 — Jobs: source id**~~ *(built)* | `_maybe_queue_sheet_after_rig` builds a path from `params["source_job"]` with only a truthiness check — the one `source_job` site in the file that skips the `is_valid_id()` its three siblings perform and justify in comment. [_q_jobs.py:415](src/warlock/_q_jobs.py). *Inspected.* `job_dir()` has no containment check, so a malformed value becomes a filesystem probe and is written into a new job's params. | Validate before it becomes a path. Regression: `test_maybe_queue_sheet_after_rig_refuses_a_malformed_source_job_before_it_becomes_a_path`. |
| ~~**service-05 — Promote: field**~~ *(built)* | Of three refusals guarding a prepared cutout, only the mismatched-job one carries no `field`; its two siblings two and four lines later both carry `field="reference"`. [service/_jobs_resubmit.py:636](src/warlock/service/_jobs_resubmit.py). *Inspected.* | Add `field="reference"`. Regression: `test_promote_to_models_mismatched_cutout_refusal_carries_the_same_field_as_its_siblings`. |
| ~~**service-06 — Troupe: more fieldless refusals**~~ *(built)* | `send_to_troupe`'s two copies of the no-mesh and Blender refusals, and `_charsheet_spec`'s missing-clip `KeyError` branch, carry no `field`, though the sibling `has_clips` check three lines up was deliberately given `field="template"`. [service/troupe.py:627](src/warlock/service/troupe.py). *Inspected.* `has_clips` only checks the library is non-empty, so a template missing the one clip a layout asks for reaches the unguarded branch. | Land with troupe-01: give all three a field. Regression: `test_a_missing_specific_clip_on_an_otherwise_clip_bearing_template_is_refused_with_a_field`. |
| ~~**pipelines-04 — Scripts: literal count**~~ *(built)* | `author_humanoid.py`'s summary hardcodes "the same twelve characters" where its three sibling scripts use the computed `count`. [scripts/author_humanoid.py:78](scripts/author_humanoid.py). *Inspected.* Correct only by coincidence. | Use `count`. Regression: `test_author_humanoid_report_uses_the_computed_species_count_not_a_literal`. |
| ~~**pipelines-05 — Workers: unreadable spec**~~ *(built)* | `fetch_worker`, `pack_worker` and `update_worker` parse their spec off stdin with no guard, so an unreadable one crashes with a raw traceback instead of the sentence-and-exit-code shape `blender_worker`, `lora_train_worker` and `separation_worker` all implement — two of them citing "blender_worker's rule" by name. [pipelines/fetch_worker.py:644](src/warlock/pipelines/fetch_worker.py). *Reproduced.* | Wrap the initial parse in the same try/except. Regression: `test_fetch_pack_and_update_worker_report_an_unreadable_spec_as_a_sentence_not_a_traceback`. |
| ~~**pipelines-06 — Weights: presence**~~ *(built)* | `matting.available()` and three weight gates in Text2Image use `exists()` rather than `is_file()`. [pipelines/matting.py:109](src/warlock/pipelines/matting.py), [pipelines/text2image.py:277](src/warlock/pipelines/text2image.py). *Reproduced:* a directory named `config.json` makes `available()` return True. These gates exist to give an actionable "not found" before the heavy load path. | Use `is_file()`, mirroring doctor's own fix. Regression: `test_weight_presence_checks_reject_a_directory_where_a_file_belongs`. |
| ~~**pipelines-07 — Prompt ceiling comment**~~ *(built)* | `MAX_PROMPT`'s docstring justifies the 1000-character refusal by saying the encoder "truncates far earlier anyway"; `prompt.chunk()` removed that truncation one day before the comment was written, and INVARIANTS now states the SDXL prompt is chunk-encoded, not truncated. [service/validation.py:64](src/warlock/service/validation.py). *Inspected.* | Restate the real reason for the ceiling. Regression: `test_max_prompt_docstring_does_not_claim_truncation_the_encoder_no_longer_does`. |
| ~~**pipelines-08 — Remesh: undocumented budgets**~~ *(built)* | `FACE_PROFILES` and `FACES_MIN`/`FACES_MAX` are live in the remesh panel's combo with no `docs/measurements/` document, while the sibling `optimize.PROFILES` tiers are deliberately kept out of the generate form until a qualification run backs them. [pipelines/remesh.py:29](src/warlock/pipelines/remesh.py). *Evidence gap.* | Backfill a document, or state the exemption. Regression: `test_remesh_face_profiles_are_backed_by_a_measurement_document`. |
| ~~**pipelines-09 — Remesh: undocumented constants**~~ *(built)* | `VOXEL_FRACTION` and `BAKE_MARGIN_PX` carry measured-sounding justifications with no document behind them, unlike `material.DEFAULT_STRENGTH` and `meshreport.WELD_TOLERANCE`, which explicitly disclaim measurement. [pipelines/remesh.py:43](src/warlock/pipelines/remesh.py). *Evidence gap.* A reader cannot tell measured from asserted-by-feel. | Backfill, or disclaim as the siblings do. Regression: `test_remesh_voxel_fraction_and_bake_margin_are_backed_by_a_measurement_document`. |
| ~~**pipelines-10 — Mesh report: thresholds**~~ *(built)* | `TRIANGLE_BUDGET`, `GROUND_TOLERANCE` and `SIZE_TOLERANCE` decide a mesh's ready/review classification with no document, unlike `HOLE_WARN` in the same function and unlike `WELD_TOLERANCE` below them, which says so deliberately. [meshreport.py:31](src/warlock/meshreport.py). *Evidence gap.* | Backfill, or disclaim. Regression: `test_meshreport_triangle_budget_and_tolerances_are_backed_by_a_measurement_document`. |
| ~~**docs-08 — Tileset test: stale rationale**~~ *(built)* | `test_tileset_gpu.py`'s module docstring still frames `SEAM_MAX` as owing a re-measurement, language predating three re-measurements and the 2026-08-30 replacement of the deciding statistic (which `seam.py`'s own comment did get). Separately, `test_the_reduced_material_still_tiles` still asserts the discredited ratio on the exact flat-cell population it false-alarms on, while its sibling was migrated. [tests/test_tileset_gpu.py:41](tests/test_tileset_gpu.py). *Inspected.* | Update the docstring; migrate the test to `report["seamless"]`. Regression: `test_reduced_material_seamlessness_is_judged_by_dominance_not_the_retired_ratio`. |
| ~~**docs-09 — Loader ceiling: no document**~~ *(built)* | `MAX_TOTAL_BYTES` is named in the checkup's own section-D example list but has no `docs/measurements/` document — only an in-code comment with an embedded ad hoc measurement. [viewer/gltf.py:72](src/warlock/studio/viewer/gltf.py). *Evidence gap.* Weaker than docs-01: this is a safety ceiling derived by arithmetic, and its in-code reasoning is thorough. | Backfill, or state once that safety ceilings are out of scope so the example list stops implying otherwise. |
| ~~**tour-01 — Tour: stale count**~~ *(built)* | `_tour_offer`'s docstring says "all four tours"; `TOURS` has held five since muse-basics was added. The code itself derives correctly from `TOURS`. [panes/landing.py:572](src/warlock/studio/panes/landing.py). *Inspected.* The identical drift was found and fixed in `scripts.py` on 2026-09-07 and gated — but the gate checks only that file, so the sibling copy went uncaught. | Derive the count or drop it; widen the guard. Regression: `test_tour_offer_docstring_does_not_hardcode_a_stale_tour_count`. |
| ~~**tour-02 — Tour: untested veil**~~ *(built)* | `_veil`'s band-decomposition arithmetic has no test, though it is separable from the two imgui calls beside it and is the part most likely to hide an off-by-one that double-paints or leaves a gap. [panes/tour.py:328](src/warlock/studio/panes/tour.py). *Inspected.* `_hole` and `_card_pos` were extracted and pinned for exactly this reason by the 2026-09-08 audit; `test_tour_geometry.py`'s docstring excuses `_veil` by reasoning about its imgui calls rather than the pure arithmetic inside. | Factor out `_veil_spans` and pin it beside the other two. Regression: `test_veil_spans_cover_every_hole_and_nothing_else`. |

## Human-only — owed, not open

Findings that need a card, a clean machine, real Aseprite or Tiled, or ears. These move to
`TODO.md` in the fix phase and are not counted as open here.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| ~~**service-H1 — `pixelklein` revision unverified**~~ *(moved to `TODO.md` P44)* | Every other `Fetch` revision in `models.py` is stated in its own comment to have been read off downloaded bytes; `Limbicnation/pixel-art-lora`'s is explicitly flagged as "unconfirmed against local bytes — the SHA comes from the hub's `refs/main`", and that caveat is not carried into `docs/MODELS.md`, which presents it alongside every verified one. Re-verify against the hub and either confirm the pin or correct it; until then, note the caveat in the doc. [models.py:1317](src/warlock/models.py), [docs/MODELS.md:203](docs/MODELS.md) | A networked machine, to re-fetch and re-hash that revision — exactly what the code comment already asks for. |

## Coverage

- **Slices and segments run:** all fourteen slices, all 53 segments, one explorer each, every
  one returned a record set and a coverage note. `shell`: boot, documents, chrome, widgets,
  home-library, review-settings. `create`: brief, workspace, viewer, panes. `inker`: document,
  paint, sheets, codecs, flourish, mode, panes. `clay`: mesh, document, mode. `poser`: rig,
  poses, mode. `troupe`: engine, jobs, render. `plotter`: map, tiles, mode. `packwright`:
  packer, mode. `muse`: engine, mode, jobs. `sirens`: engine, playback, panes. `service`:
  queue, kinds, assets, gates. `pipelines`: children, image, mesh, install. `docs`: A-B, C1,
  C2, D, E, F1, F2. `tour`: tour-1.
  **Segments skipped:** `pipelines`/`workspace` — not launched, as the table directs under a
  scope of `all`: its files are owned and were read by the workspace rows (`inker`/sheets,
  `troupe`/render, `poser`/rig and poses, `plotter`/tiles, `muse`/jobs). That is the table's
  own instruction, not an omission.
- **Explorers that failed or timed out:** none.
- **Files a segment owned but did not open, and why** — carried forward as the owed list for
  the next run:
  - `plotter`/`mode` — `plotter_tilesets.py` lines ~477–963 (the Inker round-trip and
    library-asset-to-tileset doors) and **over half of `panes/plotter_layers.py`** (the layer
    table and the whole property-editor machinery, ~1000 of 1752 lines). Newly reported; this
    segment closed its 2026-09-08 debt in full and found these while doing it.
  - `inker`/`mode` — `inker_mode.py`, `inker_ops.py` and `inker_state.py` (2.5–3.3 k lines
    each) were sampled, not read end to end; the segment found nothing in what it read and
    said so rather than manufacturing findings.
  - `inker`/`document` — `_doc_paint.py`, `_doc_indexed.py`, `_doc_anim.py`, `_doc_flourish.py`,
    `_doc_sheet.py`, `_doc_slices.py` and most of `document.py`/`transform.py` sampled rather
    than read; the segment prioritised the lead-in invariants and still found a Critical.
    `_doc_flourish.py`'s async regenerate path is named as the most likely place a
    stale-completion defect remains unlooked-at.
  - `plotter`/`map` — 11 of its 20 files grepped rather than read (`terrain.py`, `render.py`,
    `tools.py`, `tsx.py`, `props.py`, `scene.py`, `layer_rows.py` and four partials); budget
    went to confirming plotter-02 from the engine side and to the `tmx.py`/`wmap.py` ceilings.
  - `create`/`viewer` — 8 of 19 files (`camera.py`, `gizmo.py`, `glctx.py`, `grid.py`,
    `markers.py`, `math3d.py`, `programs.py`, `sheet.py`, `bonelines.py`) covered only by a
    targeted frame-thread grep, not a full checklist walk.
  - `shell`/`boot` — `state.py` (1688 lines) sampled; `shell`/`documents` — `layout.py` (1072)
    and most of `skeletons.py`/`jobs_cache.py` covered by targeted greps.
  - `clay`/`document` — `primitives.py` (2169) and `presets.py` (789) sampled rather than read.
  - `troupe`/`render` — `pipelines/spritesynth.py` read at function-inventory level only.
  - `service`/`gates` — `downloads.py`'s `_download` transaction body and progress-parsing tail
    skimmed against the verified pattern in `packs.py`/`updates.py`.
  - `pipelines`/`install` — `scripts/exercise_mode.py` and `screenshot_modes.py` grep-scanned
    only (2 of 26; the other 24 read, closing the 2026-09-08 debt).
  - `pipelines`/`children` — `blender_worker.py`'s ~2,100-line `bpy` body not read line by line
    (it spawns no children and imports nothing network-capable).
- **Owed from the 2026-09-08 run — all six closed this run**, and each said so explicitly:
  `plotter`/`mode` (`plotter_tileset.py` read whole, `plotter_tools.py` whole, the canvas and
  editor tabs read); `pipelines`/`install` (**all ten `native/*.c` kernels** read in full — no
  ceiling defect found in any of them — and 24 of 26 `scripts/` files read rather than
  grepped); `service`/`gates` (**all 28 `Fetch` records** diffed against `docs/MODELS.md` in
  both directions — one asymmetry, service-H1); `docs`/`E` (**all 36 `service/` files plus
  `queue.py`** read for docstring accuracy, 3,268 lines of extracted docstrings, ~18 claims
  spot-verified — nothing drifted); `inker`/`sheets` (manual 15 and 29 read and checked
  promise by promise); `create`/`panes` (manual 12, 22 and 23 read and checked).
- **Checklist items not evaluable in this scope:** for the headless engine packages
  (`studio/inker/`, `clay/`, `plotter/`, `tilegrid/`, `packwright/`, `sirens/`, `muse/`,
  `troupe/`) and the pure pipeline modules, classes 1–4, 6, 13 and 17–18 have no purchase —
  no frame loop, no greyed controls, no task adoption, no journal provider — and the
  explorers said so per segment rather than claiming a clean walk.
- **Deviation recorded:** the 18-item defect-class checklist and each slice's row were
  delivered to explorers **by absolute path with a required read-first step and a mandatory
  coverage-note enumeration of which classes were walked**, rather than pasted inline as
  `brief-explorer.md` specifies; 53 inlined copies would not fit the launch messages. The
  constraints block was pasted verbatim in every brief. Every returned coverage note did
  enumerate its classes, so the substitution is checkable.
- **Noted for the skill itself, not the codebase:** `defect-classes.md` item 15 says "nothing
  that could open a socket besides `fetch_worker` and `pack_worker`"; `update_worker` is a
  documented third exception (INVARIANTS names three), so the reference text undercounts.
  Also, the "54 measurement documents at 2026-09-06" figure in `subsystems.md` is now 62, and
  `docs-checkup.md`'s "239 bold lead-in paragraphs" counts 375 by the same method today.
- **Not done by this audit:** no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not close
  those.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and the
test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is deleted
rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it. Nothing under
`src/` or `scripts/` may cite this filename.
