# Warlock Studio audit — 2026-09-12

**Status: closed, except one.** All seventeen findings were built and struck on
2026-09-12, in release 0.0.45. One new finding (docs-18) was opened during the fix phase
and is the only thing keeping this file alive.

**Recommendation:** close docs-18, then delete this file.

**17 findings: 0 Critical, 5 High, 3 Medium, 9 Low; 0 human-only — all struck.
Plus docs-18, opened during the fix phase and still open.**

Audited: version **0.0.44**, working tree at **`5b6d15a6`**. Dirty at start: `clean`.
Scope: `docs` → slice `docs`, split into `7` segments and one explorer each
(`docs`: `A-B, C1, C2, D, E, F1, F2`), plus one make-up segment `F3` the orchestrator
launched mid-run (see Coverage). `uv run ruff check .`: `All checks passed!`.
The suite was not run by this audit; findings are from reading and from the offline
probes below.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong
result the user will hit, or a promise in the docs the code breaks. *Medium*: a defect
with a workaround, or a hard-invariant violation not yet reachable. *Low*: hardening,
consistency, naming. *Reproduced*: an executable example confirmed it (scripts under the
session scratchpad's `audit-2026-09-12/probes`, run with `uv run --no-sync python
<script>`). *Inspected*: follows from the current code path. *Evidence gap*: release
proof is missing from the repository, not a demonstrated failure.

**Second-look.** Every High below was re-read by the orchestrator against the working
tree: `_apply_drag` was read in full and confirmed never to consult `state.snap`;
`snap_ground` was grepped across `src/` and found only at its own declaration and its own
toggle; both `define_prefab_from_selection` call sites were opened and confirmed to pass
no name and to discard the `""` refusal; `update_worker.py:52` was confirmed to fetch
`api.github.com` while `README.md:198` says "those two are the only code in the project
that reaches the network". Dropped or downgraded during merge: `none`. Merged from two
segments that saw one defect from two sides: `none`.

## Critical

None.

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **docs-01 — Mason: Snap** | ~~Chapter 17 tells the reader to turn Snap on with grid (m) at 1 and drag a box, promising "it lands on whole metres", but Mason's gizmo-drag path never reads `state.snap` or `state.snap_translate`.~~ [mason_view.py:1157](src/warlock/studio/mason_view.py) (`_apply_drag`), [mason_view.py:1128](src/warlock/studio/mason_view.py) (`_drag_gizmo`), [17-dressing-a-scene.md:81](docs/manual/17-dressing-a-scene.md). *Reproduced*: a 2.37 m drag with Snap on and a 1 m grid lands at exactly 2.37 m. Only the placement click snaps ([mason_view.py:819](src/warlock/studio/mason_view.py)); Clay's equivalent drag does consult it ([_view_drag.py:881](src/warlock/studio/_view_drag.py)). A user believes the scene is grid-aligned because both the manual and the toggle say so, and ships mis-aligned geometry. **Built 2026-09-12:** `_apply_drag` now round-trips its parent-space translation through the existing `parent_world`/`inverse` pair, snaps it with the same `mason.ops.snap_translation`/`snap_rotation` the placement click already called, and converts back, so the grid is measured in world space however deep the node sits under a group. Regression `test_dragging_a_node_with_the_move_gizmo_snaps_translation_to_the_grid_when_snap_is_on` failed pre-fix at `translation.x = 2.37` and passes at 2.0. INVARIANTS paragraph added. | Apply `mason.ops.snap_translation` inside `_apply_drag` when `state.snap` is on, mirroring Clay — or say in the chapter that Snap governs placement clicks and the array/align tools only. Regression: `test_dragging_a_node_with_the_move_gizmo_snaps_translation_to_the_grid_when_snap_is_on`. |
| **docs-02 — Mason: Drop to ground** | ~~`state.snap_ground` is written by its own toggle and read nowhere else in the tree, while chapter 17 says turning it on makes a dragged object land on the ground rather than float or sink.~~ [mason_state.py:183](src/warlock/studio/mason_state.py), [panes/mason_tools.py:131](src/warlock/studio/panes/mason_tools.py) — the only two sites that name it — and [17-dressing-a-scene.md:82](docs/manual/17-dressing-a-scene.md). *Reproduced*: a node dragged from 5 m up with the toggle on keeps its height. The manual promises an automatic behaviour for a control that is inert during the exact gesture it is described for, so props float with no feedback. **Built 2026-09-12:** `snap_ground` is now read during a drag -- a second pass after the write, `mops.drop_to_ground` over the node's own post-drag world box, the arithmetic the one-shot button already used. Regression `test_dragging_a_node_with_drop_to_ground_enabled_keeps_it_on_the_terrain_surface` failed pre-fix at `lo.y = 4.5` and passes at 0.0. | Wire `snap_ground` into `_apply_drag` (project each dragged node onto the terrain the way the one-shot **Drop selection to ground** already does via `mops.drop_to_ground`) — or remove the toggle and let the chapter describe only the one-shot button. Regression: `test_dragging_a_node_with_drop_to_ground_enabled_keeps_it_on_the_terrain_surface`. |
| **docs-03 — Mason: Make prefab** | ~~Chapter 17 says "Choose Make prefab and give it a name", but neither context-menu call site passes a name, there is no name-entry dialog, and a duplicate name is refused silently.~~ [17-dressing-a-scene.md:117](docs/manual/17-dressing-a-scene.md), [panes/mason_menu.py:60](src/warlock/studio/panes/mason_menu.py), [panes/mason_outliner.py:225](src/warlock/studio/panes/mason_outliner.py), [mason_mode.py:419](src/warlock/studio/mason_mode.py). *Inspected*: `define_prefab_from_selection` defaults to the node's own name, swallows `doc.define_prefab`'s `ValueError` and returns `""`, and both call sites discard the return — so a colliding name makes the gesture do nothing at all, with no toast. There is no rename control in [panes/mason_prefabs.py](src/warlock/studio/panes/mason_prefabs.py) either (Place / trash / Unpack only). **Built 2026-09-12:** `prompt_define_prefab_from_selection` is now the one door onto the gesture -- the house `dialogs.Prompt`, seeded with the node's name -- and `define_prefab`'s `ValueError` is toasted as "Could not make a prefab: ..." rather than swallowed. Regressions `test_make_prefab_from_the_context_menu_lets_the_user_name_the_new_template` and `test_naming_a_prefab_after_one_it_already_places_is_refused_with_a_toast`, both failing pre-fix. INVARIANTS paragraph added. | Add a name prompt to the gesture and surface the duplicate-name refusal — or correct the chapter to say the template takes the promoted node's name, and say how to change it. Regression: `test_make_prefab_from_the_context_menu_lets_the_user_name_the_new_template`. |
| **docs-04 — Offline: CLAUDE.md** | ~~CLAUDE.md's Offline bullet says "The single exception is the user-initiated `fetch_worker` subprocess", but `pack_worker.py` and `update_worker.py` are two more sanctioned, user-initiated subprocesses that reach the network.~~ [CLAUDE.md:47](CLAUDE.md), [pipelines/update_worker.py:52](src/warlock/pipelines/update_worker.py) (`https://api.github.com/...`), [pipelines/pack_worker.py](src/warlock/pipelines/pack_worker.py). *Reproduced*. `SECURITY.md:30-34` already names all three, so the tree disagrees with itself. This is the paragraph a contributor or security reviewer reads first for the app's core guarantee, and it undercounts the network surface by two of three. **Built 2026-09-12:** the bullet names all three subprocesses. `tests/test_docs_inventories.py::test_claude_md_offline_bullet_names_every_network_worker` derives the set from the `pipelines/` modules importing the shared `download` helper, so a fourth fails the test; it reported `missing: ['pack_worker', 'update_worker']` pre-fix. | Name all three, matching `SECURITY.md`. Regression: `test_claude_md_offline_bullet_names_every_network_worker`. |
| **docs-05 — Offline: README** | ~~README says of `fetch_worker` and `pack_worker` that "those two are the only code in the project that reaches the network", which omits `update_worker`.~~ [README.md:196](README.md), [pipelines/update_worker.py:52](src/warlock/pipelines/update_worker.py), [service/updates.py](src/warlock/service/updates.py). *Reproduced*. A reader relying on this sentence for what touches the network gets a wrong answer about the one security property the project markets. **Built 2026-09-12:** the passage says three and names `update_worker`. `test_readme_setup_section_names_every_network_worker` uses the same derived set and reported `missing: ['update_worker']` pre-fix. INVARIANTS paragraph added covering both. | Say "three" and add `update_worker` (release-feed check, installer download). Regression: `test_readme_setup_section_names_every_network_worker`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **docs-06 — Mason: light range** | ~~Chapter 17 says a selected light's Properties panel shows "colour, intensity, and range", but `_light_block` draws colour and intensity only (plus the two cone angles for a Spot).~~ [17-dressing-a-scene.md:136](docs/manual/17-dressing-a-scene.md), [panes/mason_props.py:179](src/warlock/studio/panes/mason_props.py), [mason/nodes.py:267](src/warlock/studio/mason/nodes.py) — `LightNode.range` exists and reaches the glTF export. *Inspected*. The field exists in the document and exports, but no UI sets it. **Built 2026-09-12:** `_light_block` draws a range field for point and spot lights, and deliberately not for directional. Three regressions under `tests/mason/test_light_range_control.py`, the first failing pre-fix with a control census showing only colour and intensity. | Add a `range` input to `_light_block`, or drop "and range" from the chapter. Regression: `test_the_light_properties_panel_exposes_a_range_control_or_the_manual_is_corrected`. |
| **docs-07 — CONTRIBUTING: headless packages** | ~~CONTRIBUTING's list of headless editor packages names four (`inker/`, `clay/`, `plotter/`, `packwright/`) and omits `mason/`, `sirens/`, `troupe/` and `muse/`, which CLAUDE.md and `tests/_pure_packages.py` both include.~~ [CONTRIBUTING.md:50](CONTRIBUTING.md), [tests/_pure_packages.py](tests/_pure_packages.py), [CLAUDE.md](CLAUDE.md). *Inspected*. The constraint itself is enforced in code, so nothing is violatable; but a contributor reading the file whose stated purpose is onboarding can believe the import-purity rule does not cover four of the eight packages. **Built 2026-09-12:** the list names all eight and points at the derivation. `test_contributing_headless_package_list_matches_pure_packages` derives from `tests/_pure_packages.py` narrowed to packages that are also modes, and reported `missing: ['mason', 'muse', 'sirens', 'troupe']` pre-fix. | List all eight, or replace the hand list with a pointer to `tests/_pure_packages.py`'s derived set — the move `mason/`'s own pin already made. Regression: `test_contributing_headless_package_list_matches_pure_packages`. |
| **docs-08 — MODELS.md: the GGUF command** | ~~`docs/MODELS.md` inlines a paste-able `hf download` command for every Fetch record it lists except the TRELLIS.2 GGUF weights, the one thing Create cannot do without.~~ [docs/MODELS.md:30](docs/MODELS.md), [models.py:703](src/warlock/models.py). *Reproduced*. `tests/test_fetch.py::test_the_docs_name_every_repository_the_registry_does` accepts the repo id appearing in README **or** MODELS.md, so README's copy already satisfies it and this gap is invisible to the gate. **Built 2026-09-12:** the GGUF command block is in MODELS.md. `test_docs_models_md_gives_a_paste_able_command_for_every_fetch_record_it_lists` walks every `Fetch` record in the registry, exempting the two sha256-pinned direct downloads with a stated reason; it named `engine:trellis_gguf` pre-fix. The old weaker gate was left in place. | Add the `ilintar/trellis2-gguf` block to MODELS.md's reconstruction-engine section, matching README and chapter 40. Regression: `test_docs_models_md_gives_a_paste_able_command_for_every_fetch_record_it_lists`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **docs-09 — INVARIANTS: Clay's Delete button** | ~~The paragraph on a greyed Clay op says Delete draws its reason through a local `clay_tools._destructive_button` "because `widgets.destructive_button` takes no such argument", but the 2026-09-08 button-vocabulary pass gave the shared widget a `reason=`/`tooltip=` contract and deleted the local wrapper.~~ [docs/INVARIANTS.md:480](docs/INVARIANTS.md), [widgets.py:3328](src/warlock/studio/widgets.py). *Inspected*. A reader trusting the doc over the tree could reintroduce the wrapper that pass removed. Guarded in code by `tests/test_clay_tools_panels.py` and `tests/test_button_vocabulary.py`; the stale paragraph is not. **Built 2026-09-12:** the paragraph now describes the shared `widgets.destructive_button(..., reason=...)` and records that the 2026-09-08 pass reversed the argument rather than the rule. | Rewrite the sentence for the shared widget and drop the dead citation. Regression: `test_invariants_does_not_cite_the_deleted_clay_tools_destructive_button_wrapper`. |
| **docs-10 — `_deform_qa` docstring** | ~~The docstring says a cancel is "Checked at the top and again after the render", but the body holds exactly one `self._cancel.event.is_set()` check, before the render.~~ [_q_rig.py:212](src/warlock/_q_rig.py). *Inspected*. Harmless today only because the enclosing `_rig` stage has already committed the cancel token — which the docstring does not say, and which a refactor could change. **Built 2026-09-12:** the second check was added rather than the docstring retreated -- between the render returning and the `os.replace` that publishes, mirroring `_rig`. Regression `test_deform_qa_docstrings_second_cancel_check_that_does_not_exist` failed pre-fix with `deform_qa` present in params after a mid-render cancel. INVARIANTS paragraph added. | Add the second check, or correct the docstring to what the function does and why that is safe. Regression: `test_deform_qa_docstrings_second_cancel_check_that_does_not_exist`. |
| **docs-11 — TODO P35's citations** | ~~P35 cites three line numbers for the Steps-guidance sentences it says disagree; two are off by 18 and 13 lines.~~ [TODO.md](TODO.md), [16-generating-a-soundtrack.md:60](docs/manual/16-generating-a-soundtrack.md), [36-muse.md:75](docs/manual/36-muse.md), [panes/muse_recipe.py:81](src/warlock/studio/panes/muse_recipe.py). *Inspected*. The disagreement is still real; its evidence trail is not. **Built 2026-09-12:** the three line numbers are gone and the sentences are named instead, with a note saying why. | Drop the line numbers and name the sentences, as most of the file's citations already do. Regression: `test_p35_line_citations_still_point_at_the_quoted_sentences`. |
| **docs-12 — the tutorial chain stops at 14** | ~~Chapter 13 says "One tutorial left" and 14 says "That is the last tutorial", but 15, 16 and 17 are tutorials the index lists in the same walkthrough; 15 loops back to 13 and 16 never points at 17.~~ [13-putting-it-in-a-game.md:123](docs/manual/13-putting-it-in-a-game.md), [14-making-a-soundtrack.md:191](docs/manual/14-making-a-soundtrack.md), [00-index.md:28](docs/manual/00-index.md). *Inspected*. A reader following the manual's own stated method stops at 14 and never reaches the Flourish, Muse or Mason tutorials. **Built 2026-09-12:** the chain runs unbroken to 17. `test_tutorial_next_links_form_an_unbroken_chain_through_the_last_tutorial` derives the order from the index's own tutorial list and reported 14 to 15, 15 to 16 and 16 to 17 all missing pre-fix. | Repair the forward links through 17. Regression: `test_tutorial_next_links_form_an_unbroken_chain_through_the_last_tutorial`. |
| **docs-13 — the index undercounts the downloads** | ~~The index's blurb for chapter 1 says "the two downloads" while the chapter's own table now lists three, the engine having stopped shipping in the installer on 2026-09-10.~~ [00-index.md:15](docs/manual/00-index.md), [01-before-you-begin.md:90](docs/manual/01-before-you-begin.md). *Inspected*. **Built 2026-09-12:** the count is gone from the blurb; the gate compares against the chapter's own table rather than against the number three. | Drop the count. Regression: `test_index_blurb_download_count_matches_the_chapter_it_summarizes`. |
| **docs-14 — a duplicated word** | ~~"also holds your the sidebar's internal split".~~ [42-app-settings.md:355](docs/manual/42-app-settings.md). *Inspected*. **Built 2026-09-12:** the stray word is gone, under a possessive-then-article check verified to produce no false positives across the whole manual. | Delete the stray "your". Regression: `test_app_settings_chapter_has_no_duplicate_leftover_words`. |
| **docs-15 — the axis-view keys name two modes of three** | ~~The overview says Ctrl+1/3/5/7 work "in Clay and in Poser alike"; Mason calls the same `axis_view_key` and behaves identically, unmentioned.~~ [20-overview.md:172](docs/manual/20-overview.md), [mason_mode.py:1132](src/warlock/studio/mason_mode.py), [_view_frame.py:72](src/warlock/studio/_view_frame.py). *Inspected*. `tests/test_ux_consistency_pass2.py::test_the_axis_view_keys_are_one_function_both_viewports_call` predates Mason and does not cover it either. **Built 2026-09-12:** the sentence names Mason. The gate scans `studio/*_mode.py` for `axis_view_key` call sites and maps them through `modes.MODES`, so a fourth viewport fails it. | Add ", and in Mason". Regression: `test_overview_axis_view_sentence_names_every_mode_that_shares_axis_view_key`. |
| **docs-16 — the Home setup line is quoted short** | ~~The chapter quotes "Generation is not set up yet — N downloads"; the built string also appends ", about X GB".~~ [21-home.md:85](docs/manual/21-home.md), [panes/landing.py:409](src/warlock/studio/panes/landing.py). *Inspected*. **Built 2026-09-12:** the quote carries the size clause, built from `_setup_status`'s f-string via `ast` rather than pasted. | Extend the quote. Regression: `test_home_setup_line_quote_matches_landing_setup_status_format`. |
| **docs-17 — mojibake in the CHANGELOG preamble** | ~~Three em-dashes in the hand-written preamble are double-encoded, against 581 correct ones in the rest of the file.~~ [CHANGELOG.md:5](CHANGELOG.md). *Reproduced*. Cosmetic only — `changelog.py`'s parser never reads the preamble. **Built 2026-09-12:** the three double-encoded em-dashes were re-saved; the file now holds 584 correct ones and no mojibake. | Re-save the three lines. Regression: `test_changelog_preamble_has_no_mojibake`. |

## Opened during the fix phase

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **docs-18 — `_sheet` never checks its cancel** | **`_sheet` in `_q_rig.py` contains no `self._cancel.event.is_set()` check anywhere in its two hundred lines; it only calls `self._cancel.commit()` at the very end, so a cancel arriving at any point during the sheet render does the whole job anyway.** [_q_rig.py:335](src/warlock/_q_rig.py) (the stage), [_q_rig.py:542](src/warlock/_q_rig.py) (the lone commit). *Inspected*: a scan of the module shows `_rig` checking once and committing once, `_deform_qa` now checking twice, and `_sheet` checking zero times. Found by the docs-10 fixer while closing that finding, and verified by the orchestrator. Nothing is corrupted and nothing is published early — `commit()` is correctly placed — but a user who cancels a sheet waits out the entire Blender render regardless, and `_deform_qa`'s docstring claim that "every sibling stage checks the same way" is still not true of this one. | Add a check at each point work remains avoidable, the shape docs-10 established: before the render and immediately before the publish. Regression: `test_cancelling_a_sheet_stops_before_the_render_it_has_not_started`. |

## Human-only — owed, not open

None. No finding in this slice needs a card, a clean machine, real Aseprite or Tiled, or ears.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | — | — |

## Coverage

- Slices and segments run: `docs`: `A-B`, `C1`, `C2`, `D`, `E`, `F1`, `F2` — all seven of
  the skill's own segments launched and all seven returned — **plus `F3`**, a make-up
  segment. Slices or segments skipped: none.
- **The segment table left two chapters unowned.** `F1` owns manual chapters 00–16 and
  34–45 and `F2` owns 20–33, which covers neither `17-dressing-a-scene.md` (Mason's
  tutorial, added by `b26d950d`) nor `46-extending.md`. `F1` reported the gap in its own
  coverage note rather than quietly reading past its boundary, and the orchestrator
  launched `F3` over exactly those two chapters. Three of this audit's five Highs came
  from that make-up segment. **The split in
  `.claude/skills/warlock-audit/references/subsystems.md` needs the `docs` row's `F1`/`F2`
  ranges widened to 00–19 and 34–46 before the next run**; the same skill's check-up sheet
  also still calls chapter 38 the shortcut sheet, which the renumbering made 39.
- Explorers that failed or timed out: none.
- Files a segment owned but did not open, and why:
  - `C2` read all ~130 lead-ins in its range but grepped symbols paragraph-by-paragraph for
    only about 30 of them (weighted to the newest Mason, Clay-ceiling and Aseprite
    paragraphs); the other ~100 were read for internal consistency and dead names but not
    individually verified against `src/`.
  - `D` opened 14 of the 63 `docs/measurements/*.md` documents end to end (the corpus has
    grown from the table's 54) and surveyed the rest by title and by grep for supersession
    language; it verified ~15 of `docs/COMPAT.md`'s ~45 Aseprite rows in both directions,
    not all of them.
  - `F2` read chapters 28 and 29 (Inker, ~2150 lines together) in full but sampled rather
    than verified every sentence against the pane source; it names a second pass over those
    two as the highest-value next increment.
  - `F3` checked chapter 46's named identifiers and mechanisms rather than every sentence
    of its architecture prose.
- **Two gaps owed from the 2026-09-08 run are now closed.** `D` diffed every one of the ~29
  `Fetch` records in `models.py` — repo id, revision, size and file pattern — against
  `docs/MODELS.md` and found one omission (docs-08) and nothing else; both non-Hub sha256
  pins match. `E` opened and read all 36 files under `src/warlock/service/` plus `queue.py`
  in full for docstring accuracy rather than grepping them, found nothing inside that scope,
  and turned up the one defect of that exact shape just outside it (docs-10).
- Checklist items not evaluable in this scope: none. Section A's rows were confirmed gated
  and are not reported; `E` judged all 40 `TODO.md` items and found none buildable-and-unbuilt.
- Opened during the fix phase and not closed: docs-18, above. It is the same defect class
  as docs-10 in a sibling stage of the same module; it was left open rather than folded
  into that fixer's change because widening a fixer past its record is how a fix pass stops
  being auditable.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not
  close those.

## What closing the seventeen cost

Five file-disjoint fixers and the orchestrator. Every finding carries a regression test
that was shown to fail against the unfixed code first. Four new test modules
(`tests/test_docs_inventories.py`, `tests/manual/test_manual_prose_drift_2026_09_12.py`,
`tests/mason/test_prefab_naming.py`, `tests/mason/test_light_range_control.py`) and
additions to three existing ones. Five paragraphs were added to `docs/INVARIANTS.md`.
The gate ran green at 0.0.45: `ruff` clean, **21,179 passed, 52 skipped**, `preflight
--fast` passed. One failure surfaced on the first suite run — the docs-03 toast forwarded
a bare exception, which `tests/test_ux_todo_fixes.py` refuses as a house rule — and went
back to the fixer that owned the file rather than being patched from the orchestrator seat.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and
the test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is
deleted rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it.
Nothing under `src/` or `scripts/` may cite this filename.

---

# Warlock Studio audit — 2026-09-12 (second run, scope `clay`)

**Recommendation:** close the two Highs — the glTF accessor bound (clay-01) and the
forked Open tab (clay-02) — before the next installer build; clay-02 can lose a user's
work with no warning.

**Status: closed.** All five findings were built and struck on 2026-09-12; nothing
from this run is left open. The `docs` run above still holds docs-18.

**5 findings: 0 Critical, 2 High, 2 Medium, 1 Low; 0 human-only — all struck.**

Audited: version **0.0.45**, working tree at **`f33745b1`**. Dirty at start:
` M .gitignore` and untracked `training/`. Scope: `clay` → slice `clay`, split into `3`
segments and one explorer each (`clay`: `mesh`, `document`, `mode`).
`uv run ruff check .`: `All checks passed!`. The suite was not run by this audit;
findings are from reading and from the offline probes below.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong
result the user will hit, or a promise in the docs the code breaks. *Medium*: a defect
with a workaround, or a hard-invariant violation not yet reachable. *Low*: hardening,
consistency, naming. *Reproduced*: an executable example confirmed it (scripts under
`<scratchpad>/audit-2026-09-12/probes/`, run with `uv run --no-sync python <script>`).
*Inspected*: follows from the current code path. *Evidence gap*: release proof is
missing from the repository, not a demonstrated failure.

**Second-look.** Both Highs were re-read by the orchestrator against the working tree and
their probes re-run: `clay-01` reproduced all three of its cases (negative index loads
silently, positive out-of-range and a bad `bufferView` raise bare `IndexError`), and
`clay-02` reproduced two tabs on one path. Dropped or downgraded during merge: none.
Merged from two segments that saw one defect from two sides: none — the three segments
found five disjoint defects, and `clay-04` is the only record that crosses a slice
boundary (into Mason).

## Critical

None.

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-01 — glTF loader: accessor bounds** | ~~A primitive attribute's accessor index and an accessor's own `bufferView` are only type-checked.~~ **Built 2026-09-12:** `_Reader._accessor_entry` is now the single place an accessor index is read off the JSON, carrying both the type and the range check; an accessor's own `bufferView` gets the same treatment, which makes the comment in the image branch true at last. Four tests in `tests/test_gltf_loader.py`, all proven failing against HEAD first, including the skin `inverseBindMatrices` path: `test_a_negative_position_accessor_index_is_refused_rather_than_silently_wrapping_onto_another_accessor` and its three siblings. INVARIANTS gained a paragraph beside the loader ceilings. | Apply `_check_int_index` plus `0 <= x < len(...)` in `accessor()`/`decoded()` before indexing `accessors`, and the same to `acc["bufferView"]` in `_decode_accessor`, in the sibling boundaries' message shape. Regression: `test_a_negative_or_out_of_range_accessor_index_is_refused_rather_than_wrapping_or_raising_index_error`. |
| **clay-02 — Clay: File > Open forks a second tab** | ~~`ask_open` has no `find_path` check, so the picker forks a second tab onto an open path.~~ **Built 2026-09-12:** the check landed in `on_task_done`'s `clay-open` branch, where both open routes converge, so it closes the picker *and* the narrower race where one path is opened twice before the first decode returns; the existing tab's unsaved edits are untouched and the freshly decoded document is discarded rather than half-adopted. `tests/test_clay_mode.py::test_opening_the_same_file_twice_through_the_file_picker_focuses_the_existing_tab`, proven failing first. INVARIANTS gained a paragraph. | Check `state.find_path` in the `clay-open` branch of `on_task_done` (both routes land there) and activate the existing tab instead of adopting. Regression: `test_opening_the_same_file_twice_through_the_file_picker_focuses_the_existing_tab`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-03 — Clay import: non-dict JSON entry** | ~~`_declared_budget` calls `.get` on an accessor or mesh entry without checking it is a JSON object.~~ **Built 2026-09-12:** both lookups are `isinstance`-guarded, treating a non-object entry the way an out-of-range index is already treated, so the file falls through to `gltf.load`'s own named refusal instead of an uncaught `AttributeError`. `tests/clay/test_glbimport.py::test_declared_budget_does_not_crash_on_a_non_dict_accessor_or_mesh_entry`, proven failing first. No INVARIANTS paragraph: a hardening inside an existing rule. | Guard both lookups with `isinstance(..., dict)`, returning 0 / skipping as the existing type and range checks already do. Regression: `test_declared_budget_does_not_crash_on_a_non_dict_accessor_or_mesh_entry`. |
| **clay-04 — Clay mesh: raw-normals cache is unlocked on a premise that is false** | ~~`mesh._RAW_CACHE` is mutated with no lock, on a premise that is false.~~ **Built 2026-09-12:** the fixer verified every link of the chain (`to_primitives` → `render_arrays` → `_stash`; `_resolve_primitive` off-thread; `docmodes.start_save` → `TaskRunner.submit` onto a `warlock-task` thread) and closed it with a `threading.Lock` inside `mesh.py` alone, rather than a keyword three unowned files would have had to pass. The false comment is rewritten to name the real second caller. `tests/clay/test_incremental_normals.py::test_stashing_raw_face_normals_from_two_threads_never_corrupts_or_crashes_the_cache` asserts the serialisation mechanism rather than hammering threads, proven failing against a scratch copy of HEAD. INVARIANTS gained a paragraph. | Either guard the read/clear/write with a `threading.Lock`, or have the off-thread resolve skip `_stash` — a one-shot resolve never benefits from the drag-reuse optimisation. Regression: `test_stashing_raw_face_normals_from_two_threads_never_corrupts_or_crashes_the_cache`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **clay-05 — Clay properties: the last material greys with no reason** | ~~The explanatory line for a disabled Remove is gated on the same condition that disables the button.~~ **Built 2026-09-12:** the reason is extracted into the pure `_palette_remove_reason(material_count, users)`, which now drives both the button's disabled gate and the muted line, so the decision is testable though the pane is not drawable headlessly. `tests/clay/test_clay_props_widget.py::test_removing_the_last_palette_material_greys_out_with_a_stated_reason`. Manual chapter 07 now states the floor of one material. | Show a muted line such as "the last material can't be removed" when `len(doc.materials) <= 1`. Regression: `test_removing_the_last_palette_material_greys_out_with_a_stated_reason`. |

## Human-only — owed, not open

None.

## Coverage

- Slices and segments run: `clay`: `mesh`, `document`, `mode` — all three launched, all
  three returned. Slices or segments skipped: none. `clay` was not among the segments
  owed from the 2026-09-08 run.
- Explorers that failed or timed out: none.
- Files a segment owned but did not open, and why:
  - `mesh`: none — all thirteen files read cover to cover (5,542 lines).
  - `document`: `presets.py` (789 lines) was not opened directly — its behaviour was read
    from the Clay paragraphs of `docs/INVARIANTS.md` plus a grep for layering and bare
    excepts; `primitives.py` (2,209 lines) was read in excerpt (`clamp_params`, the UV
    helpers, `tube`, and the `GENERATORS`/`OPEN_GENERATORS`/`CONCAVE_GENERATORS`/
    `CATEGORIES` registry tail) rather than end to end, so the individual generators'
    numeric edge cases are unwalked. The other ten files were read in full.
  - `mode`: roughly the last 270 lines of `_view_overlay.py`'s draw-list construction,
    part of `_view_drag.py`'s marquee-commit and keyboard-restart internals, and the
    individual op bodies in `clay_ops.py` past line ~1220 (bevel, inset, loop-cut,
    dissolve, weld, fill-hole) were not read; the registry layer, `run`/`run_mesh_op`/
    `run_object_op` and the whole `OPS` list were. All eighteen files were opened.
- Checklist items not evaluable in this scope: item 5 (docs-vs-code) was not walked as a
  line-by-line diff of manual chapters 07 and 30 against each control — the `mode`
  explorer judged from in-code audit citations instead, so those two chapters are an
  unmeasured gap. Items 9 (staged writes), 15 (subprocess/network) and 17 (cancel) have
  no surface inside this slice: Clay's writes go out through `atomic`, it spawns no
  child, and it has no cancellable long-running job. Items 1 (undo), 4 (greyed) and 18
  (recovery) were evaluable only from the `mode` and `document` segments; the `mesh`
  segment's files are pure `Mesh -> Mesh` functions with no history or I/O in scope.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not
  close those.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and
the test.` When nothing is left in either run, this file is deleted rather than ticked.
Nothing under `src/` or `scripts/` may cite this filename.
