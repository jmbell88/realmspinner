# Warlock Studio audit — 2026-09-14

**Recommendation:** close the two Criticals (create-01, poser-01) and the Familiar VRAM gap
(service-01) before the next installer build, then the rest of the Highs — most are small, local
fixes with a named regression test.

**97 findings: 2 Critical, 20 High, 45 Medium, 30 Low; 0 human-only.**

Audited: version **0.0.46**, working tree at **`6a2ffa53`** (master). Dirty at start:
`M src/warlock/studio/agent_clay.py`, `M tests/test_agent_clay.py`,
`M training/clay-assistant/tests/test_scaffold.py`, `?? docs/examples/` — all the user's, none
reported as a defect. Scope: `all` → slices `shell, create, inker, clay, mason, poser, troupe,
plotter, packwright, muse, sirens, service, pipelines, docs, tour, agents`, split into **59
segments and one explorer each** (`shell`: boot, documents, chrome, widgets, home-library,
review-settings; `create`: brief, workspace, viewer, panes; `inker`: document, paint, sheets,
codecs, flourish, mode, panes; `clay`: mesh, document, mode; `mason`: engine, mode; `poser`: rig,
poses, mode; `troupe`: engine, jobs, render; `plotter`: map, tiles, mode; `packwright`: packer,
mode; `muse`: engine, mode, jobs; `sirens`: engine, playback, panes; `service`: queue, kinds,
assets, gates; `pipelines`: children, image, mesh, install; `docs`: A-B, C1, C2, D, E, F1, F2;
`tour`: 1; `agents`: protocol, host, clay, character). `uv run ruff check .`: all checks passed.
The suite was not run by this audit; findings are from reading and from the offline probes below.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong result the
user will hit, or a promise in the docs the code breaks. *Medium*: a defect with a workaround, or
a hard-invariant violation not yet reachable. *Low*: hardening, consistency, naming.
*Reproduced*: an executable example confirmed it (scripts under
`%TEMP%\claude\D--Projects-warlock\029c7817-…\scratchpad\audit-2026-09-14\probes\`, run with
`uv run --no-sync python <script>`; named per finding below). *Inspected*: follows from the
current code path. *Evidence gap*: release proof is missing from the repository, not a
demonstrated failure.

**Second-look.** Every Critical and High below was re-read by the orchestrator against the
working tree. Dropped or downgraded during merge:
- *Dropped* — packwright-mode-02 (Atlas + JSON export does not check `pack_dirty`): the code
  explains in `export_library`'s comment that `export_files`' PNG and sidecar both derive from the
  landed pack and so agree with each other; by design, not a defect.
- *Critical → High* — agents-02 (bridge `RecursionError`): a crash, but of the `warlock mcp`
  child, from a pathologically nested frame only a broken client sends.
- *Critical → Medium* — agents-03 (malformed tool-result body): needs a Studio-side RPC bug first.
- *High → Medium* — mason-04 (Move hint; the Properties pane is the workaround), packwright-01
  (uncaught batch-add refusal still surfaces as a generic toast), muse-02 (the service door still
  refuses), muse-03 (a stored duration label, the audio itself is right), sirens-01 (playhead lit
  through the release tail, cosmetic), sirens-02 (only reachable at the 256-pattern ceiling),
  pipelines-02 (the chapter under-promises: the FLUX.2 LoRA does work), agents-04 (the
  task-mode `warlock_status` gap is a documented choice in `_call_task`'s docstring whose
  consequence — a listed tool answering "no such tool" — still reads wrong).
- *High → Low* — pipelines-05 (meshreport's "after welding" wording, only on the degenerate
  fallback path), docs-12 (chapter 34 omits 256 from the preset list; Custom still reaches it).

Merged from two segments that saw one defect from two sides: troupe-01 (`troupe`/jobs +
`agents`/character, which showed an agent can reach it on any library row), agents-02
(`agents`/protocol 01 + 04), inker-01 (`inker`/document; `inker`/flourish confirmed reachability),
shell-03 (`shell`/review-settings; `docs`/F1 confirmed), tour-01 (`tour`; `docs`/F2 confirmed),
packwright-02 (`packwright`/packer; `docs`/F2 confirmed), pipelines-02 (`pipelines`/image;
`docs`/F2 confirmed), mason-02 (`mason`/mode; `docs`/F2 confirmed chapter 31's wording),
inker-03 (`inker`/codecs; `docs`/D confirmed COMPAT row 348 is right and the chapter is stale).

## Critical

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-01 — Viewer: glTF material factors** | ~~**A GLB whose `baseColorFactor`/`emissiveFactor` has the wrong element count loads, then crashes the render loop** when `GpuMaterial.bind` writes it into a vec4/vec3 uniform. [gltf.py:988](../src/warlock/studio/viewer/gltf.py), [gltf.py:991](../src/warlock/studio/viewer/gltf.py), [scene.py:88](../src/warlock/studio/viewer/scene.py), [render.py:386](../src/warlock/studio/viewer/render.py). *Reproduced* (`create-viewer-01.py`). Every other numeric glTF field is shape-checked; this one reaches `self.viewer.render(...)`, which is not wrapped.~~ | **Built 2026-09-14:** `gltf._factor` shape- and number-checks both factors in `material()` and falls back to the glTF default. `tests/test_gltf_loader.py::test_a_malformed_base_color_factor_is_defaulted_rather_than_crashing_the_render_loop`. |
| **poser-01 — Poser: clip import** | ~~**`clip_import.import_into_library` commits a clip library with no `MAX_CLIP_LIBRARY_BYTES` check**, so an import can write a library the read door then refuses forever; the template silently reverts to the shipped clips and the clip editor raises on it. [clip_import.py:229-267](../src/warlock/service/clip_import.py); the sibling `clips.save` has the check at [clips.py:421-429](../src/warlock/service/clips.py). *Reproduced* (`poser-poses-01.py`, 4,852,705 bytes written, unreadable after).~~ | **Built 2026-09-14:** `import_into_library` serializes the merged library and refuses with `Conflict` past `MAX_CLIP_LIBRARY_BYTES` before committing. `tests/test_clip_import_service.py::test_import_into_library_refuses_a_clip_library_the_read_door_could_never_load_back`. |

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **service-01 — Queue: Familiar VRAM admission** | ~~**`vram.familiar_admission` is never called**; `LlamaServer.ensure_started` spawns `llama-server.exe -ngl 999` with no VRAM check, while every other model door is admitted — the 2026-08-03 overcommit crash class. [vram.py:617](../src/warlock/vram.py), [llama.py:320-387](../src/warlock/pipelines/llama.py). *Inspected* (only docstring references in `src/` and `tests/`).~~ | **Built 2026-09-14:** `LlamaServer._check_vram` calls `vram.familiar_admission(vram.live_memory())` before spawning, refusing when NVML cannot answer. `tests/test_familiar.py::test_ensure_started_refuses_to_spawn_familiar_without_vram_headroom`. |
| **pipelines-01 — Children: t2i vitals** | ~~**A cancelled or failed generate makes the parent think the SDXL checkpoint unloaded**: error replies carry no `loaded`, and `_publish` sets `self._loaded = bool(msg.get("loaded"))` on every reply before raising. [text2image_worker.py:239-257](../src/warlock/pipelines/text2image_worker.py), [t2i_client.py:453-470](../src/warlock/pipelines/t2i_client.py). *Reproduced* (`pipelines-children-01.py`). Admission then stops crediting a resident checkpoint — the miscount `music_worker` was rewritten to avoid.~~ | **Built 2026-09-14:** `_Server.handle` merges `_vitals()` into every response, cancel, failure and unknown op included. `tests/test_t2i_worker.py::test_a_cancelled_or_failed_generate_response_still_carries_the_vitals`. |
| **service-02 — Gates: pack selection** | ~~**Installing a pack whose wheels are already at the pinned version never records it in `selected.json`**, so "Restore packs" cannot offer it after an installer upgrade wipes `site-packages` — the M02 incident again. [packs.py:317-338](../src/warlock/service/packs.py). *Reproduced* (`service-gates-02.py`).~~ | **Built 2026-09-14:** The already-satisfied branch calls `_record_selected` before returning. `tests/test_service_packs.py::test_installing_an_already_satisfied_pack_is_still_recorded_as_selected`. |
| **service-03 — Kinds: double rig** | ~~**`service.rig.create_rig` never checks `rig_in_flight`**, so Library's Rig and Poser's Re-rig (different `ctx.submit` keys) can mint two rig jobs on one mesh; both finalize into one `job_dir` and the history reports two "done" rigs for one served result. [rig.py:56-78](../src/warlock/service/rig.py); only `troupe.send_to_troupe` and the agent wrapper guard. *Reproduced* (`service-kinds-01.py`).~~ | **Built 2026-09-14:** `create_rig` checks `rig_in_flight` itself and raises `Conflict(field="job_id")` before the Blender probe. `tests/test_service_rig_door.py::test_create_rig_refuses_a_second_rig_while_one_for_the_mesh_is_already_in_flight`. |
| **mason-01 — Mason: array ops** | ~~**Nothing bounds an array count before `MasonDoc.add_nodes`**; a count past `MAX_PLACED` is fully built synchronously, then every `scene.resolve` refuses for good — the viewport draws empty and the HUD says `placed: 0`. [mason_tools.py:220-240](../src/warlock/studio/panes/mason_tools.py) (`max(1, count)`, no upper bound), [document.py:200](../src/warlock/studio/mason/document.py), [scene.py:155](../src/warlock/studio/mason/scene.py). *Reproduced* (`mason-01.py`, 150,000 copies in 1.18 s, no refusal).~~ | **Built 2026-09-14:** `MasonDoc.add_nodes` refuses before attaching past `MAX_PLACED` (structural count), and the array row toasts and builds nothing. `tests/mason/test_document.py::test_add_nodes_refuses_before_building_past_max_placed_rather_than_after`. |
| **mason-02 — Mason: Align / Distribute / Drop to ground** | ~~**Each pushes one undo step per moved node**, where chapter 31 promises "Each of these lands as a single undo step". [mason_tools.py:147-203](../src/warlock/studio/panes/mason_tools.py) (`_apply_deltas`, no `mark`/`collapse_since`), [mason_menu.py:80-105](../src/warlock/studio/panes/mason_menu.py). *Reproduced* (`mason-mode-01.py`).~~ | **Built 2026-09-14:** `_apply_deltas` and the context menu's `_drop_to_ground` fold into one step with `mark`/`collapse_since`. `tests/test_mason_panes.py::test_align_on_three_nodes_undoes_in_one_step`, `::test_drop_to_ground_context_menu_row_undoes_in_one_step`. |
| **create-02 — Create: asset exits on a mesh** | ~~**A finished mesh is offered "Add to Plotter as a tileset" and "Add to Packwright as an atlas source"**: the ready branch checks only `"input.png" in files`, and a mesh row carries its reference's `input.png`. [asset_exits.py:406-448](../src/warlock/studio/asset_exits.py), [asset_exits.py:467-488](../src/warlock/studio/asset_exits.py). *Reproduced* (`create-01.py`). The test fixture's mesh omits `input.png`, so the pinned exit set passes.~~ | **Built 2026-09-14:** The ready branch also requires an image stage. The mesh fixture now carries `input.png`. `tests/test_asset_exits.py::test_a_finished_mesh_does_not_offer_plotter_or_packwright_add_doors`. |
| **create-03 — Create: Style LoRA picker** | ~~**`lora_options`/`lora_note` judge fit against the raw `form["base_model"]`**, not the resolved recipe, so under Automatic routing the picker calls a LoRA fitted while `validate_request` refuses it at Generate — the class already fixed for `negative_prompt_note` and `img2img_note`. [settings_2d.py:1652-1689](../src/warlock/studio/panes/settings_2d.py). *Reproduced* (`create-panes-01.py`).~~ | **Built 2026-09-14:** `_lora_base` resolves the recipe first for `lora_note`, `lora_options` and `lora_filter_note`. `tests/test_settings_2d_notes.py::test_lora_options_follows_the_resolved_tier_under_automatic_routing`. |
| **clay-01 — Clay: `analyze` past the pair cap** | ~~**Past `MAX_TRIANGLE_PAIRS`, `analyze()` reports `intersects=False` without testing**, so two genuinely interpenetrating 20k-face meshes read as not overlapping, indistinguishable from an honest "no" (`exact=False` covers distance only). [analyze.py:765-778](../src/warlock/studio/clay/analyze.py). *Reproduced* (`clay-mesh-03.py`, `clay-mesh-03b.py`).~~ | **Built 2026-09-14:** Past the pair cap `intersects` is `None` (unknown); `PairAnalysis` and `clay_analyze`'s output schema allow null. `tests/clay/test_analyze.py::test_a_pair_past_the_triangle_pair_cap_never_reports_a_false_no_intersection`, `tests/test_agent_clay_door_types.py::test_clay_analyze_output_schema_allows_an_unknown_intersects_past_the_pair_cap`. |
| **clay-02 — Clay: `analyze` grid stall** | ~~**One large flat triangle registers into millions of grid cells in pure Python**; none of the three `analyze` ceilings bound it — 14 triangles with an 80 m floor took 2.35 s, a 160 m floor 10.6 s. [analyze.py:422-479](../src/warlock/studio/clay/analyze.py), [analyze.py:762](../src/warlock/studio/clay/analyze.py). *Reproduced* (`clay-mesh-01.py`, `clay-mesh-02.py`).~~ | **Built 2026-09-14:** `_grid_candidates` grows the cell so no triangle registers into more than `_MAX_CELLS_PER_TRIANGLE_AXIS` (64) cells per axis (10.8 s before). `tests/clay/test_analyze.py::test_analyze_does_not_stall_on_one_large_flat_triangle`. |
| **clay-03 — Clay: edge-selection subtract** | ~~**`elements._rows_minus` builds a dense `(len(a), len(b), 2)` broadcast**, so select-all-edges on a large import then a Ctrl-drag subtract marquee allocates gigabytes on the frame thread. [elements.py:192-198](../src/warlock/studio/clay/elements.py), [_view_drag.py:715-734](../src/warlock/studio/_view_drag.py). *Inspected*.~~ | **Built 2026-09-14:** `_rows_minus` keys each row as one integer and uses `np.isin` (peak 192 MB to under 2 MB at 8,000 by 8,000). `tests/clay/test_elements.py::test_combine_subtract_of_large_edge_selections_does_not_allocate_quadratically`. |
| **inker-01 — Inker: Flourish regenerate** | ~~**`apply_flourish` grows frames before its linked-cel pre-validation can refuse**, so a refused regenerate leaves real blank frames with no undo step. [_doc_flourish.py:300-329](../src/warlock/studio/inker/_doc_flourish.py). *Inspected*; the existing guard only covers a grid that does not grow.~~ | **Built 2026-09-14:** `_flourish_ensure_frames` runs after the linked-cel validation. `tests/inker/test_flourish_document.py::test_apply_flourish_leaves_no_added_frames_when_it_refuses_a_linked_cel_after_growing_the_grid`. |
| **inker-02 — Inker: `.aseprite` group opacity** | ~~**`dropped_by_aseprite` never checks group opacity**, so a document with a non-1.0 group opacity saves as `.aseprite` with no lossy warning while the writer stores 255. [aseout.py:1385-1412](../src/warlock/studio/inker/aseout.py), [aseout.py:588-589](../src/warlock/studio/inker/aseout.py); COMPAT row 317 records the drop. *Inspected*.~~ | **Built 2026-09-14:** `dropped_by_aseprite` reports a group opacity other than 1.0. `tests/inker/test_findings_engine.py::test_dropped_by_aseprite_reports_a_non_default_group_opacity`. |
| **inker-03 — Manual 29: Aseprite reference layer** | ~~**Chapter 29 says an imported reference layer "opens hidden"**; `asein` keeps the file's own visibility bit (the override was removed; COMPAT row 348 is right). [29-inker-animation.md:478](../docs/manual/29-inker-animation.md), [asein.py:816-826](../src/warlock/studio/inker/asein.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 29 says the reference layer keeps the file's visibility, with only the flattened-export warning. `tests/test_docs_audit_2026_09_14.py::test_manual_29_reference_layer_claim_matches_asein_visibility_behaviour`. |
| **troupe-01 — Troupe: Godot scene export** | ~~**A clip name containing `/` is embedded raw in `states/{name}/node` and `states/{name}/position`**, splitting the state key away from the `transitions` entry and corrupting the `.tscn`; `_validate_clip_name` bans only quote/backslash/CR/LF and nothing upstream bans `/`. [godotscene.py:133-144](../src/warlock/godotscene.py), [godotscene.py:401-402](../src/warlock/godotscene.py), [characters.py:899-975](../src/warlock/service/characters.py). *Reproduced* (`troupe-jobs-01.py`). An agent reaches it through `character_export(format="godot_scene")` on any library row.~~ | **Built 2026-09-14:** `_validate_clip_name` also refuses every `_INVALID_NODE_NAME_CHARS` character, `/` included. `tests/test_godotscene.py::test_a_clip_name_containing_a_slash_is_refused_rather_than_corrupting_the_states_path`. |
| **muse-01 — Muse: loop-cache precompute** | ~~**A loop cache computed for one take installs on another** whose region shares `(start, end, fade)` in samples: the `CACHE_PREFIX` branch checks the key only, unlike its `LOAD_PREFIX`/`FIND_PREFIX` neighbours. Play on take B then sounds take A. [muse_io.py:132-148](../src/warlock/studio/muse_io.py), [muse_mode.py:251-264](../src/warlock/studio/muse_mode.py). *Reproduced* (`muse-01.py`).~~ | **Built 2026-09-14:** The `CACHE_PREFIX` branch also checks the key's job id against the loaded take. `tests/test_muse_mode.py::test_precompute_loop_cache_does_not_land_on_a_different_take_with_the_same_region`. |
| **agents-01 — Agents: `character_create` size** | ~~**The tool schema promises any size 8–256, "off the ladder resized nearest-neighbour"**, but the value goes straight to `Recipe.from_dict`, which refuses anything off `charsheet.SIZES`. [agent_character.py:320-351](../src/warlock/studio/agent_character.py), [agent_character.py:945-949](../src/warlock/studio/agent_character.py), [recipe.py:207-210](../src/warlock/characters/recipe.py). *Reproduced* (`agents-character-01.py`).~~ | **Built 2026-09-14:** Widened: `Recipe` accepts `LOGICAL_SIZE_RANGE` (8-256), which the render path has accepted since 8b091e98. `tests/test_field_error_wiring.py`'s provoke value moved from 17 to 300. `tests/characters/test_recipe.py::test_character_create_accepts_an_off_ladder_size_its_own_schema_calls_legal`. |
| **agents-02 — Agents: bridge parse** | ~~**A deeply nested JSON frame well under `MAX_FRAME` raises `RecursionError` out of `bridge_dispatch`**, which catches only `UnicodeDecodeError`/`JSONDecodeError`, and out of `run_bridge`, killing `warlock mcp`; `decode()`'s docstring also promises only `ValueError`. [protocol.py:793-798](../src/warlock/mcp/protocol.py), [protocol.py:74-90](../src/warlock/mcp/protocol.py), [bridge.py:525-526](../src/warlock/mcp/bridge.py). *Reproduced* (`agents-protocol-01.py`, `agents-protocol-04.py`).~~ | **Built 2026-09-14:** `decode()` turns `RecursionError` into `ValueError`, and `bridge_dispatch` answers -32700. `tests/mcp/test_protocol.py::test_bridge_dispatch_refuses_deeply_nested_json_without_crashing`. |
| **docs-01 — THIRD-PARTY-NOTICES: Familiar** | ~~**The notices say llama.cpp and Gemma 4 E2B are "shown by hand in the tables above"; neither appears in any table.** [THIRD-PARTY-NOTICES.md:144](../THIRD-PARTY-NOTICES.md); `docs/MODELS.md:84-107` documents both. *Inspected*. `tests/test_licence_claims.py` checks the registry sentence, not the tables.~~ | **Built 2026-09-14:** llama.cpp and the Gemma 4 E2B GGUF rows added to the tables, with the licences `docs/MODELS.md` records. `tests/test_docs_audit_2026_09_14.py::test_third_party_notices_names_llama_cpp_and_gemma_where_it_claims_to`. |
| **docs-02 — Manual 17: prefab instances** | ~~**"An instance holds nothing of its own but a position"**: a `PrefabNode` carries its own full transform, and Make Prefab copies translation, rotation and scale onto it. [17-dressing-a-scene.md:123-125](../docs/manual/17-dressing-a-scene.md), [nodes.py:301-312](../src/warlock/studio/mason/nodes.py), [mason_mode.py:469-472](../src/warlock/studio/mason_mode.py). *Inspected*. A reader will unpack an instance just to rotate it.~~ | **Built 2026-09-14:** Chapter 17 says an instance carries its own transform (position, rotation, scale) and nothing else. `tests/test_docs_audit_2026_09_14.py::test_manual_says_a_prefab_instance_carries_its_own_transform_not_only_position`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **shell-01 — ConfirmQueue Enter** | ~~Enter always cancels a confirm, so pressing Enter to commit Prune's "Keep the newest" count closes the dialog as cancelled and prunes nothing, silently. [dialogs.py:337-430](../src/warlock/studio/dialogs.py), [library.py:2464-2493](../src/warlock/studio/panes/library.py). *Inspected*.~~ | **Built 2026-09-14:** `dialogs._confirm_cancelled_by_key` decides; `ConfirmQueue.draw` samples `imgui.is_any_item_active()` before the body draws, so Enter while a body field has focus no longer cancels, and Escape still does. `tests/test_shell_audit_2026_09_14.py::test_pressing_enter_after_editing_the_prune_keep_count_does_not_silently_cancel_it`. |
| **shell-02 — Library export popup** | ~~The browse task writes `popup.dest` and `popup.plan` as two unlocked assignments the frame thread reads each frame, so a new destination can draw beside the old plan. [library.py:1960-1969](../src/warlock/studio/panes/library.py), [library.py:2021-2054](../src/warlock/studio/panes/library.py). *Inspected*.~~ | **Built 2026-09-14:** `dest` and `plan` are one `_DestPlan` tuple published in a single assignment, and read back through properties. `tests/test_shell_audit_2026_09_14.py::test_browse_never_shows_a_destination_paired_with_the_previous_plan`. |
| **shell-03 — Manual 42 / Settings: agent help text** | ~~"…an agent already running on this computer connects to *it*" reads as connecting to Familiar, the opposite of the architecture; same wording in chapter 46. [42-app-settings.md:356-358](../docs/manual/42-app-settings.md), [app_settings.py:673-679](../src/warlock/studio/panes/app_settings.py), [46-extending.md:272-273](../docs/manual/46-extending.md). *Inspected*.~~ | **Built 2026-09-14:** Chapter 42, chapter 46 and the Settings help text now make Warlock, never Familiar, the thing an agent connects to. `tests/test_docs_audit_2026_09_14.py::test_agent_settings_help_text_never_implies_agents_reach_familiar`. |
| **shell-04 — `status_bar.draw`** | ~~INVARIANTS, `bottom_pane.py`'s docstring and `test_editor_shell.py` all say it is deleted; it still exists, unreferenced, with its own width-reservation logic. [status_bar.py:175-245](../src/warlock/studio/status_bar.py), [bottom_pane.py:4-6](../src/warlock/studio/panes/bottom_pane.py). *Inspected*.~~ | **Built 2026-09-14:** `status_bar.draw` and `STATUS_H` deleted. `tests/test_shell_audit_2026_09_14.py::test_status_bar_module_has_no_draw_function_once_the_menu_bar_owns_status`. |
| **mason-03 — Assets palette arming** | ~~Arming a primitive, light or camera never clears `state.place_prefab`, so the next click places the old prefab while the palette highlights the new item. [mason_palette.py:130-167](../src/warlock/studio/panes/mason_palette.py), [mason_palette.py:253-263](../src/warlock/studio/panes/mason_palette.py), [mason_mode.py:229-268](../src/warlock/studio/mason_mode.py). *Inspected*.~~ | **Built 2026-09-14:** `mason_palette._arm_kind` clears `place_prefab` when a primitive, light or camera is armed. `tests/test_mason_panes.py::test_arming_a_primitive_after_a_prefab_disarms_the_prefab`. |
| **mason-04 — Move hint** | ~~The Move hint says "type a number, or X/Y/Z to lock an axis"; Mason has no typed-value or axis-lock path. [mason_hud.py:53](../src/warlock/studio/panes/mason_hud.py). *Inspected*.~~ | **Built 2026-09-14:** The hint now reads "Drag to move the selection"; chapter 31 never promised typed entry. `tests/test_mason_panes.py::test_the_move_hint_does_not_promise_typed_entry_the_mode_lacks`. |
| **mason-05 — `scene_stats` per frame** | ~~Two readouts call `scene.resolve` directly every frame, bypassing `MasonView.resolved`'s memo — triple the resolve cost at the warn threshold. [mason_mode.py:933-950](../src/warlock/studio/mason_mode.py), [mason_bridge.py:69-83](../src/warlock/studio/panes/mason_bridge.py), [mason_hud.py:61-88](../src/warlock/studio/panes/mason_hud.py). *Inspected*.~~ | **Built 2026-09-14:** `scene_stats` memoizes per `(doc, rev)` in a `WeakKeyDictionary`. `tests/test_mason_mode.py::test_scene_stats_resolves_once_per_revision`. |
| **create-04 — All-failed candidate group** | ~~When every mesh candidate errors, no Keep gate ever opens, the group has no dismiss, and `Filters.matches` hides its rows forever. [candidates.py:39-47](../src/warlock/studio/candidates.py), [generation_workspace.py:350-361](../src/warlock/studio/generation_workspace.py), [candidates_panel.py:184-199](../src/warlock/studio/panes/candidates_panel.py), [state.py:518-524](../src/warlock/studio/state.py). *Inspected*.~~ | **Built 2026-09-14:** `Group.all_failed`; both pickers offer **Discard all**, which settles the group through `keep_candidate` and then asks before deleting; caption and chapter 23 updated. `tests/test_candidates.py::test_a_candidate_group_whose_every_member_fails_can_be_dismissed`. |
| **create-05 — Direction strip on adopt** | ~~`Viewer.adopt_model` releases the `GpuModel` an in-flight direction strip holds without `cancel_sheet_strip()`, so the strip finishes blank. [viewer_embed.py:139-160](../src/warlock/studio/viewer_embed.py), [viewer_embed.py:456-479](../src/warlock/studio/viewer_embed.py). *Inspected*.~~ | **Built 2026-09-14:** `adopt_model` calls `cancel_sheet_strip()` first. `tests/test_clear_viewport.py::test_adopting_a_different_model_mid_strip_cancels_the_strip_instead_of_finishing_it_blank`. |
| **clay-04 — `analyze` triangulates before refusing** | ~~`MAX_ANALYZE_TRIANGLES` is checked after every object is triangulated, so a refused call still pays the ear-clip cost. [analyze.py:872-891](../src/warlock/studio/clay/analyze.py). *Inspected*.~~ | **Built 2026-09-14:** `analyze` counts post-triangulation faces from loops and refuses before triangulating. `tests/clay/test_analyze.py::test_analyze_refuses_before_triangulating_past_max_analyze_triangles`. |
| **clay-05 — Bevel ceiling with UVs** | ~~`MAX_BEVELED_CORNERS` was tuned without UVs; a UV-bearing mesh at the ceiling took 1.62 s against 0.81 s. [ops_bevel.py:297-333](../src/warlock/studio/clay/ops_bevel.py), [ops_bevel.py:439-486](../src/warlock/studio/clay/ops_bevel.py). *Reproduced* (`clay-mesh-04.py`…`07.py`).~~ | **Built 2026-09-14:** `_refuse_size` divides the ceiling by `_UV_CEILING_FACTOR` (2) when the mesh has UVs; INVARIANTS paragraph added. `tests/clay/test_ops_bevel.py::test_bevel_edges_refuses_a_uv_mesh_past_the_uv_corner_ceiling`. |
| **clay-06 — `.wblk` materials and textures** | ~~`read_wblk` bounds objects but not the `materials` or `textures` arrays, so a compact hand-edited or recovered file can stall the load. [serialize.py:658-742](../src/warlock/studio/clay/serialize.py), [serialize.py:441-466](../src/warlock/studio/clay/serialize.py). *Inspected*.~~ | **Built 2026-09-14:** `MAX_DECLARED_MATERIALS`/`MAX_DECLARED_TEXTURES` (100,000) checked before both loops. `tests/clay/test_serialize.py::test_read_wblk_refuses_a_document_declaring_too_many_materials` and its textures twin. |
| **inker-04 — `hue_saturation` masked divide** | ~~The saturation divide still uses `np.divide(..., out=, where=)`, the masked-lane pattern this codebase removed elsewhere after intermittent `errstate` failures. [filters.py:153](../src/warlock/studio/inker/filters.py). *Inspected*.~~ | **Built 2026-09-14:** Safe `np.where` denominator; this numpy does not raise, so the test pins the pattern by AST as well as under `errstate`. `tests/inker/test_filters.py::test_hue_saturation_does_not_raise_under_errstate_on_pure_black_or_white_pixels`. |
| **inker-05 — Flourish restyle on the frame thread** | ~~`submit_restyle` composites and PNG-encodes every anchor before submitting, ~51 ms per anchor at 1024², 100–300 ms per press. [inker_flourish.py:862-866](../src/warlock/studio/inker_flourish.py). *Reproduced* (`inker-flourish-01.py`).~~ | **Built 2026-09-14:** Anchor planes are captured on the frame thread and PNG-encoded inside the task. `tests/inker/test_flourish_restyle.py::test_submit_restyle_does_not_encode_png_on_the_frame_thread`. |
| **inker-06 — Use selection as texture** | ~~Pushes its own `FlourishEdit` separate from the render's step, and on a layer without a texture slot commits an invisible asset as a permanent undo step. [inker_flourish.py:449-461](../src/warlock/studio/inker_flourish.py), [_doc_flourish.py:466-487](../src/warlock/studio/inker/_doc_flourish.py). *Inspected*.~~ | **Built 2026-09-14:** A `NO_TEXTURE_SLOT` predicate greys the control and `texture_from_selection` refuses before any asset lands. On a compatible layer the asset is still its own undo step, separate from the render's; that fold was not attempted. `tests/inker/test_flourish_texture.py::test_texture_from_selection_on_an_incompatible_layer_leaves_no_undo_step`. |
| **inker-07 — Mirror-to double toast** | ~~An empty source cel toasts the refusal and then "The mirror already matches outside the face." [inker_sheet.py:522-529](../src/warlock/studio/inker_sheet.py), [inker_sheet.py:306-314](../src/warlock/studio/inker_sheet.py). *Inspected*.~~ | **Built 2026-09-14:** `_framed` returns a falsy `_REFUSED` sentinel on a raised refusal, and `mirror_to` and its sibling `mirror_run` stop before their own toast (the other callers return `bool(done)`). `tests/test_inker_audit_2026_09_14.py::test_mirror_to_does_not_follow_a_refusal_toast_with_an_already_matches_toast`, `::test_mirror_run_does_not_follow_a_refusal_toast_with_an_already_matches_toast`. |
| **poser-02 — Limb and template parse** | ~~`_parse_limb_preset`/`_parse_template` skip `_check_acyclic` and assume parent-before-child order, so a malformed preset raises an uncaught `KeyError` on the frame thread or reaches Blender unchecked. [rigging.py:1469-1500](../src/warlock/rigging.py), [rigging.py:1412-1431](../src/warlock/rigging.py), [rigging.py:176-213](../src/warlock/rigging.py). *Inspected*.~~ | **Built 2026-09-14:** `_parse_template` and `_parse_limb_preset` call `_check_acyclic`, and a limb preset listing a child before its parent is refused; INVARIANTS paragraph added. `tests/test_rigging.py::test_a_limb_preset_with_a_disconnected_parent_cycle_is_skipped_not_fatal` (+ out-of-order and template twins). |
| **poser-03 — Library during a skeleton edit** | ~~New pose and the three Apply buttons stay live during a skeleton edit and repose the mesh the editor assumes is at rest. [poser_library.py:64-291](../src/warlock/studio/panes/poser_library.py), [poser_mode.py:884-902](../src/warlock/studio/poser_mode.py). *Inspected*.~~ | **Built 2026-09-14:** `_refuse_while_skeleton_editing` refuses in `new_pose`, `apply_pose`, `apply_asset_pose` and `apply_preset`, and the library greys the four buttons with a reason. `tests/test_poser_mode.py::test_apply_pose_refuses_while_editing_the_skeleton`. |
| **troupe-02 — Effect parameter zero** | ~~`effect_params.get("rise", 0.35) or 0.35` replaces an explicit `0.0` with the default (also `rate`). [effects.py:180](../src/warlock/characters/effects.py), [effects.py:222](../src/warlock/characters/effects.py). *Inspected*.~~ | **Built 2026-09-14:** Plain `.get(key, default)` for `rise` and `rate`. `tests/characters/test_effects.py::test_an_explicit_zero_rise_is_not_replaced_by_the_default`. |
| **plotter-01 — Tile object gid field** | ~~`input_int("##obj-gid", …)` has neither `commit=True` nor `fold_undo`, so typing a tile id pushes one step per keystroke. [plotter_layers.py:1214-1244](../src/warlock/studio/panes/plotter_layers.py). *Inspected*.~~ | **Built 2026-09-14:** `commit=True`, as the sibling fields. `tests/plotter/test_properties.py::test_editing_a_tile_objects_gid_field_folds_into_one_undo_step`. |
| **plotter-02 — Seam erase untested** | ~~`_erase_seam`, a mid-job file replace with its own cancel check, has no behavioural test; only the door flag is covered. [_q_tileset.py:77-131](../src/warlock/_q_tileset.py). *Evidence gap*.~~ | **Built 2026-09-14:** Evidence gap closed with no defect: two worker-level tests call `_erase_seam` with a fake generator and pin the staged `os.replace` redraw and a cancel mid-pass leaving the file untouched. Both pass on HEAD by design. `tests/test_plotter_audit_2026_09_14.py::test_seam_erase_actually_redraws_the_material_and_a_cancel_mid_pass_leaves_it_untouched`, `::test_a_cancel_mid_erase_pass_leaves_the_material_file_untouched`. |
| **packwright-01 — Batch add past a ceiling** | ~~`_add_sprites` does not catch the ceilings' `ValueError`, so a multi-file add surfaces a generic "did not finish landing" toast and leaves the landed sprites without `pack_dirty`. [packwright_mode.py:456-471](../src/warlock/studio/packwright_mode.py), [document.py:312-365](../src/warlock/studio/packwright/document.py). *Inspected*.~~ | **Built 2026-09-14:** `_add_sprites` catches each add's ceiling `ValueError`, keeps what landed with `pack_dirty` set, and toasts how many made it in. `tests/test_packwright_mode.py::test_a_batch_add_that_trips_the_document_ceiling_refuses_instead_of_raising`. |
| **packwright-02 — Manual 33 ceilings** | ~~Chapter 33 lists three refusal ceilings but not `MAX_DOCUMENT_PIXELS`, which also fires on ordinary adds. [33-packwright.md:20-25](../docs/manual/33-packwright.md), [wpack.py:60-77](../src/warlock/studio/packwright/wpack.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 33's "Starting an atlas" names the document-wide pixel ceiling (about 67 million). `tests/test_docs_audit_2026_09_14.py::test_manual_packwright_chapter_names_the_document_wide_pixel_ceiling`. |
| **muse-02 — Missing-weights gate** | ~~Only Generate's draw call checks `model_gate.missing`; Ctrl+Enter and Sirens' Compose in Muse submit and are refused at the door with a `music_model` toast. [muse_mode.py:744-903](../src/warlock/studio/muse_mode.py), [muse_brief.py:534-537](../src/warlock/studio/muse_brief.py), [sirens_bridge.py:155-167](../src/warlock/studio/panes/sirens_bridge.py). *Reproduced* (`muse-mode-01.py`).~~ | **Built 2026-09-14:** `generate()` and `compose_from_sirens()` check `model_gate.missing` themselves. `tests/test_muse_mode.py::test_ctrl_enter_generate_is_blocked_when_the_music_model_is_missing`, `::test_compose_from_sirens_is_blocked_when_the_music_model_is_missing`. |
| **muse-03 — audio2audio duration** | ~~`actual_duration` echoes the requested duration; for an audio2audio take the sampler uses the reference's length, so the card can be off by minutes. [_q_music.py:317-322](../src/warlock/_q_music.py), [pipeline_ace_step.py:936-941](../src/warlock/pipelines/acestep/pipeline_ace_step.py). *Inspected*.~~ | **Built 2026-09-14:** `_wav_duration_seconds` reads `track.wav`'s header with stdlib `wave`; INVARIANTS paragraph added. `tests/test_q_music_tasks.py::test_audio2audio_actual_duration_matches_the_rendered_file_not_the_request`. |
| **muse-04 — Extend sliders** | ~~Bounded by the 240 s sampler ceiling, but the door refuses anything longer than the take itself. [muse_results.py:410-420](../src/warlock/studio/panes/muse_results.py), [_jobs_music.py:510-529](../src/warlock/service/_jobs_music.py). *Reproduced* (`muse-mode-02.py`).~~ | **Built 2026-09-14:** `_max_extend_for(parent_duration)` bounds both sliders. `tests/test_muse_panes_smoke.py::test_extend_sliders_are_bounded_by_the_takes_own_duration_when_shorter_than_the_sampler_ceiling`. |
| **sirens-01 — Playhead in the release tail** | ~~`Sounding.mark_at` has no upper bound, so the final row stays lit through the decay tail; its own docstring says `None` after the last row. [sirens_state.py:114-135](../src/warlock/studio/sirens_state.py), [synth.py:424-507](../src/warlock/studio/sirens/synth.py). *Reproduced* (`sirens-01.py`).~~ | **Built 2026-09-14:** `mark_at` returns `None` past the last row's own duration for a one-shot buffer. `tests/test_findings_sirens.py::test_the_playhead_goes_dark_during_the_release_tail_after_the_last_row`. |
| **sirens-02 — `add_oneshot` at the pattern ceiling** | ~~`add_pattern` raises between `mark()` and `collapse_since()`, leaking an open gesture that disables undo eviction for the session. [document.py:793-808](../src/warlock/studio/sirens/document.py), [undo.py:229-285](../src/warlock/studio/undo.py). *Reproduced* (the explorer's `sirens-01.py`, later overwritten by a sibling's probe; re-run needed).~~ | **Built 2026-09-14:** `add_oneshot` closes the gesture in `finally` (re-derived: 148 history entries against a cap of 64 before). `tests/sirens/test_document.py::test_add_oneshot_at_the_pattern_ceiling_does_not_leak_an_open_gesture`. |
| **sirens-03 — `_apply_sample` alias** | ~~No `.copy()`, unlike every other `_apply_*`, so after undo/redo the live sample is the undo stack's own array. [document.py:859-863](../src/warlock/studio/sirens/document.py). *Reproduced* (`sirens-engine-02.py`).~~ | **Built 2026-09-14:** `_apply_sample` copies. `tests/sirens/test_document.py::test_undo_of_a_sample_edit_does_not_alias_the_undo_stacks_own_array`. |
| **sirens-04 — Add to the order reason** | ~~Checks the effect before `editable`, so while saving with the caret on an effect the tooltip names the wrong fix. [sirens_orders.py:53-109](../src/warlock/studio/panes/sirens_orders.py). *Inspected*.~~ | **Built 2026-09-14:** `add_to_order_reason` checks `editable` first. `tests/test_findings_sirens.py::test_add_to_order_reason_names_busy_even_when_the_caret_is_on_an_effect`. |
| **service-04 — Reroll on a stem split** | ~~`rerollable()` offers Reroll on a finished `separate` job, which `rerun_job` always refuses. [_jobs_resubmit.py:60-132](../src/warlock/service/_jobs_resubmit.py), [library.py:1088-1093](../src/warlock/studio/panes/library.py). *Reproduced* (`service-kinds-02.py`).~~ | **Built 2026-09-14:** `rerollable()` returns False for `separate`. `tests/test_reroll_offers.py::test_a_finished_stem_split_is_not_offered_a_reroll`. |
| **service-05 — Reroll on LoRA training** | ~~`rerollable()` offers Reroll on `lora_train`; the new row gets no `train/` images and fails at dispatch. [_jobs_resubmit.py:60-80](../src/warlock/service/_jobs_resubmit.py), [_q_lora.py:34-36](../src/warlock/_q_lora.py). *Inspected*.~~ | **Built 2026-09-14:** `rerollable()` returns False for `lora_train`. `tests/test_reroll_offers.py::test_a_finished_lora_train_job_is_not_offered_a_reroll`. |
| **pipelines-02 — Manual 22: style LoRAs** | ~~"Style LoRAs … are SDXL-only" and "the picker is disabled on FLUX.2", then the same chapter describes the FLUX.2 klein pixel-art LoRA the picker offers. [22-generating-references.md:180-210](../docs/manual/22-generating-references.md), [models.py:1548](../src/warlock/models.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 22 states the FLUX.2 klein adapter as the exception to "SDXL-only" consistently. `tests/test_docs_audit_2026_09_14.py::test_manual_style_lora_sdxl_only_claim_does_not_contradict_the_flux2_entry`. |
| **pipelines-03 — `_appharness.isolate_home`** | ~~Clears six of the ten `WARLOCK_*` roots; `WARLOCK_TRELLIS_MODELS`, `_TRELLIS_RUNTIME`, `_FAMILIAR_RUNTIME`, `_FAMILIAR_MODELS` leak real paths into captures. [_appharness.py:41-48](../scripts/_appharness.py), [config.py:362-402](../src/warlock/config.py). *Inspected*.~~ | **Built 2026-09-14:** `_ROOTS_UNDER_HOME` is derived from `config.py`'s AST (every `_env_path` defaulting under `_home()`), ten roots where there were six. `tests/test_exercise_mode.py::test_isolate_home_clears_every_env_var_config_resolves_under_home`. |
| **pipelines-04 — `exercise_mode.py` REFUSED list** | ~~Settings' "Import a LoRA file..." and "Train from a folder..." open native pickers inline and match no REFUSED stem, so an unattended pass parks on a modal. [exercise_mode.py:78-112](../scripts/exercise_mode.py), [app_settings.py:1421-1440](../src/warlock/studio/panes/app_settings.py). *Inspected*.~~ | **Built 2026-09-14:** Both labels added to `REFUSED`; the test also counts inline picker calls in `app_settings.py`, so a third fails loudly. `tests/test_exercise_mode.py::test_exercise_mode_refuses_every_control_that_opens_a_native_file_dialog`. |
| **agents-03 — Bridge: malformed tool body** | ~~`splice_tool_result` sits outside `_dispatch_tools_call`'s try, so a non-object body raises `ValueError` and kills the bridge. [protocol.py:740-763](../src/warlock/mcp/protocol.py), [protocol.py:245-261](../src/warlock/mcp/protocol.py). *Reproduced* (`agents-protocol-02.py`).~~ | **Built 2026-09-14:** `splice_tool_result` runs inside the try and falls back to an `isError` reply. `tests/mcp/test_protocol.py::test_bridge_dispatch_refuses_a_malformed_tool_result_body_without_crashing`. |
| **agents-04 — `warlock_status` in task mode** | ~~`_call_task` queues `warlock_status` as a frame job, which answers "no such tool"; a Tasks client gets task mode for every call. [agent_host.py:1699-1732](../src/warlock/studio/agent_host.py). *Reproduced* (`agents-host-01.py`).~~ | **Built 2026-09-14:** `_call_task` answers `warlock_status` immediately. `tests/test_agent_host.py::test_a_task_mode_warlock_status_call_answers_immediately_instead_of_queuing_behind_the_frame_thread`. |
| **agents-05 — Modern-era notification** | ~~The `_meta` version check returns an error before the `has_id` gate, so a notification gets a reply. [protocol.py:640-648](../src/warlock/mcp/protocol.py). *Reproduced* (`agents-protocol-03.py`).~~ | **Built 2026-09-14:** `has_id` is checked before the version check. `tests/mcp/test_protocol.py::test_bridge_dispatch_never_replies_to_a_modern_notification_with_a_bad_protocol_version`. |
| **agents-06 — Refusal field mapping** | ~~`_mapped_field` returns before its `job_id` fallback when the door's `field` is empty, which is every `check_job_id`/`check_sheet_id` refusal. [agent_character.py:150-171](../src/warlock/studio/agent_character.py), [validation.py:735-763](../src/warlock/service/validation.py). *Reproduced* (`agents-character-02.py`).~~ | **Built 2026-09-14:** `_mapped_field` tries the declared id fallback when the door's field is empty, telling `job_id` from `sheet_id` by message on two-id tools. `tests/test_agent_character.py::test_a_bad_job_id_refusal_still_names_the_job_id_field`. |
| **agents-07 — Familiar apply on a closed tab** | ~~`apply()` mints a new document for any missing tab, including a previewed tab the user closed, where its docstring says it refuses. [apply.py:13-58](../src/warlock/studio/familiar/apply.py). *Inspected*. Not yet reachable in production (P54).~~ | **Built 2026-09-14:** `apply()` refuses a truthy `tab_uid` whose tab is gone (before, it minted a document and raised `KeyError`). `tests/familiar/test_apply.py::test_apply_refuses_when_the_previewed_tab_was_closed_not_never_opened`. |
| **docs-03 — SECURITY.md: Familiar listener** | ~~Scope never mentions `llama-server.exe`, a second loopback HTTP listener with an API-key file. [SECURITY.md:27-63](../SECURITY.md), [llama.py:67-185](../src/warlock/pipelines/llama.py). *Inspected*.~~ | **Built 2026-09-14:** SECURITY.md names `llama-server.exe` as a loopback listener behind a key file, spawned only when Familiar is used. `tests/test_docs_audit_2026_09_14.py::test_security_md_names_familiars_loopback_listener`. |
| **docs-04 — Measurement: pixel-art-xl** | ~~The 2026-08-06 pre-registration still reads as live and plans edits to `guidance.PRESETS`, deleted 2026-08-17; shipped sheets pin `sdxl_cfg`. [2026-08-06-pixel-art-xl.md](../docs/measurements/2026-08-06-pixel-art-xl.md), [tilesheets.py:77-81](../src/warlock/service/tilesheets.py). *Inspected*.~~ | **Built 2026-09-14:** A dated "Retired unrun" notice at the top, following `2026-08-10-zelda-props.md`'s precedent. `tests/test_docs_audit_2026_09_14.py::test_pixel_art_xl_preregistration_is_marked_retired_or_closed`. |
| **docs-05 — TODO F7 buildable** | ~~Backward knee signs in `jump crouch`/`jump land` are present verbatim; F7 names the replacement values. [humanoid.json](../src/warlock/templates/clips/humanoid.json), TODO.md F7. *Inspected*.~~ | **Built 2026-09-14:** Shins flipped to +0.6428 and +0.5299, and F7 struck in TODO.md; not re-rendered (no Blender here), and launch/rise/fall/apex left alone. `tests/test_clip_library_poses.py::test_jump_crouch_and_land_bend_the_knee_forward_not_backward`. |
| **docs-06 — TODO F9 buildable** | ~~`_h_op` checks only that `params` is a dict, so a wrong-typed param reaches `float()` in `clay_ops.run`. [agent_clay.py:4286-4297](../src/warlock/studio/agent_clay.py), [clay_ops.py:285](../src/warlock/studio/clay_ops.py), TODO.md F9. *Inspected*. `agent_clay.py` carries the user's uncommitted work.~~ | **Built 2026-09-14:** `agent_clay._op_params_type_refusal` refuses undeclared keys and non-numeric values by `field="params"`, and F9 struck in TODO.md. `tests/test_agent_clay_door_types.py::test_clay_op_refuses_a_wrong_typed_param_value_instead_of_crashing`. |
| **docs-07 — Manual 38: sweep axis names** | ~~Chapter 38 names axes "Style strength", "IP-Adapter scale"…; the Add axis combo shows raw keys because `FIELD_LABELS` is empty, and a test pins that. [38-review.md:135-142](../docs/manual/38-review.md), [settings_2d.py:187-191](../src/warlock/studio/panes/settings_2d.py), [review_panes.py:654](../src/warlock/studio/review_panes.py). *Inspected*.~~ | **Built 2026-09-14:** `FIELD_LABELS` restored with the eleven axis names chapter 38 prints. `tests/test_settings_2d_notes.py::test_sweep_add_axis_combo_shows_the_manuals_human_readable_axis_names`. |
| **shell-09 — Settings: LoRA pickers on the frame thread** | ~~*Raised during the fix phase by the pipelines-04 fixer.* "Import a LoRA file..." and "Train from a folder..." call `dialogs.open_file`/`select_folder` inline from the draw call, so the OS-modal picker blocks the frame loop; every other picker site under `studio/` submits it as a task. [app_settings.py:1421-1440](../src/warlock/studio/panes/app_settings.py). *Inspected*.~~ | **Built 2026-09-14:** Both pickers run inside keyed `ctx.submit("preview", run)` closures, like the neighbouring Train from my library; `test_exercise_mode.py`'s inline-picker check is now nesting-aware and expects none. `tests/test_app_settings_pickers.py::test_the_lora_import_and_train_buttons_never_open_a_picker_on_the_frame_thread`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **shell-05** | ~~`inspector.is_graded`'s memo is never updated by a verdict recorded in Review. [inspector.py:1392-1446](../src/warlock/studio/panes/inspector.py), [review_mode.py:972-1004](../src/warlock/studio/review_mode.py). *Inspected*.~~ | **Built 2026-09-14:** `inspector.mark_graded`, called by `review_mode.record` after a verdict lands. `tests/test_shell_audit_2026_09_14.py::test_a_verdict_recorded_in_review_mode_is_reflected_by_is_graded_in_another_host`. |
| **shell-06** | ~~`review_mode._REFERENCE_CACHE` grows without bound for the session. [review_mode.py:1932-1950](../src/warlock/studio/review_mode.py). *Inspected*.~~ | **Built 2026-09-14:** `_REFERENCE_CACHE` is an `OrderedDict` LRU capped at `_REFERENCE_CACHE_MAX` (4096). `tests/test_shell_audit_2026_09_14.py::test_reference_path_cache_does_not_grow_without_bound`. |
| **shell-07** | ~~Chapter 42's Models overview says "every model" but omits Familiar, music and separation. [42-app-settings.md:77-78](../docs/manual/42-app-settings.md). *Inspected*.~~ | **Built 2026-09-14:** Chapter 42's Models overview names every fetch-group heading the pane draws. `tests/test_docs_audit_2026_09_14.py::test_models_section_overview_names_every_fetch_group_heading`. |
| **shell-08** | ~~`zipguard` docstring says four doors; Mason `.wscn` and Sirens `.wsng` are two more. [zipguard.py:3-61](../src/warlock/studio/zipguard.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring names all six `BoundedZip` doors; the test derives them from the tree. `tests/test_shell_audit_2026_09_14.py::test_zipguard_docstring_names_every_module_that_opens_a_bounded_zip`. |
| **mason-06** | ~~`scene.resolve`'s docstring describes an `expand_prefabs=False` it does not accept. [scene.py:442-459](../src/warlock/studio/mason/scene.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring attributes `expand_prefabs=False` to `walk()`. `tests/mason/test_scene.py::test_resolve_docstring_claim_about_expand_prefabs_matches_its_actual_signature`. |
| **mason-07** | ~~`_MUTATING_CTRL` lists `i` and `m`, which nothing dispatches. [mason_mode.py:1097](../src/warlock/studio/mason_mode.py). *Inspected*.~~ | **Built 2026-09-14:** `i` and `m` removed. `tests/test_mason_mode.py::test_mutating_ctrl_keys_are_all_actually_dispatched`. |
| **clay-07** | ~~`duplicate_selected` iterates a set, so copies land in arbitrary order. [selection.py:155-179](../src/warlock/studio/clay/selection.py). *Inspected*.~~ | **Built 2026-09-14:** Iterates `sorted(doc.selection, key=doc.index_of)`. `tests/clay/test_select.py::test_duplicate_selected_preserves_the_objects_original_relative_order`. |
| **inker-08** | ~~`Document.gradient`'s share divide uses the masked-lane pattern (as inker-04). [_doc_paint.py:1509](../src/warlock/studio/inker/_doc_paint.py). *Inspected*.~~ | **Built 2026-09-14:** Safe denominator, pinned by AST and `errstate` as in inker-04. `tests/inker/test_inker_document.py::test_gradient_fill_does_not_raise_under_errstate_where_coverage_and_backdrop_are_both_transparent`. |
| **inker-09** | ~~INVARIANTS and `render.FrameCtx` say Flourish uids are reissued on every load; load preserves them, only preset insert reissues. [INVARIANTS.md:817](INVARIANTS.md), [render.py:49-50](../src/warlock/studio/inker/flourish/render.py). *Inspected*.~~ | **Built 2026-09-14:** `render.FrameCtx`'s comment and the INVARIANTS sentence now say load preserves uids and only preset insert reissues. `tests/inker/test_flourish_document.py::test_opening_a_flourish_document_preserves_its_layer_uids`. |
| **inker-10** | ~~`engines._godot` embeds the effect name unescaped in GDScript strings. [engines.py:122-146](../src/warlock/studio/inker/flourish/engines.py). *Inspected*.~~ | **Built 2026-09-14:** `_gdscript_string` escapes backslash, quote and newline. `tests/inker/test_flourish_export.py::test_a_quote_in_the_effect_name_does_not_break_the_godot_snippet`. |
| **inker-11** | ~~`ora._read_animation` has no per-frame palette-override ceiling. [ora.py:1442-1448](../src/warlock/studio/inker/ora.py). *Inspected*.~~ | **Built 2026-09-14:** A frame palette past `ixp.MAX_COLOURS` raises into the existing flat-read fallback. `tests/test_untrusted_input.py::test_read_animation_refuses_a_frame_palette_past_the_colour_ceiling`. |
| **inker-12** | ~~Open/import/palette doors discard `ctx.submit`'s False, so a second press is silent. [inker_open.py:35-217](../src/warlock/studio/inker_open.py), [inker_palette_io.py:73-384](../src/warlock/studio/inker_palette_io.py). *Inspected*.~~ | **Built 2026-09-14:** Every open, import and palette door in both files (fifteen, the four beyond the cited range included) toasts when `ctx.submit` answers False. `tests/test_inker_audit_2026_09_14.py::test_pressing_open_twice_while_the_picker_is_up_says_something`. |
| **inker-13** | ~~`_sort_and_ramp`'s docstring says indexed sorts are free; they are undo steps. [inker_colors.py:433-439](../src/warlock/studio/panes/inker_colors.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring separates the free RGB sort from the undoable indexed sort. `insert_ramp`'s docstring in `_doc_indexed.py`, which made the same claim, is corrected too. `tests/test_inker_audit_2026_09_14.py::test_sort_and_ramp_docstring_does_not_claim_indexed_sorts_are_free`, `::test_insert_ramp_docstring_does_not_claim_an_indexed_ramp_is_free`. |
| **inker-14** | ~~`_has_options`' docstring says eyedropper has no options; the code lists it. [inker_tools.py:1100-1115](../src/warlock/studio/panes/inker_tools.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring names only move as option-less. `tests/test_inker_audit_2026_09_14.py::test_has_options_docstring_names_only_move_as_optionless`. |
| **poser-04** | ~~Closing or opening an asset with a dirty skeleton draft warns about "pose changes". [poser_mode.py:696-737](../src/warlock/studio/poser_mode.py), [docmodes.py:463-485](../src/warlock/studio/docmodes.py). *Inspected*.~~ | **Built 2026-09-14:** `guard()` names the dirty draft through `_dirty_draft_noun` (skeleton, pose, or both). `tests/test_poser_mode.py::test_close_asset_confirm_names_the_skeleton_draft_not_the_pose`. |
| **troupe-03** | ~~`export_godot`'s docstring names `godotscene.rename_animations`; it is `glbio.rename_animations`. [characters.py:912](../src/warlock/service/characters.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring names `glbio.rename_animations`. `tests/test_character_docstrings.py::test_export_godot_docstring_names_the_module_that_actually_defines_rename_animations`. |
| **troupe-04** | ~~`movement_min_frames`' docstring says two callers still index `MOVEMENT_MIN_FRAMES`; neither does. [charsheet.py:220-229](../src/warlock/pipelines/charsheet.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring corrected; the dict stays exported. `tests/test_character_docstrings.py::test_movement_min_frames_docstring_does_not_claim_callers_that_do_not_exist`. |
| **plotter-03** | ~~A long material line's refusal names `prompt_items` without the line. [tilesheets.py:639-640](../src/warlock/service/tilesheets.py). *Inspected*.~~ | **Built 2026-09-14:** `create_tile_sheet` re-raises a line's `check_prompt` refusal prefixed "line N:", keeping `field="prompt_items"`. `tests/test_tileset_service.py::test_an_overlong_material_line_refusal_names_which_line`. |
| **plotter-04** | ~~`land_tileset`'s comment says the projection arm is reached only from tests; `use_as_tileset` reaches it. [plotter_tilesets.py:378-389](../src/warlock/studio/plotter_tilesets.py). *Inspected*.~~ | **Built 2026-09-14:** Comment corrected: `use_as_tileset` reaches the projection arm through `_sheet_or_tileset`. `tests/test_plotter_audit_2026_09_14.py::test_land_tileset_docstring_does_not_claim_the_projection_arm_is_test_only`. |
| **tour-01** | ~~Chapter 21 lists tours in a different order from `TOURS`' offer order. [21-home.md:24-33](../docs/manual/21-home.md), [scripts.py:717-724](../src/warlock/studio/tour/scripts.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 21's list follows `TOURS`; the "first three need no GPU" sentence now names Muse as the exception. `tests/test_docs_audit_2026_09_14.py::test_home_chapter_lists_tours_in_TOURS_offer_order`. |
| **pipelines-05** | ~~meshreport's "after welding" reason is used when the weld was skipped. [meshreport.py:100-129](../src/warlock/meshreport.py). *Reproduced* (`pipelines-mesh-01.py`).~~ | **Built 2026-09-14:** `build` tracks a skipped weld and says "unwelded". `tests/test_meshreport.py::test_watertight_reason_does_not_claim_welding_when_weld_was_skipped`. |
| **pipelines-06** | ~~`fetch_worker.py` still calls itself the one process allowed the network; there are three. [fetch_worker.py:1](../src/warlock/pipelines/fetch_worker.py). *Inspected*.~~ | **Built 2026-09-14:** "The first of three processes...". `tests/test_network_worker_docstrings.py::test_fetch_worker_docstring_does_not_claim_to_be_the_only_network_process`. |
| **pipelines-07** | ~~The tile family and tile-vs-sheet refusals run after the checkpoint load. [text2image.py:992-1035](../src/warlock/pipelines/text2image.py). *Inspected*.~~ | **Built 2026-09-14:** The tile-family refusal moved before `self.load()`, beside the tile/sheet check. `tests/test_text2image_tile_refusals.py::test_tile_on_a_non_sdxl_family_is_refused_before_the_checkpoint_loads`. |
| **agents-08** | ~~`bridge.py`'s docstring says exactly one fatal start-up case; a `hello` version mismatch is a second. [bridge.py:36-38](../src/warlock/mcp/bridge.py), [bridge.py:466-472](../src/warlock/mcp/bridge.py). *Inspected*.~~ | **Built 2026-09-14:** Docstring names both fatal start-up cases. `tests/mcp/test_bridge.py::test_bridge_docstring_names_every_fatal_startup_case_the_code_actually_has`. |
| **agents-09** | ~~`_h_reference_get` does not check the frame budget after `bounded_png`, as its docstring requires of callers; latent at today's constants. [agent_clay.py:5791-5811](../src/warlock/studio/agent_clay.py). *Inspected*.~~ | **Built 2026-09-14:** `_h_reference_get` applies `_h_render`'s base64 frame-budget check after `bounded_png`. `tests/test_agent_clay_door_types.py::test_reference_get_refuses_a_reply_too_large_for_the_wire_frame`. |
| **docs-08** | ~~TODO P54 hides an unblocked screenshots/exercise-mode refresh inside an entry gated on the T3 card. TODO.md P54. *Inspected*.~~ | **Built 2026-09-14:** Split out of P54 as TODO.md P55 (refresh `screenshots/`, `/exercise-mode clay`); P54 points at it. TODO.md is not under test. |
| **docs-09** | ~~Chapter 12 bolds "Background removal"; the pane says "Background". [12-tuning-what-you-get.md:92](../docs/manual/12-tuning-what-you-get.md), [settings_3d.py:136-138](../src/warlock/studio/panes/settings_3d.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 12 bolds **Background**, the pane's label. `tests/test_docs_audit_2026_09_14.py::test_manual_12_names_the_bg_removal_control_by_its_pane_label`. |
| **docs-10** | ~~Chapter 9 bolds "Tile layout"; the combo says "Layout". [09-building-a-map.md:30](../docs/manual/09-building-a-map.md), [settings_2d.py:395-415](../src/warlock/studio/panes/settings_2d.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 9 bolds **Layout**, the combo's label. `tests/test_docs_audit_2026_09_14.py::test_manual_09_names_the_tileset_layout_combo_by_its_pane_label`. |
| **docs-11** | ~~Chapter 24 says the toolbar carries up to twelve controls; the code comment says ten. [24-the-3d-viewport.md:53](../docs/manual/24-the-3d-viewport.md), [overlay.py:83-86](../src/warlock/studio/panes/overlay.py). *Inspected*.~~ | **Built 2026-09-14:** The manual's twelve was right (ten wrapped controls, Open in Inker, front yaw); `overlay.py`'s stale "ten" comment was fixed instead. `tests/test_docs_audit_2026_09_14.py::test_viewport_toolbar_control_count_matches_manual_claim`. |
| **docs-12** | ~~Chapter 34 lists sprite sizes to 128 then Custom 8–256; 256 is a preset. [34-troupe.md:155](../docs/manual/34-troupe.md), [troupe.py:52](../src/warlock/service/troupe.py). *Inspected*.~~ | **Built 2026-09-14:** Chapter 34 lists every preset `troupe.py` defines, 256 included, then Custom. `tests/test_docs_audit_2026_09_14.py::test_troupe_manual_lists_256_as_a_preset_sprite_size_not_custom_only`. |

## Human-only — owed, not open

Findings that need a card, a clean machine, real Aseprite or Tiled, or ears. These move to
`TODO.md` in the fix phase and are not counted as open here.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | none this run | — |

## Coverage

- **Slices and segments run:** all 16 rows, 59 segments (listed in the header). **Skipped:**
  `pipelines`/`workspace`, by rule under `all` (its files were read by the inker, troupe, poser,
  plotter and muse rows).
- **Files in no row of the table**, attached by the orchestrator to the nearest segment's brief:
  `cli.py`, `studio/modes.py` (shell/boot); `panes/bottom_pane.py`, `studio/tool_palette.py`
  (shell/chrome); `studio/colorwheel.py` (shell/widgets); `clay/analyze.py` (clay/mesh);
  `clay/scratch.py` (clay/document); `studio/_view_frame.py` (clay/mode); `templates/limbs/*`
  (poser/rig); `clipmaps.py`, `cliptransfer.py`, `service/clip_import.py`,
  `templates/clip_maps/*` (poser/poses); the `characters/` package (troupe/engine);
  `godotscene.py` (troupe/jobs); `service/evidence.py` (service/gates); `pipelines/llama.py`
  (pipelines/children); `pipelines/imageout.py` (pipelines/image); `bench/` (pipelines/install);
  `studio/familiar/` (agents/clay); manual chapters 17 and 46 (docs/F1, the table's owed
  widening). `studio/ninepatch.py` is already in inker/mode, though shell/widgets thought it
  unowned. `src/warlock/assets/` (4 data files) was not assigned.
- **Explorers that failed or timed out:** none.
- **Owed gaps from 2026-09-13, closed this run:** shell/widgets `widgets.py` (all 3,913 lines);
  clay/document `presets.py`; create/viewer `gizmo`, `markers`, `bonelines`, `math3d`,
  `programs`, `glctx`; create/panes chapter 23; inker/document's six `_doc_*`/`undo` files;
  inker/mode `inker_ops`, `inker_state`, `inker_mode`; inker/panes `inker_canvas`,
  `inker_timeline`; plotter/mode `plotter_mode`, `plotter_state`, `plotter_tilesets`,
  `plotter_layers`; plotter/map `tsx.py`; docs/F2 chapters 20, 22, 24, 27, 32, 33.
  **Partly closed:** pipelines/install `scripts/` — `rebuild.ps1`, `make_update_manifest.py`,
  `agent_bench.py`, `strip_font_pua.py`, `_campaign.py` read; `bench_native.py`,
  `calibrate_seam*.py`, `author_*.py`, `retexture_probe.py`, most of `_appharness.py`,
  `exercise_mode.py`, `screenshot_modes.py`, and `installer/build.ps1`/`warlock.iss` still not.
- **Files a segment owned but did not fully read, and why (room):** shell/boot `main.py`
  ~35–40% (2660–3009, 3240–3588, 4000–4360, 4405–5366) and `state.py` ~1,000 lines;
  shell/review-settings `app_settings.py` ~40%; create/workspace the `RECIPES` rows;
  create/panes `settings_2d` tile/sprite helpers and `validate`, parts of `settings_3d` and
  `settings_character`, five panels grep-only; clay/mesh `ops_topo.py` bridge/extrude;
  clay/document `primitives.py` generator bodies and part of `viewer/gltf.py`; **inker/document
  `_doc_tiles.py`, `_doc_slices.py`, `layers.py`, `selection.py`, `transform.py` (grep only)** and
  part of `document.py`/`_doc_indexed.py`; inker/sheets chapters 15 and 29; inker/codecs
  `aseout.py` ~620–1385 and `ora.py` ~200–1385; inker/flourish `noise.py`, `prims/__init__.py`
  200–466; **inker/panes `inker_bridge.py` ~900 lines**; poser/rig most `op_*` in
  `blender_worker.py`; **poser/mode ~2,500 lines of `poser_mode.py`** (clip timing setters, key
  edits); troupe/engine three archetype `generate.py` interiors; troupe/jobs ~700 lines of
  `service/characters.py`; troupe/render ~45% of `spritesynth.py`, ~65% of `sheet.py`, ~55% of
  `troupe_mode.py`; plotter/map `render.py`, `project.py`, `tools.py`; **plotter/mode
  `plotter_canvas.py` (3,625 lines, grep only)**; service/queue ~1,100 lines of `db.py` and ~1,400
  of `queue.py`; service/kinds the per-kind `_q_*` bodies (left to workspace rows), `_jobs_list`,
  `_jobs_rework`; service/gates `models.py` registry tables; muse/jobs `pipelines/acestep/`
  grep-walked by design; agents/clay `agent_prompts.py` and most individual `agent_clay.py`
  handlers; agents/character two schema/door test bodies; docs/C2 stable Sirens and Troupe
  paragraphs symbol-checked only; docs/D COMPAT's refused catalogue and MODELS.md's SDXL-family
  revision hashes; docs/E most `service/` docstrings; docs/F1 chapters 00–15, 38–41, 43–45 and
  docs/F2 chapters 23, 25, 26, 28–30 label-passed only.
- **Checklist items not evaluable in this scope:** pane-shaped classes (undo folding, greyed
  reasons, per-frame, blind-spots) do not apply to the headless and service segments, and
  service-shaped classes (sweep tables, cancel, recovery) to the panes; each explorer's return
  recorded which it skipped.
- **Process notes.** Two explorers wrote probes under the same name in the shared probes
  directory and one overwrote the other's `sirens-01.py` (sirens-02's reproduction must be re-run);
  later briefs named probes `<slice>-<segment>-NN.py`. An explorer probe (`pipelines-mesh-01.py`) left a
  724-byte `degenerate.glb` at the repository root (absent from the baseline `git status`), outside
  the scratchpad the constraints require; the orchestrator removed it at merge. Synergy MCP connected for about half the explorers and not at all for the
  rest; where it did, `kg_file_summary` on large modules was the one consistent saving.
- **Table repairs this run surfaced** (for `references/subsystems.md`): the unlisted files above;
  every INVARIANTS line number in the rows now points at unrelated paragraphs (the agents row's
  55–77 collide with them); manual numbers in the rows predate the renumbering; `docs` C1/C2 should
  split by lead-in count (1–190 / 191–382), not by 320.
- **Not done by this audit:** no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not close those.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ **Built YYYY-MM-DD:** what was done, and
the test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is
deleted rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it.
Nothing under `src/` or `scripts/` may cite this filename.
