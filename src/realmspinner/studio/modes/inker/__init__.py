"""Inker: pixel art and animation, one document mode.

The pixel engine is ``realmspinner.kernels.pixel`` (Plotter, Packwright and the
character sheets draw with it too). What is Inker's own lives here: the
document state and tabs (``state``, ``mode``), the op registry (``ops``), key
handling, open/export, playback, sheets, the walk-cycle bridge, and the
drawn half under ``ui/``."""
