"""Library: every asset made so far, as a grid and as a full-screen view.

The jobs themselves are ``realmspinner.service``'s; what is the Library mode's own
is the drawn half under ``ui/`` -- including the drag payload Mason's palette
and Create's stages offer the same rows through."""
