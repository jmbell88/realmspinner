"""Create: prompt to picture to mesh, one staged mode.

``engine/`` is the headless half -- what a form asks for, what it may not
ask for, and the keyword arguments a submit sends -- and imports no window;
``ui/`` is the stage rail, the command bar and the settings columns drawn
over it.
"""
