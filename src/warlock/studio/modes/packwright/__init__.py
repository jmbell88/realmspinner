"""Packwright: atlas packing, one document mode.

The packer, trimmer and atlas writers are the headless ``engine/``. What else
is Packwright's own lives here: the controller (``mode``), its document tabs
(``state``), its file layer (``fileio``) and the drawn half under ``ui/``."""
