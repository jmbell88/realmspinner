"""Plotter: tile maps, exported to Tiled.

The map model, its edits and the TMX/TSX/wmap formats are the headless
``engine/``. What else is Plotter's own lives here: the controller
(``mode``), the document tabs (``state``), the file layer (``fileio``), the
new-map questions (``setup``), getting a tileset onto the map
(``tilesets``), and the drawn half under ``ui/``."""
