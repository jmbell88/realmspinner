"""Poser: reusable poses and clips against a skeleton template or one rigged
asset.

The skeletons, clip library and pose records are ``warlock.kernels.rig``.
What is Poser's own lives here: the controller (``mode``) and the drawn half
under ``ui/``."""
