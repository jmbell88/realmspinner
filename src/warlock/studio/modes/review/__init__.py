"""Review: sweeps over one reference and the verdict loop that grades them.

The verdicts, findings and judge live in ``warlock.service``. What is
Review's own lives here: the controller (``mode``) and the drawn half under
``ui/``."""
