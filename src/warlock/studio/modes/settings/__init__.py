"""Settings: the app's own preferences, models, packs and updates.

The persisted settings file is ``studio/settings.py`` (every mode reads it);
what is the Settings mode's own is the pane that edits it, under ``ui/``."""
