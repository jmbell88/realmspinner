"""Troupe: a rigged 3D character to a sprite sheet (the one Experimental mode).

The headless frame table and QA live in ``engine/``; the ``troupe`` job kind
is ``warlock.service.troupe``. What else is Troupe's own lives here: the
controller (``mode``), its session state (``state``) and the drawn half
under ``ui/``."""
